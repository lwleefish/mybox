---
phase: 04-跨平台完善
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/modules/capture/src/capture.rs
  - crates/modules/palette/src/position.rs
  - crates/mybox-core/src/window.rs
  - crates/mybox-core/src/app.rs
  - crates/mybox-core/src/command.rs
  - crates/modules/palette/src/lib.rs
  - crates/modules/palette/src/session.rs
  - crates/modules/palette/src/ui.rs
  - crates/modules/palette/src/bin/palette_checks.rs
autonomous: true
requirements: [INFRA-04, FRMW-06]
user_setup: []

must_haves:
  truths:
    - "DPI 换算一致（成功标准 4 的验证基础，D-06/D-07）：capture 的 xcap 点坐标 × scale_factor 换算抽成 point_to_physical 纯函数，1.0/1.25/1.5/2.0 四个 scale 单测通过；palette compute_geometry @ scale 1.5 的物理尺寸与位置与手算一致"
    - "WR-01 修复：create_window 失败 → on_create_failed 回调触发 → palette session 复位 Hidden、has_live_window()==false → toggle 可再次 summon（不再永久卡死）"
    - "WR-02 修复：模块 on_event / on_event_win 回调 panic 被 catch_unwind 隔离，事件循环存活（不再经 CR-01 类 panic 被杀）"
    - "WR-03 修复：零命令时 summon 高度与帧循环同步高度都取 Empty 144px，fallback 块不再被 128px 窗口截断"
    - "IN-01 修复：run_command 线程 spawn 失败不主线程 panic，错误经 ui.run hop 到 finalize(Err) 渲染 Error 态"
    - "IN-04 修复：config_dir 不可用时 open_config/open_log 运行器显式返回错误，不再静默打开空路径/CWD 相对路径"
    - "IN-02/03/05/06 修复：锁序不变量文档化、realize_window 注释与实现一致（旧窗口被 destroy）、TextEdit 输入点 64 字符上限、E2E 探针几何/颜色具名常量"
  artifacts:
    - path: "crates/modules/capture/src/capture.rs"
      provides: "point_to_physical 纯函数 + 4 scale 用例"
      contains: "point_to_physical"
    - path: "crates/modules/palette/src/position.rs"
      provides: "compute_geometry @1.5 用例（补进 position::tests）"
      contains: "1.5"
    - path: "crates/mybox-core/src/window.rs"
      provides: "WindowSpec.on_create_failed 槽位（WR-01）"
      contains: "on_create_failed"
    - path: "crates/mybox-core/src/app.rs"
      provides: "notify_create_failed + dispatch_window_event（WR-02 catch_unwind）+ IN-04 config_dir 显式错误"
      contains: "catch_unwind"
    - path: "crates/mybox-core/src/command.rs"
      provides: "run_command spawn-Err hop（IN-01）+ BuiltinCommands Option<PathBuf>（IN-04）"
      contains: "failed to spawn command runner thread"
    - path: "crates/modules/palette/src/lib.rs"
      provides: "WR-03 effective 高度 conditional（summon + sync 双处）+ build_window_spec on_create_failed 接线"
      contains: "on_create_failed"
    - path: "crates/modules/palette/src/session.rs"
      provides: "on_create_failed 复位方法 + IN-02 锁序文档"
      contains: "on_create_failed"
    - path: "crates/modules/palette/src/ui.rs"
      provides: "TextEdit char_limit（IN-05）"
      contains: "char_limit"
    - path: "crates/modules/palette/src/bin/palette_checks.rs"
      provides: "realize_window destroy 旧窗口 + 注释修正（IN-03）+ 带几何/颜色具名常量（IN-06）"
      contains: "ACCENT_RGB"
  key_links:
    - from: "app.rs create_window 两个错误路径"
      to: "WindowSpec.on_create_failed 回调"
      via: "notify_create_failed(&mut spec)（take + invoke，只触发一次）"
      pattern: "on_create_failed"
    - from: "build_window_spec"
      to: "session.on_create_failed()"
      via: "on_create_failed: Some(Box::new(move || created_session.on_create_failed()))"
      pattern: "on_create_failed"
    - from: "App::window_event"
      to: "dispatch_window_event(state, event)"
      via: "抽取后按 handle_redraw 模式包 catch_unwind"
      pattern: "catch_unwind"
    - from: "run_command spawn Err / runner Err"
      to: "UiThreadProxy::run → finalize(gen, Err)"
      via: "dispatch_completion 共用 hop（无 loop 时入 pending，测试经 drain_pending 取回）"
      pattern: "dispatch_completion"
    - from: "summon_palette / sync_window_geometry"
      to: "ui::effective_window_height（零命令 → Empty 144）"
      via: "命令数为 0 时走 Empty 分支，杜绝 128/144 截断"
      pattern: "effective_window_height"
---

<objective>
修复 Phase 3 遗留的 9 项错误处理债（WR-01..03 + IN-01..06，D-08 全修）并完成 DPI 换算验证（D-06/D-07）：把 capture 唯一的逻辑→物理换算点抽成纯函数 + 多 scale 单测，确保 Windows 高 DPI（如 150%）下截图选区与实际捕获区域一致（成功标准 4）。

Purpose: D-08 锁定 9 项错误债一次性清完——WR-01/02 是「窗口创建失败卡死会话」「模块回调 panic 杀死事件循环」两类可用性/稳定性缺陷（T-4-02/03），IN-01/05 是输入与线程边界的健壮性缺口（T-4-04/05），IN-04 是配置路径静默降级（INFRA-04 Windows 路径关联项），IN-03/06 是探针可维护性债。D-06/D-07 锁定 DPI 工作为验证导向：winit 已自动设置 PER_MONITOR_AWARE_V2，无需自补 awareness，只需证明现有 scale_factor 换算在 Windows 高 DPI 下正确——headless 纯函数单测即验证载体（the agent's Discretion 第 3 项：纯函数单测为主）。
Output: 9 项错误债修复 + 对应单测；point_to_physical 纯函数 + 4 scale 用例；compute_geometry @1.5 用例；全仓测试绿、16 探针不回归。
</objective>

<execution_context>
@/Users/fish/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fish/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-跨平台完善/04-CONTEXT.md
@.planning/phases/04-跨平台完善/04-RESEARCH.md
@.planning/phases/04-跨平台完善/04-PATTERNS.md
@.planning/phases/04-跨平台完善/04-VALIDATION.md
@.planning/phases/03-命令面板/03-REVIEW.md

**决策锁（04-CONTEXT.md）：** D-06（DPI 验证导向，不补 awareness）、D-07（验证对象 = capture.rs:29-42 换算点 + position.rs compute_geometry）、D-08（9 项错误债全修）、D-09（9 项债归入 04-02）。
**验证契约：** 04-VALIDATION.md Per-Task Verification Map 的 04-02 各行（11 行）+ Wave 0 第 2-7 项。本 plan 内各任务**内嵌创建测试**（Wave 0 内联——先建测试断言再修实现），任务结束即可运行 `<automated>`，无 MISSING 引用。
**需求映射：** INFRA-04 ← IN-04（Windows 路径 config_dir 显式错误）；FRMW-06 ← WR-01/02 稳定性硬化 + DPI 验证（Windows 平台可用性基础）。04-01 已覆盖同一对需求的 CI 侧。

<interfaces>
<!-- 关键契约与测试接缝（已从源码核实，执行器直接使用，无需再探索） -->

From crates/mybox-core/src/window.rs:29-84 (WindowSpec——WR-01 新增槽位的宿主):
```rust
pub struct WindowSpec {
    pub kind: WindowKind, pub title: String, pub transparent: bool,
    pub always_on_top: bool, pub decorations: bool, pub visible: bool,
    pub inner_size: Option<(u32, u32)>,      // 物理像素
    pub position: Option<(i32, i32)>,        // 物理像素
    pub cursor_icon: Option<winit::window::CursorIcon>,
    pub on_event: Option<Box<dyn Fn(&winit::event::WindowEvent) + Send + Sync>>,
    pub on_event_win: Option<Box<dyn Fn(&Arc<winit::window::Window>, &winit::event::WindowEvent) + Send + Sync>>,
    pub on_draw: Option<Box<dyn Fn(&mut tiny_skia::PixmapMut, u32, u32) + Send + Sync>>,
    pub on_created: Option<Box<dyn Fn(WindowId) + Send + Sync>>,   // ← on_create_failed 跟随此模式
}
// Default 为全 None——新增字段在 Default 中补 None
```

From crates/mybox-core/src/app.rs (WR-01/WR-02 落点):
```rust
// create_window 的两个失败路径（均在 spec 移入 register 之前，可 &mut spec）：
// ① el.create_window(attrs) → map_err(MyboxError::Window)   (L368-370)
// ② (self.renderer_factory)(Arc::clone(&window)) → ?        (L402)
// on_created 在 register 之后 take 出并调用 (L406, L419-421)——on_create_failed 在错误路径 notify

// window_event 内 466-476（WR-02 包裹对象）：
if let Some(cb) = &state.spec.on_event { cb(&event); }
if let Some(cb) = &state.spec.on_event_win { if let Some(w) = &state.window { cb(w, &event); } }

// 既有 catch_unwind 模式（handle_redraw L441，WR-02 逐字镜像）：
let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| draw(pixmap, w, h)));
```

From crates/mybox-core/src/context.rs:148-161 (IN-01 测试接缝):
```rust
pub fn run(&self, f: Box<dyn FnOnce() + Send>) {   // 无 loop proxy → push 进 pending
    ...
}
#[cfg(test)] pub(crate) fn drain_pending(&self) -> Vec<Box<dyn FnOnce() + Send>> { ... }
// 测试模式：new() 后不 set_proxy → run 的闭包留在 pending → drain_pending 取回手动执行
```

From crates/mybox-core/src/command.rs (IN-01 + IN-04 落点):
```rust
pub fn build(bus: Arc<EventBus>, config_dir: PathBuf, log_path: PathBuf) -> Vec<Command>  // L90
pub fn build_with(bus: Arc<EventBus>, config_dir: PathBuf, log_path: PathBuf,
    opener: Arc<dyn Fn(&Path) -> anyhow::Result<()> + Send + Sync>,
    spawner: Arc<dyn Fn(&Path) -> anyhow::Result<()> + Send + Sync>) -> Vec<Command>      // L96
// run_command (L232-246)：thread::Builder::spawn(...).expect("spawn command runner thread") ← IN-01 替换点
// 现有测试调用点（改 Option 后需包 Some）：L286-293 builtins()、L333-339、L368-374、L402-408
```

From crates/modules/capture/src/capture.rs:32-46 (D-07 换算点——Task 1 抽取对象):
```rust
let geom = MonitorGeom {
    x: (f64::from(monitor.x()?) * f64::from(scale)).round() as i32,   // ← point_to_physical(f64::from(monitor.x()?), f64::from(scale))
    y: (f64::from(monitor.y()?) * f64::from(scale)).round() as i32,
    width: img.width(), height: img.height(),
};
```

From crates/modules/palette/src/position.rs:22-42 (D-07 验证对象):
```rust
pub fn compute_geometry(monitors: &[(f64,f64,f64,f64,f64)], cursor: (f64,f64), panel_logical: (f32,f32)) -> Option<PanelGeometry>
// 已有用例：@1x (L104-112)、@2x (L137-145) → Task 1 补 @1.5
// 手算锚点（Task 1 断言值）：monitor (0,0,1920,1080,1.5)，光标 (960,540)，面板 (600,560)
//   pw=900, ph=840, px=(0+960)*1.5-450=990, py=(0+540)*1.5-420=390 → inner (900,840), pos (990,390)
```

From crates/modules/palette/src/ui.rs (WR-03/IN-05 落点):
```rust
pub fn window_height(state: PaletteState, visible: usize) -> f32   // L59：Idle/Filtering 80+48·n；Empty/Error 144；Executing 112+48·n
// IN-05（L181-190）：egui::TextEdit::singleline(&mut text).id("palette-input").font(...).text_color(TEXT).frame(false).margin(ZERO)
//   → 追加 .char_limit(filter::MAX_QUERY_LEN)（输入点截断；session.set_input L298 的 take() 截断保留为 backstop）
```

From crates/modules/palette/src/lib.rs (WR-01/03 落点):
```rust
// L181 summon_palette：let height = ui::window_height(PaletteState::Idle, all.len().max(1));
// L527 sync_window_geometry：let logical_h = ui::window_height(session.state(), session.filtered().len().max(1));
// L390-395 build_window_spec on_created 接线（on_create_failed 跟随同一模式，复用 created_session clone）
// L545-549 close_palette：session.close() 取 id → windows.destroy(id)
```

From crates/modules/palette/src/bin/palette_checks.rs (IN-03/IN-06 落点):
```rust
// IN-03（L87-124 realize_window）：重新 register 前无 destroy——旧窗口留在 wm 里（Arc 存活）
//   → 在 wm.register 之前加 self.wm.destroy(self.created_id.take()) 并修正 doc comment
// IN-06（L2048-2065 accent_pixels_in_row_band）：y_top=(68.0*scale), y_bottom=(116.0*scale)，0xFF,0x60,0x00 像素三元组
//   → 抽具名常量 + 注释声明 presence-only 断言无法检测偏移（CR-01 教训）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DPI 换算纯函数抽取与多 scale 单测（D-06/D-07）</name>
  <files>crates/modules/capture/src/capture.rs, crates/modules/palette/src/position.rs</files>
  <read_first>
    - crates/modules/capture/src/capture.rs（32-46 行 capture_all_monitors 的换算点）
    - crates/modules/palette/src/position.rs（22-42 行 compute_geometry；95-151 行既有测试的 fixtures 风格）
    - .planning/phases/04-跨平台完善/04-RESEARCH.md（Pattern 3 物理像素统一——「唯一换算点」标注）
  </read_first>
  <action>
    1. `capture.rs`：在 `MonitorGeom` 之后新增纯函数（RESEARCH Pattern 3 的换算逻辑落点，headless 可测）：
       ```rust
       /// Logical→physical point conversion (RESEARCH Pattern 3 — the ONLY
       /// logical-to-physical conversion point). Round-half-away: the same
       /// `.round()` semantics as the pre-extraction code.
       pub fn point_to_physical(value: f64, scale: f64) -> i32 {
           (value * scale).round() as i32
       }
       ```
       将 38-39 行改为调用它：`x: point_to_physical(f64::from(monitor.x()?), f64::from(scale))`，y 同理。行为零变化（同一算式）。
    2. `capture.rs` 新增 `#[cfg(test)] mod tests`（文件目前无测试模块——新建；不要动 `CaptureFn` 注入链）：
       - 4 个 scale 用例：`(100.0, 1.0) → 100`、`(100.0, 1.25) → 125`、`(100.0, 1.5) → 150`、`(100.0, 2.0) → 200`
       - 舍入用例：`(33.0, 1.5) = 49.5 → 50`、`(33.0, 2.0) = 66.0 → 66`、`(20.0, 0.5) = 10.0 → 10`
       - 负坐标用例（虚拟屏幕多显示器）：`(-1920.0, 1.0) → -1920`、`(-100.0, 1.5) = -150.0 → -150`
    3. `position.rs` 补 `compute_geometry` @1.5 用例（加入既有 tests 模块，风格对齐 L137-145）：
       - `compute_geometry(&[(0.0, 0.0, 1920.0, 1080.0, 1.5)], (960.0, 540.0), (600.0, 560.0))`
         → `assert_eq!(g.inner_size, (900, 840)); assert_eq!(g.position, (990, 390));`
         （手算链：pw=600×1.5=900, ph=560×1.5=840, px=(0+1920/2)×1.5−900/2=1440−450=990, py=(0+1080/2)×1.5−840/2=810−420=390）
       - 非中心光标 @1.5：光标 (300, 200) → px=(0+960)×1.5−450=990 不变、py=(0+540)×1.5−420=390 不变——断言内尺寸与位置与中心光标一致（面板始终居中于光标所在屏）
    4. 运行 `cargo nextest run -p mybox-capture -p mybox-palette`——新旧用例全绿（既有 @1x/@2x/空 monitors 用例不得改动）。
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-capture point_to_physical；cargo nextest run -p mybox-palette position::tests</automated>
  </verify>
  <done>
    - `capture.rs` 含 `pub fn point_to_physical(value: f64, scale: f64) -> i32`，capture_all_monitors 的 x/y 经它换算（行为零变化）
    - `capture.rs` 含 4 scale + 舍入 + 负坐标测试，全绿
    - `position.rs` 含 compute_geometry @1.5 用例（inner (900,840) / pos (990,390)），全绿
    - 既有 position 用例（@1x、@2x、fallback、empty、flip）未被改动
    - `cargo nextest run -p mybox-capture -p mybox-palette` 全绿
  </done>
</task>

<task type="auto">
  <name>Task 2: 核心层错误债——WR-01 回调 + WR-02 catch_unwind + IN-01 spawn 失败 + IN-04 config_dir</name>
  <files>crates/mybox-core/src/window.rs, crates/mybox-core/src/app.rs, crates/mybox-core/src/command.rs</files>
  <read_first>
    - .planning/phases/03-命令面板/03-REVIEW.md（WR-01 L96-100、WR-02 L102-113、IN-01 L123-127、IN-04 L141-145 的修复方向）
    - crates/mybox-core/src/window.rs（29-84 行 WindowSpec——on_created 槽位是 on_create_failed 的模板）
    - crates/mybox-core/src/app.rs（362-429 行 create_window 两失败路径；466-476 行 window_event；145-147 行 IN-04）
    - crates/mybox-core/src/command.rs（232-246 行 run_command；90-102 行 build/build_with；280-410 行既有测试调用点）
    - crates/mybox-core/src/context.rs（148-161 行 UiThreadProxy::run/drain_pending——IN-01 测试接缝）
  </read_first>
  <action>
    1. **WR-01（window.rs + app.rs）——create_window 失败 → on_create_failed，session 不卡死：**
       - `window.rs` WindowSpec 新增槽位（镜像 on_created 的字段风格与 doc 注释格式）：
         ```rust
         /// Per-window creation-FAILURE callback: invoked by `App::create_window`
         /// on the main thread whenever the window cannot be created (OS window
         /// creation error or renderer construction error), so the requesting
         /// module can reset its state instead of wedging (WR-01 — the palette
         /// session would otherwise stay `has_live_window()==true` forever).
         /// No-arg: no `WindowId` exists when creation fails. `Send + Sync`
         /// matches the other per-window callbacks (may be invoked from the
         /// `about_to_wait` drain on the main thread).
         pub on_create_failed: Option<Box<dyn Fn() + Send + Sync>>,
         ```
         `Default` 补 `on_create_failed: None`。
       - `app.rs` 新增自由函数（与 `handle_redraw` 同级；两失败路径共用，take 保证只触发一次）：
         ```rust
         /// WR-01: invoke the spec's creation-failure callback exactly once.
         /// `take()` so a later retry of the same spec cannot double-fire.
         fn notify_create_failed(spec: &mut WindowSpec) {
             if let Some(cb) = spec.on_create_failed.take() {
                 cb();
             }
         }
         ```
       - `create_window` 两个失败路径（el.create_window 的 `map_err` 返回处 L370、renderer_factory 的 `?` L402——均在 spec 移入 register 之前，可 `&mut spec`）在返回 `Err` 前调用 `notify_create_failed(&mut spec)`：
         - 路径①（L368-370）改为：
           ```rust
           let window = match el.create_window(attrs) {
               Ok(w) => w,
               Err(e) => {
                   notify_create_failed(&mut spec);
                   return Err(MyboxError::Window(format!("create window '{:?}': {e}", spec.kind)));
               }
           };
           ```
         - 路径②（L402）：`let renderer = match (self.renderer_factory)(Arc::clone(&window)) { Ok(r) => r, Err(e) => { notify_create_failed(&mut spec); return Err(e); } };`
         - 注意路径②的 `?` 原来是 `anyhow` 传播——保持 `MyboxError`/`anyhow` 类型兼容（create_window 返回 `Result<WindowId>`，`anyhow::Error` 可直接返回）。
    2. **WR-02（app.rs）——模块回调 catch_unwind：**
       - 新增自由函数（与 `handle_redraw` 同级，可 headless 测试）：
         ```rust
         /// WR-02: dispatch the per-window event callbacks in panic isolation —
         /// a panicking module closure must not kill the event loop (mirrors
         /// `handle_redraw`'s catch_unwind; CR-01 proved a CJK keyword-tier
         /// highlight panic inside `on_event_win` kills the whole loop).
         fn dispatch_window_event(state: &mut crate::window::WindowState, event: &winit::event::WindowEvent) {
             if let Some(cb) = &state.spec.on_event {
                 let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| cb(event)));
             }
             if let Some(cb) = &state.spec.on_event_win {
                 if let Some(w) = &state.window {
                     let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| cb(w, event)));
                 }
             }
         }
         ```
       - `window_event`（L466-476）两段 if-let 替换为 `dispatch_window_event(state, &event)`（renderer match 分支保持不变）。
    3. **IN-01（command.rs）——spawn 失败 hop 到 Error 态：**
       - 新增自由函数（runner 失败与 spawn 失败共用同一 hop）：
         ```rust
         /// IN-01: hop a command-runner result to the main thread. Both the
         /// worker-thread completion and the spawn-failure path funnel through
         /// here — the palette's `finalize(gen, Err)` renders the Error state.
         fn dispatch_completion(
             ui: &UiThreadProxy,
             on_done: Box<dyn FnOnce(anyhow::Result<()>) + Send>,
             result: anyhow::Result<()>,
         ) {
             ui.run(Box::new(move || on_done(result)));
         }
         ```
       - `run_command` 的 `.expect("spawn command runner thread")` 改为：
         ```rust
         match std::thread::Builder::new().name(format!("mybox-cmd-{}", cmd.id)).spawn(move || {
             let result = pollster::block_on((cmd.runner)());
             dispatch_completion(&ui, on_done, result);
         }) {
             Ok(_) => {}
             Err(e) => {
                 log::error!("failed to spawn command runner thread: {e}");
                 dispatch_completion(&ui, on_done, Err(anyhow::anyhow!("failed to spawn command runner thread: {e}")));
             }
         }
         ```
         （`ui` 已在 spawn 前 clone；闭包 move 捕获的 `cmd`/`on_done` 在 spawn-Err 分支不再可用——spawn-Err 分支只用 `ui` 与错误消息，消息里不要引用 `cmd` 的 move 值。若编译器报 move 冲突，把 `cmd.id` 在 spawn 前拷成 `let cmd_id = cmd.id;` 并在消息中使用 `cmd_id`。）
    4. **IN-04（command.rs + app.rs）——config_dir 不可用 → 显式错误：**
       - `BuiltinCommands::build` / `build_with` 的 `config_dir: PathBuf`、`log_path: PathBuf` 改为 `Option<PathBuf>`（两个签名同步改，参数名不变）。
       - `open_config` 运行器（L136-144）：`let config_dir = config_dir.clone();` → 闭包内 `let dir = config_dir.clone().ok_or_else(|| anyhow!("config directory unavailable"))?; opener(&dir)`。
       - `open_log` 运行器同理：`log_path` Option，闭包内 `.ok_or_else(|| anyhow!("log path unavailable"))?` 后调 opener。
       - 既有测试调用点全部包 `Some(...)`（command.rs L286-293 builtins()、L333-339 quit、L368-374 restart、L402-408 open_config；`/tmp/...` 路径 → `Some(PathBuf::from(...))`）。
       - `app.rs:145-147` 改为显式错误处理：
         ```rust
         let config_dir = crate::config::config_dir();
         if let Err(e) = &config_dir { log::warn!("config dir unavailable: {e}"); }
         let config_dir_opt = config_dir.ok();
         let log_path_opt = config_dir_opt.as_ref().map(|d| d.join("logs").join("mybox.log"));
         for cmd in crate::command::BuiltinCommands::build(Arc::clone(&bus), config_dir_opt, log_path_opt) {
         ```
         （`config_dir()` 返回 `anyhow::Result<PathBuf>`（config.rs:158）——保留 Result 语义，不改为 Option；降级为空路径 `unwrap_or_default()` 的行为被移除。）
    5. **测试（先写断言再修实现，全部 headless）：**
       - `app.rs tests`：`create_failed_notifies_callback_once`——`WindowSpec { on_create_failed: Some(Box::new(|| flag.store(true))) , ..}` 传给 `notify_create_failed(&mut spec)` → flag==true 且 `spec.on_create_failed.is_none()`（take 语义）；再次调用不再触发。
       - `app.rs tests`：`panic_isolated_event_callbacks`——`WindowState::new(1, Panel, WindowId::from(1u64), None, Box::new(MockRenderer), spec)`，spec 的 `on_event`/`on_event_win` 各放一个「先置 flag 再 panic」的闭包（on_event_win 分支的 window 为 None——on_event 分支用 on_event 验证 catch；on_event_win 需要 `window: None` 时静默跳过，符合 L473-476 既有守卫）；调用 `dispatch_window_event(&mut state, &evt)`（evt 用 `WindowEvent::RedrawRequested`）→ 不 panic、flag 置位。
       - `command.rs tests`：`spawn_failure_hops_error_to_main_thread`——`UiThreadProxy::new()`（不 set_proxy）+ 返回 `Err(anyhow!("runner boom"))` 的 runner → `run_command(cmd, &proxy, on_done)`（on_done 记录收到的 Result）→ `wait_until(|| !proxy.drain_pending().is_empty())` 或轮询取回 → 执行取回的闭包 → 断言 on_done 收到 `Err`。注释明确：OS 级 spawn 失败无法确定性触发（资源型），spawn-Err 分支与 runner-Err 分支共用已测的 `dispatch_completion` hop，分支本身由源码断言覆盖（同 app.rs:847-850 的 ActiveEventLoop 约定）。
       - `command.rs tests`：`no_config_dir_builtins_bail`——`build_with(bus, None, None, noop_opener(), noop_spawner())` → 取 open_config 与 open_log 运行器 → `pollster::block_on(runner())` 均返回 `Err`，消息含 "config directory unavailable"（open_config）/ "log path unavailable"（open_log）。
    6. 全量回归：`cargo nextest run -p mybox-core`（改动涉及 core 三文件，跑全 core 套件确认无既有用例破坏）。
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-core create_failed；cargo nextest run -p mybox-core panic_isolated；cargo nextest run -p mybox-core spawn_failure；cargo nextest run -p mybox-core no_config_dir</automated>
  </verify>
  <done>
    - `WindowSpec` 含 `on_create_failed: Option<Box<dyn Fn() + Send + Sync>>`，Default 为 None
    - `create_window` 两个失败路径（el.create_window / renderer_factory）在返回 Err 前调用 `notify_create_failed`（源码可查）
    - `window_event` 经 `dispatch_window_event` 分发，on_event/on_event_win 均 catch_unwind（与 handle_redraw 同构）
    - `run_command` 无 `.expect("spawn command runner thread")`——spawn-Err 经 `dispatch_completion` 送 `on_done(Err)`
    - `BuiltinCommands::build`/`build_with` 签名改为 `Option<PathBuf>`，open_config/open_log 运行器在 None 时返回带消息的 Err；`app.rs:145` 不再 `unwrap_or_default()`
    - 4 个新测试全绿；`cargo nextest run -p mybox-core` 全绿（既有用例未破坏）
  </done>
</task>

<task type="auto">
  <name>Task 3: 面板层错误债——WR-01 接线 + WR-03 高度 + IN-02/03/05/06</name>
  <files>crates/modules/palette/src/lib.rs, crates/modules/palette/src/session.rs, crates/modules/palette/src/ui.rs, crates/modules/palette/src/bin/palette_checks.rs</files>
  <read_first>
    - .planning/phases/03-命令面板/03-REVIEW.md（WR-01 接线方向 L100、WR-03 L115-119、IN-02 L129-133、IN-03 L135-139、IN-05 L147-151、IN-06 L153-157）
    - crates/modules/palette/src/lib.rs（181 行 summon 高度、520-541 行 sync_window_geometry、380-397 行 build_window_spec on_created 接线、545-549 行 close_palette）
    - crates/modules/palette/src/session.rs（198-223 行 has_live_window/close——WR-01 楔子形态；290-316 行 set_input 截断 backstop）
    - crates/modules/palette/src/ui.rs（59-67 行 window_height、181-190 行 TextEdit）
    - crates/modules/palette/src/bin/palette_checks.rs（81-124 行 realize_window、2048-2065 行 accent_pixels_in_row_band）
  </read_first>
  <action>
    1. **WR-01 接线（session.rs + lib.rs）——create 失败复位 session：**
       - `session.rs` 新增方法（放在 `on_window_created` 附近）：
         ```rust
         /// WR-01: creation-failure reset — a summon whose window could not be
         /// created must leave the session summonable again. `summon()` moved
         /// the state to Idle with `window_id=None`; the failure never pairs an
         /// id, so `has_live_window()` would stay true forever (pending_close
         /// never clears). Reset to Hidden and drop the in-flight state; the
         /// next toggle summons fresh.
         pub fn on_create_failed(&self) {
             let mut inner = self.state.lock().unwrap();
             inner.state = PaletteState::Hidden;
             inner.window_id = None;
             inner.pending_close = false;
             inner.error = None;
             // geometry_revision intentionally untouched: no window exists to
             // sync, and the next summon bumps it anyway.
         }
         ```
       - `lib.rs` `build_window_spec`（L390-395 on_created 旁）追加：
         ```rust
         on_create_failed: Some(Box::new(move || created_session.on_create_failed())),
         ```
         （复用已有的 `created_session` clone——闭包捕获同一 Arc。）
       - 测试 `session.rs tests`：`create_failed_resets_session_and_unwedges`——`summon(vec![])` → `has_live_window()==true`（Idle）→ `on_create_failed()` → `state()==Hidden`、`has_live_window()==false`、`window_id().is_none()`；随后再次 `summon(vec![])` 成功（generation 递增），证明 toggle 不再卡死。
    2. **WR-03（ui.rs + lib.rs）——零命令 fallback 高度 144：**
       - `ui.rs` 在 `window_height` 旁新增（summon 与 sync 共用，杜绝两处 diverging）：
         ```rust
         /// WR-03: the effective summon/frame height — the zero-command
         /// fallback block (12+48+8+64+12 = 144px) must fit the window, so an
         /// empty command list uses the Empty geometry instead of the 128px
         /// 1-row Idle height that clipped it.
         pub fn effective_window_height(state: PaletteState, visible: usize, command_count: usize) -> f32 {
             if command_count == 0 {
                 window_height(PaletteState::Empty, 0)
             } else {
                 window_height(state, visible)
             }
         }
         ```
       - `lib.rs` L181（summon_palette）：`let height = ui::effective_window_height(PaletteState::Idle, all.len(), all.len());`
       - `lib.rs` L527（sync_window_geometry）：`let logical_h = ui::effective_window_height(session.state(), session.filtered().len().max(1), session.commands().len());`
       - 测试 `ui.rs tests`：`effective_height_zero_commands_uses_empty`——`effective_window_height(Idle, 0, 0)==144.0`、`effective_window_height(Filtering, 0, 0)==144.0`；非零命令回退既有表——`effective_window_height(Idle, 1, 1)==128.0`、`effective_window_height(Idle, 4, 4)==272.0`、`effective_window_height(Executing, 2, 2)==208.0`。
    3. **IN-02（session.rs + lib.rs）——锁序文档化：**
       - `session.rs` `ensure_winit_state`（493-518）doc comment 追加：
         > **Lock-order invariant (IN-02):** `state` → `egui_ctx` order here. The frame loop (`lib.rs` RedrawRequested arm) takes `egui_ctx` first then `state` via `ui::draw`. **Never call `ensure_winit_state` / `with_winit_state_mut` while holding `egui_ctx()`** — both orders are main-thread-only and never nest today; this comment documents the invariant so future edits cannot nest them into a deadlock.
       - `lib.rs` RedrawRequested 臂（293-322）既有注释旁追加一句指向该不变量：`// Lock-order invariant (IN-02): egui_ctx → state here — see ensure_winit_state's doc comment; never nest the opposite order.`
       - 不改任何锁代码（本 phase 无嵌套证据，REVIEW 方案一）。
    4. **IN-03（palette_checks.rs）——realize_window 注释属实 + 旧窗口销毁：**
       - `realize_window`（L87-124）在 `wm.register` 之前插入：
         ```rust
         // IN-03: destroy the previous round's window — `wm.register` retains
         // an Arc, so merely dropping `self.window` would leave the old palette
         // window alive (and visible) for the whole check duration.
         if let Some(prev) = self.created_id.take() {
             self.wm.destroy(prev);
         }
         ```
       - 修正 doc comment（81-86）：删除「dropping it closes it」的错误描述，改为「destroy the previous round's window via the WindowManager before re-registering (IN-03)」。
    5. **IN-05（ui.rs）——输入点 64 字符上限：**
       - TextEdit（181-190）追加 `.char_limit(filter::MAX_QUERY_LEN)`（在 `.margin(egui::Margin::ZERO)` 之后、闭括号之前）。`session.set_input` 的 `take(MAX_QUERY_LEN)` 截断（session.rs:298）保留为 backstop（headless 直调路径仍受保护）。
       - 测试 `session.rs tests`：`input_limit_truncates_paste`——`set_input(&"a".repeat(100))` → `input().chars().count()==filter::MAX_QUERY_LEN`（既有截断契约；TextEdit char_limit 本身是 egui 行为，由源码断言 + 04-UAT 交互项覆盖）。
    6. **IN-06（palette_checks.rs）——探针具名常量：**
       - `accent_pixels_in_row_band`（2048-2065）把魔法数字抽为文件级常量（放在函数上方，紧邻 03-10 段注释）：
         ```rust
         /// IN-06: named layout geometry — band top = input box 12..60 + 8px
         /// gap (logical y 68), band bottom = 116 = one 48px row later. Mirrors
         /// ui.rs's SP_MD/SP_2XL/SP_SM/SP_3XL layout arithmetic; if the layout
         /// changes, update these constants (the comment below explains why
         /// presence-only assertions cannot detect a shift — CR-01).
         const ROW_BAND_TOP_LOGICAL: f64 = 68.0;
         const ROW_BAND_BOTTOM_LOGICAL: f64 = 116.0;
         /// The exact ACCENT token (UI-SPEC #FF6000) — ui.rs's ACCENT constant.
         const ACCENT_RGB: (u8, u8, u8) = (0xFF, 0x60, 0x00);
         ```
         - `let y_top = (ROW_BAND_TOP_LOGICAL * scale).round() as usize;` / `let y_bottom = (ROW_BAND_BOTTOM_LOGICAL * scale).round() as usize;`
         - 像素比较改为 `if data[i] == ACCENT_RGB.0 && data[i + 1] == ACCENT_RGB.1 && data[i + 2] == ACCENT_RGB.2 && data[i + 3] > 0`
         - 函数 doc comment 追加 IN-06 说明：presence-only 断言无法检测偏移（CR-01 教训）；位置盲是文档化限制，非本次修复目标（REVIEW「at minimum」档）。
    7. **回归验证：**
       - `cargo nextest run -p mybox-core -p mybox-palette -p mybox-capture` 全绿
       - 探针抽查：`cargo run -p mybox-palette --bin palette_checks -- summon_render`（WR-01/03 改动涉及 summon/sync 路径）与 `cargo run -p mybox-palette --bin palette_checks -- keyword_highlight`（IN-06 改动涉及）——两者 exit 0
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-palette；cargo nextest run -p mybox-core -p mybox-palette -p mybox-capture；cargo run -p mybox-palette --bin palette_checks -- summon_render && cargo run -p mybox-palette --bin palette_checks -- keyword_highlight</automated>
  </verify>
  <done>
    - `build_window_spec` 含 `on_create_failed: Some(...)` → `session.on_create_failed()`；session 方法把 state 复位 Hidden、pending_close=false
    - `session.rs` 测试 `create_failed_resets_session_and_unwedges` 绿（summon → 失败复位 → 再 summon 成功）
    - `ui::effective_window_height` 存在且 summon/sync 两处调用；零命令 → 144；`effective_height_zero_commands_uses_empty` 测试绿
    - `ensure_winit_state` 与 frame loop 的锁序不变量均有文档注释（无锁代码改动）
    - `realize_window` 重新 register 前 destroy 旧窗口，doc comment 与实现一致
    - TextEdit 含 `.char_limit(filter::MAX_QUERY_LEN)`；`input_limit_truncates_paste` 测试绿
    - `accent_pixels_in_row_band` 用 ROW_BAND_* / ACCENT_RGB 具名常量 + IN-06 注释
    - 三 crate nextest 全绿；summon_render / keyword_highlight 探针 exit 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模块回调 → 核心事件循环 | 模块提供的 `on_event` / `on_event_win` / `on_draw` 闭包在核心分发路径上执行（WR-02 的 panic 隔离边界——CR-01 已证明可达） |
| 命令 worker 线程 → 主线程 | `run_command` 线程 spawn 失败/运行器错误经 `UiThreadProxy` 回到主线程（IN-01 的失败传播边界） |
| TextEdit 输入 → session 状态 | 用户可粘贴任意长度文本进查询框（IN-05 的输入边界，Security V5） |
| config_dir 解析 → builtin 文件打开 | `config_dir()` 失败时 builtin 的打开目标（IN-04 的路径边界——静默空路径/CWD 相对路径是错误降级） |
| create_window 失败 → 模块会话状态 | 窗口创建失败跨核心→模块的状态回写（WR-01 的配对边界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | DoS | `App::create_window` 失败路径（WR-01） | mitigate | 新增 `WindowSpec.on_create_failed` 无参回调，create_window 两个失败路径经 `notify_create_failed`（take + 单次触发）调用；palette 侧 `session.on_create_failed()` 复位 Hidden + 清 pending_close，`has_live_window()` 恢复 false——失败后 toggle 可再次 summon，会话不永久卡死（app.rs tests `create_failed_*` + session tests 覆盖） |
| T-4-03 | DoS | `window_event` 模块回调（WR-02） | mitigate | 抽取 `dispatch_window_event` 自由函数，on_event / on_event_win 均包 `catch_unwind(AssertUnwindSafe)`（逐字镜像既有 handle_redraw L441 模式）；panic 被吞、事件循环存活（app.rs tests `panic_isolated_*` 覆盖——CR-01 类 CJK panic 不再可杀循环） |
| T-4-04 | DoS | `run_command` 线程 spawn（IN-01） | mitigate | 移除 `.expect("spawn command runner thread")`；spawn-Err 与 runner-Err 共用 `dispatch_completion` → `ui.run` → `finalize(gen, Err)` 渲染 Error 态，主线程不 panic（command.rs tests `spawn_failure_*` 覆盖 hop 路径；OS 级 spawn 失败无法确定性触发，分支源码断言） |
| T-4-05 | DoS | TextEdit 查询输入（IN-05，Security V5） | mitigate | `.char_limit(filter::MAX_QUERY_LEN)` 在输入点截断 + `session.set_input` 的 `take(64)` 保留为 backstop——粘贴超长文本不再先显示后回弹，查询串长度恒 ≤64（session tests `input_limit_*` 覆盖契约） |
| T-4-09 | Integrity | builtin 文件打开目标（IN-04，INFRA-04） | mitigate | `BuiltinCommands::build/build_with` 改 `Option<PathBuf>`；config_dir 不可用时 open_config/open_log 运行器返回带消息的 `Err`，绝不静默打开空路径或 CWD 相对 `logs/mybox.log`；`app.rs` 显式 `log::warn!` + 传 None（command.rs tests `no_config_dir_*` 覆盖） |

*注：T-4-01/06/07/08（CI 供应链 / 探针剪贴板 / 公开仓库）由 04-01 计划覆盖。本 plan 不引入新依赖——零 Package Legitimacy Gate 触发。*
</threat_model>

<verification>
- Task 1 后：`cargo nextest run -p mybox-capture -p mybox-palette`（04-VALIDATION 采样率）
- Task 2 后：`cargo nextest run -p mybox-core`（core 三文件改动全量回归）
- Task 3 后：`cargo nextest run -p mybox-core -p mybox-palette -p mybox-capture` + 探针抽查（summon_render / keyword_highlight）
- 计划末：`cargo nextest run && cargo test -- --ignored`（本地 macOS 全套，04-VALIDATION wave 级采样）
- 推送后：04-01 的 windows-ci workflow 自动验证 Windows 编译 + 单测 + 16 探针（04-02 改动经 CI 回归——04-01 Task 3 建立的触发链）
</verification>

<success_criteria>
- D-08 九项错误债全部修复并有测试锚定：WR-01（on_create_failed 回调 + session 复位）、WR-02（dispatch_window_event catch_unwind）、WR-03（effective_window_height 零命令 144）、IN-01（spawn-Err hop）、IN-04（Option<PathBuf> + bail）、IN-02（锁序文档）、IN-03（realize_window destroy + 注释）、IN-05（char_limit）、IN-06（具名常量）
- D-06/D-07 达成：point_to_physical 纯函数 + 4 scale 用例 + compute_geometry @1.5 用例——高 DPI 选区/捕获一致性的可验证基础（成功标准 4）
- 全仓 nextest 绿 + 既有 16 探针不回归（本地 macOS）；Windows CI 由 04-01 workflow 在推送后自动回归
- 04-VALIDATION.md 的 04-02 各行状态翻绿（含新增测试文件）

<output>
Create `.planning/phases/04-跨平台完善/04-02-SUMMARY.md` when done
</output>
