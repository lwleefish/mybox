---
phase: 02-screenshot
plan: 02
type: execute
wave: 2
depends_on: [02-01]
autonomous: true
files_modified:
  - crates/modules/capture/src/overlay.rs
  - crates/modules/capture/src/selection.rs
  - crates/modules/capture/src/text.rs
  - crates/modules/capture/src/session.rs
  - crates/modules/capture/src/lib.rs
requirements: [CAP-02, CAP-03, CAP-05]

must_haves:
  truths:
    - "用户按热键/托盘「开始截图」后，每块显示器各出现一个覆盖窗口，显示捕获画面，选区外覆盖半透明黑色遮罩（CAP-02）"
    - "用户拖拽时，选区边框 + 尺寸标签（WxH 像素）实时跟随（CAP-03）"
    - "拖拽结束显示 8 个调整手柄（四角 + 四边中点），可拖动调整选区位置和大小（D-02）"
    - "按 ESC 全部覆盖窗口立即销毁，不复制任何内容（CAP-05, D-04）"
  artifacts:
    - path: "crates/modules/capture/src/overlay.rs"
      provides: "每屏一窗 WindowSpec 构建 + on_draw 合成（画面+遮罩+选区+手柄+WxH）+ on_event 交互路由"
      contains: "create_overlays"
    - path: "crates/modules/capture/src/selection.rs"
      provides: "拖拽选区 + 8 手柄命中/拖动 + normalize 纯逻辑"
      exports: ["hit_test_handle", "apply_handle_drag", "normalize"]
    - path: "crates/modules/capture/src/text.rs"
      provides: "ab_glyph 文本渲染（WxH 标签）"
      contains: "draw_text"
    - path: "crates/modules/capture/src/session.rs"
      provides: "selection 状态转换（Selecting/Selected）+ cancel() + overlay_ids 跟踪"
      contains: "cancel"
    - path: "crates/modules/capture/src/lib.rs"
      provides: "捕获完成后的覆盖窗口创建接线"
      contains: "create_overlays"
  key_links:
    - from: "overlay.rs on_draw 闭包"
      to: "session.shots"
      via: "捕获图像 premultiply 后 draw_pixmap 填充窗口"
      pattern: "premultiply_rgba8"
    - from: "overlay.rs on_event"
      to: "session 状态转换"
      via: "鼠标/键盘事件驱动 + WindowRequest::Redraw 重绘"
      pattern: "handle_event"
    - from: "session.cancel()"
      to: "WindowManagerHandle::destroy"
      via: "遍历 overlay_ids 逐窗销毁"
      pattern: "overlay_ids"
---

<objective>
交付截图流程的第一条端到端用户能力：**用户按热键/托盘触发截图 → 每屏覆盖窗口显示捕获画面与半透明遮罩 → 鼠标拖拽选择区域（实时边框 + WxH 尺寸）→ 8 手柄可调整 → ESC 一键取消**。本 plan 基于 02-01 的捕获后端（SessionState.shots）与框架渲染链路（on_draw + Redraw）。

Purpose: 让 Phase 2 第一个真实能力可见（成功标准 1/2/5），并落实 CAP-02 的"遮罩 + 选区内显示原画面"、CAP-03 的"拖拽选区实时反馈"、CAP-05 的"ESC 取消"与 D-02 的"8 手柄可调整选择"。
Output: overlay.rs（覆盖窗口 + 合成 + 交互）、selection.rs（纯选区逻辑）、text.rs（WxH 标签）、session.rs（选区状态机 + 取消）、lib.rs（捕获后建窗接线）。

⚠ 已知限制（MVP 接受，RESEARCH Open Question 1 / A3 决议）：覆盖窗口仅使用 winit `AlwaysOnTop`（Overlay profile），未提升到 macOS `screenSaver` 窗口层级——在 macOS 全屏应用上方或菜单栏上方可能不显示。本 plan 的手动验证项须确认该限制在开发机上可接受；若不可接受记录为 Phase 4 待办，不阻塞本 plan。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-screenshot/02-01-SUMMARY.md（本 plan 依赖的前置交付，读取其实际 API 形状）
@.planning/phases/02-screenshot/02-RESEARCH.md（Pattern 1/2/3、Pitfall 1/2/3/5/7、Code Example: tiny-skia 绘制）
@.planning/phases/02-screenshot/02-PATTERNS.md（overlay.rs / selection.rs / text.rs 段）
@.planning/phases/02-screenshot/02-CONTEXT.md（D-01/D-02/D-04）
@.planning/phases/02-screenshot/02-VALIDATION.md（CAP-02/03/05 测试映射）

# 关键接口（02-01 已交付，执行者直接使用）
<interfaces>
From crates/mybox-core/src/window.rs（02-01 扩展后）：
  pub struct WindowSpec { ..., pub on_event: Option<Box<dyn Fn(&winit::event::WindowEvent) + Send + Sync>>, pub on_draw: Option<Box<dyn Fn(&mut tiny_skia::PixmapMut, u32, u32) + Send + Sync>> }
  pub enum WindowRequest { Create(WindowSpec), Destroy(WindowId), Redraw(WindowId) }
  pub struct WindowManagerHandle { pub fn create(&self, spec); pub fn destroy(&self, id); pub fn redraw(&self, id); }
From crates/modules/capture/src/session.rs（02-01 交付）：CaptureSession { state(): Arc<parking_lot::Mutex<SessionState>>, store_shots(shots) }；SessionState { shots: Vec<(MonitorGeom, RgbaImage)>, ... }
From crates/modules/capture/src/capture.rs：pub struct MonitorGeom { pub x: i32, pub y: i32, pub width: u32, pub height: u32 }（物理像素）
From crates/mybox-core/src/lib.rs（02-01 新增）：pub use tiny_skia;
xcap 类型：xcap::image::RgbaImage（RGBA8 straight alpha）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 覆盖窗口创建 + 画面/遮罩合成（CAP-02）</name>
  <files>
    crates/modules/capture/src/overlay.rs
    crates/modules/capture/src/lib.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/test/src/lib.rs">WindowSpec 结构体字面量 + create 入队模式（行 67-72）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/bin/display_checks.rs">draw-then-present 形状（行 86-99）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/window.rs">WindowKind::Overlay 字段、WindowManagerHandle::create/destroy/redraw</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Pattern 3（每屏一窗物理几何）、Pitfall 1（先捕获后建窗）、Pitfall 2（straight→premultiplied）、Pitfall 5（物理像素坐标）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-PATTERNS.md">overlay.rs 段（WindowSpec 字面量、draw-then-present 形状）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/lib.rs">本 Task 修改的 02-01 交付：init 订阅与 start_capture 完成回调（store_shots 后接 create_overlays 的接线点）</file>
  </read_first>
  <action>
    <step>overlay.rs 定义纯函数 `pub fn premultiply_rgba8(rgba: &[u8]) -> Vec<u8>`：把 straight RGBA8 字节转 premultiplied RGBA8（`r = r*a/255` 等三通道，a 不变），供 `tiny_skia::Pixmap::from_vec` 使用（Pitfall 2；单元测试用已知颜色断言，复用 Phase 1 `premul_rgba_to_u32` 测试风格）。</step>
    <step>overlay.rs 定义 `pub fn create_overlays(session: &CaptureSession, windows: &WindowManagerHandle)`：遍历 `session.state().lock().shots`（每项 `(MonitorGeom, RgbaImage)`），对每屏构建 `WindowSpec { kind: WindowKind::Overlay, title: "capture-overlay".to_string(), inner_size: Some((geom.width, geom.height)), position: Some((geom.x, geom.y)), on_event: Some(Box::new(闭包 → session.handle_event(event))), on_draw: Some(Box::new(闭包 → 合成)), ..Default::default() }` 并 `windows.create(spec)` 入队。on_draw 闭包捕获该屏 `monitor_index`（shots 下标）与 `MonitorGeom` 克隆。捕获先于建窗（shots 已存，Pitfall 1 满足）。</step>
    <step>on_draw 合成闭包（每次重绘全量重画，immediate-mode）：(1) 锁读 SessionState；(2) 取该 monitor_index 的 RgbaImage → `premultiply_rgba8` → `tiny_skia::Pixmap::from_vec(bytes, w, h)` → `pm.draw_pixmap(0, 0, PixmapRef::from_pixmap(&pixmap), &Paint::default(), Transform::identity(), None)` 铺满窗口；(3) 遮罩：若 `state.selection` 的 monitor_index == 本屏，画 4 个半透明黑 fill_rect（selection 矩形上/下/左/右外侧，颜色 `Color::from_rgba8(0,0,0,0x80)` 左右，本屏外全屏遮罩 `Color::from_rgba8(0,0,0,0x80)` 铺满）；选区本身不遮罩（选区内显示原始画面，CAP-02）。</step>
    <step>lib.rs：把 02-01 Task 3 的捕获完成 `ui.run` 闭包改为「`session.store_shots(shots)` 后调用 `overlay::create_overlays(&session, &windows)`」，并同时把每屏 expected 数写入 `session.pending_overlays = shots.len()`；`windows` 句柄需 clone 进 start_capture。</step>
    <step>session.rs 增加 `pending_overlays: usize` 字段默认 0、`overlay_ids: Vec<WindowId>` 默认空，并增加 `pub fn window_created(&self, id: WindowId)`：若 `pending_overlays > 0` 则 `overlay_ids.push(id)` 且 `pending_overlays -= 1`（与 framework WindowCreated 事件配对，用于 ESC/确认销毁，CAP-05 前置）。lib.rs init 订阅 `EventFilter::kind("core", "window-created")` → `session.window_created(id)`。</step>
  </action>
  <acceptance_criteria>
    <criterion>overlay.rs 含 `premultiply_rgba8`（单测：straight (255,128,64,128) → premultiplied (128,64,32,128)；alpha=0 → (0,0,0,0)）。</criterion>
    <criterion>overlay.rs 含 `create_overlays`，每个 shots 项构建一个 `WindowKind::Overlay` WindowSpec，position/inner_size 为物理像素（geom.x/geom.y/geom.width/geom.height），on_event 与 on_draw 均 Some。</criterion>
    <criterion>on_draw 合成顺序为：图像 blit → 遮罩 4 fill_rect（或全屏遮罩）→（Task 3 补选区）——遮罩只在 selection 外部（单测：无 selection 时全屏遮罩像素 alpha>0；有 selection 时选区中心像素保持原图）。</criterion>
    <criterion>lib.rs 捕获完成路径调用 `overlay::create_overlays`；init 订阅 `core/window-created` 调 `session.window_created`。</criterion>
    <criterion>session.rs 含 `pending_overlays` / `overlay_ids` / `window_created`。</criterion>
    <criterion>`cargo nextest run -p mybox-capture overlay::tests session::tests` 绿；`cargo check --workspace` 退出 0。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-capture overlay:: session:: && cargo check --workspace</automated>
  </verify>
  <done>覆盖窗口出现并显示捕获画面 + 遮罩（CAP-02）；每屏一窗按物理几何放置。</done>
  <commit_message>feat(capture): per-monitor overlay windows compositing capture + mask</commit_message>
</task>

<task type="auto">
  <name>Task 2: 选区状态机 + 8 手柄（纯逻辑, CAP-03, D-02）</name>
  <files>
    crates/modules/capture/src/selection.rs
    crates/modules/capture/src/session.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/renderer/mod.rs">纯函数 + 无头测试风格（premul_rgba_to_u32 行 37-48, 测试行 50-109）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-PATTERNS.md">selection.rs 段（hit_test_handle/apply_handle_drag/normalize）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">CursorMoved 已是物理坐标（CAP-03 行）；D-02 8 手柄</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-CONTEXT.md">D-02 可调整选择、D-03 无显式模式</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/session.rs">本 Task 修改的 02-01 交付：SessionState/CaptureSession 现有形状（Phase 枚举与选区状态字段将在此追加）</file>
  </read_first>
  <action>
    <step>selection.rs 定义 `pub enum Handle { N, NE, E, SE, S, SW, W, NW }`（8 手柄：四角 + 四边中点）。</step>
    <step>selection.rs 纯函数（全部无头可测）：`pub fn normalize(r: SelectionRect) -> SelectionRect`（保证 x0<=x1, y0<=y1）；`pub fn handle_rect(sel: &SelectionRect, h: Handle, size: f32) -> tiny_skia::Rect`（手柄的矩形位置）；`pub fn hit_test_handle(sel: &SelectionRect, pos: tiny_skia::Point, size: f32) -> Option<Handle>`（pos 落入某手柄矩形则返回该手柄）；`pub fn apply_handle_drag(sel: &SelectionRect, h: Handle, pos: tiny_skia::Point) -> SelectionRect`（按手柄移动对应边/角，最小尺寸 clamp 到 >= 4px）；`pub fn drag_start(sel: &SelectionRect, pos: tiny_skia::Point) -> SelectionRect`（新选区起点）；`pub fn drag_update(sel: &SelectionRect, pos: tiny_skia::Point) -> SelectionRect`（终点更新，normalize）。</step>
    <step>session.rs 增加选区状态转换：`pub enum Phase { Idle, Selecting, Selected }`；SessionState 增 `phase: Phase`（默认 Idle）与 `selection: Option<(usize, SelectionRect)>`（元组 = monitor_index + 局部选区）；CaptureSession 方法：`pub fn on_mouse_down(&self, monitor: usize, pos: tiny_skia::Point)`（Idle→Selecting：drag_start）、`pub fn on_mouse_move(&self, monitor: usize, pos: tiny_skia::Point)`（Selecting 中 drag_update；Selected 中若有 active_handle 则 apply_handle_drag）、`pub fn on_mouse_up(&self)`（Selecting→Selected：固定选区）、`pub fn set_active_handle(&self, h: Option<Handle>)`、`pub fn active_handle(&self) -> Option<Handle>`。</step>
    <step>SelectionRect/Tool/Annotation 若已在 02-01 session.rs 声明，保持并补全 selection 相关；所有状态转换方法加 `#[cfg(test)]` 可测（纯逻辑，不依赖 winit）。</step>
  </action>
  <acceptance_criteria>
    <criterion>selection.rs 含 Handle 枚举（8 变体）与 normalize/handle_rect/hit_test_handle/apply_handle_drag/drag_start/drag_update 全部纯函数。</criterion>
    <criterion>单测覆盖：drag_start→drag_update 产生归一化选区；8 个手柄命中测试各返回正确 Handle；apply_handle_drag 移动正确边/角并 clamp 最小 4px；normalize 对反序坐标归一。</criterion>
    <criterion>session.rs 含 Phase 枚举与 on_mouse_down/on_mouse_move/on_mouse_up/set_active_handle 状态转换，阶段迁移 Idle→Selecting→Selected。</criterion>
    <criterion>`cargo nextest run -p mybox-capture selection::tests session::tests` 全绿。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-capture selection:: session::</automated>
  </verify>
  <done>拖拽选区的纯逻辑完备：新建、8 手柄命中与调整、归一化，全部无头单测绿。</done>
  <commit_message>feat(capture): pure selection state machine with 8 resize handles</commit_message>
</task>

<task type="auto">
  <name>Task 3: 选区渲染 + WxH 标签 + ESC 取消（CAP-03, CAP-05, D-04）</name>
  <files>
    crates/modules/capture/src/text.rs
    crates/modules/capture/src/overlay.rs
    crates/modules/capture/src/session.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Code Example: ab_glyph 文本光栅化（行 408-426）、Pitfall 3（Redraw 请求）、Pitfall 7（键盘焦点）、Assumption A4（Arial.ttf）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/app.rs">on_event 路由（行 366-369 cb(&event)）与 window-created 事件</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-CONTEXT.md">D-04（ESC 一键取消全部）、CAP-03（WxH 实时）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/session.rs">本 Task 修改的 02-01 交付：SessionState 现有字段（cancel/overlay_ids 将在此追加）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/overlay.rs">本 Task 修改的 02-02 Task 1 交付：on_draw/on_event 现有合成与交互接线（选区渲染/ESC 将在此追加）</file>
  </read_first>
  <action>
    <step>text.rs 定义 `pub fn load_font() -> ab_glyph::FontArc`：`FontArc::try_from_slice(include_bytes!("assets/DejaVuSans.ttf"))` 失败则回退 `std::fs::read("/System/Library/Fonts/Supplemental/Arial.ttf")`（A4，macOS 已验证存在），用 `std::sync::OnceLock<FontArc>` 缓存。</step>
    <step>text.rs 定义 `pub fn draw_text(pm: &mut tiny_skia::PixmapMut, font: &ab_glyph::FontArc, text: &str, at: (f32, f32), size: f32)`：`font.scaled_glyph(font.glyph_id(c))` → `outline_glyph` → `og.draw(|gx, gy, cov| 把 coverage 作为 alpha 混合进 pixmap)`（颜色白色、1px 黑色描边可选——本 Task 仅要求白色实心，颜色参数留默认）。</step>
    <step>session.rs 增加 `pub fn cancel(&self)`：阶段重置为 Idle、`selection` 置 None、返回待销毁的 `overlay_ids` 克隆（`Vec<WindowId>` 取出后清空自身）。</step>
    <step>overlay.rs 的 on_draw 合成闭包在图像 + 遮罩之后追加：(1) 若 `state.selection` 的 monitor_index == 本屏，用 `tiny_skia::Rect::from_ltrb` + `stroke_rect` 画选区边框（颜色白、width 1.5）；(2) 用 selection::handle_rect 遍历 8 个 Handle 画手柄（6px 白色填充小方块 + 1px 黑描边）；(3) 用 text::draw_text 画 WxH 标签 `"{w} × {h}"`（w/h = selection 宽高物理像素，位置在选区左上角外侧上方，字号 18）。(4) 每次重绘全量重画（immediate-mode）。</step>
    <step>overlay.rs 的 on_event 闭包处理：`WindowEvent::CursorMoved{ position, .. }` → `session.on_mouse_move(monitor, position 转 tiny_skia::Point)` + 请求本窗重绘；`WindowEvent::MouseInput{ state: Pressed, .. }` → `on_mouse_down`（若光标命中 8 手柄之一则 set_active_handle）→ 重绘；`Released` → `on_mouse_up` + set_active_handle(None) → 重绘；`WindowEvent::KeyboardInput{ event: KeyboardInput{ logical_key: Key::Escape, state: Pressed, .. }, .. }` → `session.cancel()` → 对返回的每个 overlay_id 调 `windows.destroy(id)`（CAP-05, D-04：一键取消全部，不复制）。重绘请求统一走 `for id in overlay_ids.clone() { windows.redraw(id) }`（或最小化为本窗 id）。</step>
    <step>重绘路径验证：每次状态变更后必须请求重绘（Pitfall 3——`ControlFlow::Wait` 不会自动重绘）。</step>
  </action>
  <acceptance_criteria>
    <criterion>text.rs 含 `load_font()`（Arial.ttf 回退 + OnceLock）与 `draw_text()`（ab_glyph outline_glyph.draw 覆盖率混合）。</criterion>
    <criterion>session.rs 含 `cancel()`：重置 Idle、清 selection、返回 overlay_ids 并清空。</criterion>
    <criterion>on_draw 在 selection 存在时绘制：选区边框 stroke_rect、8 手柄、WxH 标签（用 selection::handle_rect 与 text::draw_text）。</criterion>
    <criterion>on_event 处理 CursorMoved/MouseInput(Pressed/Released)/KeyboardInput(Escape)，每状态变更后请求重绘（源码断言：on_event 内调用 windows.redraw 或等价重绘请求）。</criterion>
    <criterion>单测 `session::tests::cancel`：on_mouse_down/up 产生 Selected 选区后 cancel 返回含该 overlay_id 的 Vec 且清空、phase 回 Idle、selection 为 None（02-VALIDATION CAP-05）。</criterion>
    <criterion>无头单测 `text::tests`：draw_text 在 40x20 Pixmap 写 "12 × 34" 后非空（中心行存在 alpha>0 像素）。</criterion>
    <criterion>`cargo nextest run -p mybox-capture` 全绿；`cargo check --workspace` 退出 0。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-capture && cargo check --workspace</automated>
  </verify>
  <done>覆盖窗口显示捕获画面 + 遮罩 + 选区边框 + 8 手柄 + WxH 标签；ESC 一键销毁全部覆盖窗口（CAP-02/03/05, D-02/D-04）。</done>
  <commit_message>feat(capture): selection rendering with WxH label and ESC cancel</commit_message>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 鼠标/键盘事件 → 模块状态 | on_event 在主线程执行，事件坐标（PhysicalPosition）为不可信输入，需 clamp 到窗口边界 |
| 模块状态 → 渲染闭包 | on_draw 每帧全量重画，读共享 SessionState；panic 已在 02-01 用 catch_unwind 兜底 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | SessionState.shots（像素驻留内存） | mitigate | 02-04 确认/取消时清空 shots；本 plan 的 cancel 至少清空 overlay_ids 与 selection |
| T-2-02 | Spoofing / Integrity | 覆盖窗口自我捕获 | mitigate | 捕获先于建窗（02-01 顺序保证，Pitfall 1）；覆盖窗口创建不触发重新捕获 |
| T-2-05 | Availability | on_event 越界坐标 | mitigate | 事件坐标 clamp 到窗口（0..width/0..height）再进 selection 状态机；空 shots/空 selection 时 on_draw 安全返回（guard） |
| T-2-06 | Availability | ESC 重复触发销毁 | mitigate | cancel 幂等：overlay_ids 取出后即清空，重复 ESC 无窗口可销毁 |
| T-2-07 | Availability | Redraw 风暴 | mitigate | 每次输入最多一次批量重绘（对 overlay_ids 循环 redraw），无 60fps 轮询（保持 ControlFlow::Wait，Pitfall 3） |
</threat_model>

<verification>
- 快速套件：`cargo nextest run -p mybox-core -p mybox-capture` 退出 0
- 显示会话（手动/`#[ignore]`）：`cargo run -p mybox-app` → 按托盘「开始截图」（或已配热键）→ 每屏出现覆盖窗口显示捕获画面 + 遮罩 → 拖拽选区出现边框与 WxH → ESC 关闭
</verification>

<success_criteria>
- [ ] 覆盖窗口按每屏物理几何出现，显示捕获画面 + 选区外半透明遮罩（CAP-02）
- [ ] 拖拽产生选区，边框 + WxH 标签实时跟随（CAP-03）
- [ ] 8 手柄（四角 + 四边中点）可拖动调整选区（D-02）
- [ ] ESC 销毁全部覆盖窗口、不复制（CAP-05, D-04）
- [ ] selection 纯逻辑 + cancel 单测全绿
</success_criteria>

<output>
Create `.planning/phases/02-screenshot/02-02-SUMMARY.md` when done
</output>
