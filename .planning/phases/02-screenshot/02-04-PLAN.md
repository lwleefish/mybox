---
phase: 02-screenshot
plan: 04
type: execute
wave: 4
depends_on: [02-03]
autonomous: true
files_modified:
  - crates/modules/capture/src/clipboard.rs
  - crates/modules/capture/src/session.rs
  - crates/modules/capture/src/overlay.rs
  - crates/modules/capture/src/permission.rs
  - crates/modules/capture/src/lib.rs
  - crates/modules/capture/src/bin/capture_checks.rs
  - crates/modules/capture/tests/integration.rs
  - crates/modules/capture/tests/manual_checklist.md
requirements: [CAP-04, CAP-08]

must_haves:
  truths:
    - "用户按 Enter 或点工具栏确认后，当前选区（含标注）复制到系统剪贴板，覆盖窗口立即关闭（CAP-04, D-01, D-04）"
    - "不标注直接确认则复制原始选区图像（D-01）"
    - "按 ESC 取消不复制任何内容（02-02 已交付，本 plan 验证闭环）"
    - "macOS 无屏幕录制权限时：首次触发请求授权（CGRequest），被拒后引导打开系统设置对应面板（CAP-08）"
  artifacts:
    - path: "crates/modules/capture/src/clipboard.rs"
      provides: "选区裁剪 + 标注烘焙 + arboard set_image（RGBA8 straight）"
      contains: "set_image"
    - path: "crates/modules/capture/src/session.rs"
      provides: "confirm() 状态 + 清空 shots（drop-before-close）"
      contains: "confirm"
    - path: "crates/modules/capture/src/permission.rs"
      provides: "CGRequestScreenCaptureAccess + 系统设置深链引导"
      contains: "CGRequestScreenCaptureAccess"
    - path: "crates/modules/capture/src/lib.rs"
      provides: "权限拒绝时的请求 + 引导流程"
      contains: "open_system_settings"
    - path: "crates/modules/capture/src/bin/capture_checks.rs"
      provides: "子进程式 #[ignore] 端到端检查（overlay/drag/enter/esc）"
      contains: "check_"
    - path: "crates/modules/capture/tests/manual_checklist.md"
      provides: "人工验证清单（成功标准 1-6）"
      contains: "截图"
  key_links:
    - from: "Enter/确认输入"
      to: "clipboard.rs copy_selection"
      via: "session.confirm() → 裁剪+烘焙 → arboard set_image（主线程受限作用域）"
      pattern: "copy_to_clipboard"
    - from: "clipboard.rs"
      to: "WindowManagerHandle::destroy"
      via: "复制成功后遍历 overlay_ids 销毁全部覆盖窗口"
      pattern: "overlay_ids"
    - from: "preflight 失败"
      to: "permission.rs request_access"
      via: "CGRequestScreenCaptureAccess + open_system_settings 深链"
      pattern: "CGRequestScreenCaptureAccess"
---

<objective>
交付截图流程的收尾能力：**Enter/工具栏确认 → 当前选区（含标注）复制到剪贴板 → 覆盖窗口关闭**，以及 **macOS 屏幕录制权限检测/请求/引导**。这是 Phase 2 最后一条垂直切片，打通 Select → Annotate → Confirm 全流程（D-01）。

Purpose: 落实 CAP-04（选区复制到剪贴板）、CAP-08（权限检测 + 引导授权）、D-01（确认复制含标注图像，不标注复制原图）与 D-04（Enter 确认 / ESC 取消的收口）。同时交付端到端 `#[ignore]` 集成测试与人工清单，完成 Phase 成功标准 3/6。
Output: clipboard.rs（裁剪 + 烘焙 + arboard）、session.rs confirm + 清空、overlay.rs Enter 接线、permission.rs 请求 + 引导、lib.rs 流程、capture_checks bin + 集成测试 + 手动清单。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-screenshot/02-03-SUMMARY.md（前置交付的实际 API 形状）
@.planning/phases/02-screenshot/02-RESEARCH.md（Code Example: arboard 裁剪 + set_image；Pitfall 4/6/7；Security Domain：exclude_from_history、线程亲和性）
@.planning/phases/02-screenshot/02-PATTERNS.md（clipboard.rs / permission.rs 段；integration.rs 子进程模式）
@.planning/phases/02-screenshot/02-CONTEXT.md（D-01 确认复制、D-04 Enter/ESC、CAP-08）
@.planning/phases/01-framework/01-SKELETON.md（透明/权限延续项）
@.planning/phases/01-framework/01-04-SUMMARY.md（integration.rs 子进程 harness 先例 + manual_checklist 位置）

# 关键接口（02-03 已交付，执行者直接使用）
<interfaces>
From crates/modules/capture/src/session.rs：
  pub struct CaptureSession { state(): Arc<parking_lot::Mutex<SessionState>>; overlay_ids 跟踪; pending_overlays; cancel(); undo(); current_tool(); tool_action(); on_annotation_*; ... }
  pub struct SessionState { shots: Vec<(MonitorGeom, RgbaImage)>, selection: Option<(usize, SelectionRect)>, annotations: AnnotationList, overlay_ids: Vec<WindowId>, phase: Phase, ... }
From crates/modules/capture/src/capture.rs：pub struct MonitorGeom { x: i32, y: i32, width: u32, height: u32 }
From crates/modules/capture/src/annotate.rs：AnnotationList { items: Vec<Annotation>, undo(), iter() }
From crates/mybox-core：WindowManagerHandle::{create,destroy,redraw}; mybox_core::tiny_skia
xcap::image::RgbaImage（RGBA8 straight）—— arboard ImageData 直接匹配，无需格式转换（RESEARCH）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 剪贴板复制 + 确认流程（CAP-04, D-01, D-04）</name>
  <files>
    crates/modules/capture/src/clipboard.rs
    crates/modules/capture/src/session.rs
    crates/modules/capture/src/overlay.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/error.rs">错误映射先例（thiserror From 桥）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Code Example: arboard 裁剪 + set_image（行 357-370）、Pitfall 6（剪贴板线程亲和性/占用）、Security Domain exclude_from_history（ConcealedType 可用）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-CONTEXT.md">D-01（含标注复制 / 不标注复制原图）、D-04（Enter 确认关闭）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/session.rs">本 Task 修改的 02-01~02-03 交付：SessionState/CaptureSession 现有形状（confirm/finish 将在此追加）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/overlay.rs">本 Task 修改的 02-02/02-03 交付：on_event 确认输入接线点（Enter/ToolAction::Confirm 将在此追加）</file>
  </read_first>
  <action>
    <step>clipboard.rs 定义纯函数 `pub fn crop_image(img: &xcap::image::RgbaImage, x0: u32, y0: u32, w: u32, h: u32) -> Vec<u8>`：对 `img.as_raw()` 做轴对齐子矩形手动拷贝（RGBA8 straight 逐行复制），返回 `Vec<u8>`（无格式转换，直接匹配 arboard ImageData）。</step>
    <step>clipboard.rs 定义 `pub fn bake_annotations(cropped: &[u8], w: u32, h: u32, annotations: &AnnotationList) -> Vec<u8>`：把裁剪区转 premultiply（复用 overlay::premultiply_rgba8）→ `tiny_skia::Pixmap::from_vec` → 遍历 annotations 调 `Annotation::draw` → 转回 straight RGBA8 输出（反 premultiply：`r = min(255, r*255/a)` 当 a>0，a==0 → 0）。（不标注时 annotations 为空 → 返回原始裁剪字节，D-01。）</step>
    <step>clipboard.rs 定义 `pub fn copy_to_clipboard(bytes: &[u8], w: usize, h: usize) -> anyhow::Result<()>`：在**受限作用域**内 `let mut cb = arboard::Clipboard::new()?;` → `cb.set_image(arboard::ImageData { width: w, height: h, bytes: std::borrow::Cow::Owned(bytes.to_vec()) })?;`（作用域结束即 drop——Pitfall 6：Windows 剪贴板线程亲和性，且必须 drop before loop/exit）；可选 `cb.exclude_from_history()`（macOS ConcealedType）——若 API 可用则开启（Claude 裁定：截图内容不进系统剪贴板历史更安全）。错误经 `anyhow::Context` 标注。</step>
    <step>session.rs 增加 `pub fn confirm(&self) -> Option<(usize, SelectionRect, Vec<u8>, u32, u32)>`：锁定 selection 与 annotations，返回 (monitor_index, rect, 该屏 shots 克隆, 屏宽, 屏高) 并清空 overlay_ids（幂等）；`pub fn finish(&self)`：清空 shots / selection / annotations / overlay_ids / pending（drop-before-close，T-2-01）。</step>
    <step>overlay.rs on_event 增加 Enter：`KeyboardInput` 且 `logical_key == Key::Enter`（或 `Key::Named(NamedKey::Enter)`）且 Pressed → 调 `session.confirm()` → 若 Some：(1) `clipboard::crop_image(&img, sel.x0, sel.y0, w, h)`（裁剪到选区）；(2) `bake_annotations(...)`；(3) `copy_to_clipboard(...)` 主线程执行（on_event 即主线程，满足线程亲和）；(4) 成功则遍历 session 记录的 overlay_ids `windows.destroy(id)` + `session.finish()` + 发 `capture/screenshot-taken` bus 事件（from "capture", payload Module）；失败 `log::error` 不关窗。工具栏确认按钮（ToolAction::Confirm，02-03 已留）同样走 confirm 流程。</step>
  </action>
  <acceptance_criteria>
    <criterion>clipboard.rs 含 crop_image（单测：2x2 RgbaImage 裁剪 1x1 返回 4 字节精确子矩形，RGBA8 straight）与 copy_to_clipboard（受限作用域内 arboard set_image，drop 后返回）。</criterion>
    <criterion>clipboard.rs 含 bake_annotations：空列表返回原始裁剪字节（D-01）；非空列表像素经 premultiply→draw→反 premultiply（单测：烘焙 1 条 Rect 后选区中心像素为标注橙）。</criterion>
    <criterion>session.rs 含 confirm()（返回选区+裁剪所需数据并清 overlay_ids）与 finish()（清空 shots/annotations——drop-before-close）。</criterion>
    <criterion>overlay.rs Enter/确认按钮 → confirm → crop → bake → copy_to_clipboard（主线程）→ 成功销毁全部 overlay + finish + emit capture/screenshot-taken；失败仅 log 不关窗。</criterion>
    <criterion>单测 `clipboard::tests` 绿：`cargo nextest run -p mybox-capture clipboard:: session::tests::confirm`；`cargo check --workspace` 退出 0。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-capture clipboard:: session:: && cargo check --workspace</automated>
  </verify>
  <done>Enter/确认后选区图像（含标注）进入系统剪贴板、覆盖窗口关闭；不标注复制原图（CAP-04, D-01, D-04）。</done>
  <commit_message>feat(capture): clipboard copy with annotation bake and confirm flow</commit_message>
</task>

<task type="auto">
  <name>Task 2: macOS 屏幕录制权限请求 + 引导（CAP-08）</name>
  <files>
    crates/modules/capture/src/permission.rs
    crates/modules/capture/src/lib.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Code Example: CGPreflight/CGRequest（行 428-441）、Pitfall 4（权限缺失 → 黑图；CGRequest + 深链 x-apple.systempreferences:...Privacy_ScreenCapture）、Assumption A1/A7</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-PATTERNS.md">permission.rs 段（cfg(macos) 可注入 checker）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-CONTEXT.md">CAP-08 引导授权</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/permission.rs">本 Task 修改的 02-01 交付：check_access/AccessChecker 现有形状（request_access/open_system_settings 将在此追加）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/lib.rs">本 Task 修改的 02-01/02-02 交付：start_capture 现有权限预检路径（request/引导将在此扩展）</file>
  </read_first>
  <action>
    <step>permission.rs 在已有 `check_access(AccessChecker)` / `real_access_checker()`（CGPreflightScreenCaptureAccess）基础上增加：`pub fn request_access() -> bool`——`#[cfg(target_os = "macos")]` 为 `unsafe { objc2_core_graphics::CGRequestScreenCaptureAccess() }`，非 macOS stub 返回 true；`pub fn open_system_settings()`——`#[cfg(target_os = "macos")]` 用 `std::process::Command::new("open").arg("x-apple.systempreferences:com.apple.preference.security?Privacy_ScreenCapture").spawn()` 打开系统设置屏幕录制面板（A7），非 macOS no-op。</step>
    <step>lib.rs 的 `start_capture` 扩展权限流程（macOS）：`if !permission::check_access(real) { log::warn!("Screen Recording permission denied"); permission::request_access(); /* 首次触发系统授权弹窗，A1：部分系统仅打开设置 */ if !permission::check_access(real) { permission::open_system_settings(); log::info!("引导用户到 系统设置→隐私与安全性→屏幕录制 授权 mybox；授权后可能需要重启 mybox（A1）"); return; } }` 再继续捕获。非 macOS 直接捕获（checker stub true）。</step>
    <step>单测（注入 AccessChecker 模拟 denied→granted 序列，open_system_settings/request_access 以可注入闭包替换避免真弹窗）：(1) 初始 denied → start_capture 调用 request 并 abort，不调用 CaptureFn；(2) granted → 正常捕获。测试内不触发真实 CGRequest/open 命令（用 `#[cfg(test)]` 注入开关把 request/open 换成记录计数闭包）。</step>
  </action>
  <acceptance_criteria>
    <criterion>permission.rs 含 `request_access()`（CGRequestScreenCaptureAccess，macOS）与 `open_system_settings()`（`x-apple.systempreferences:com.apple.preference.security?Privacy_ScreenCapture` 深链，macOS）。</criterion>
    <criterion>lib.rs start_capture 在 denied 时：request_access → 复检仍 denied → open_system_settings + 引导日志 + abort（CAP-08 完整链路）。</criterion>
    <criterion>单测 `permission::tests`：denied 路径触发 request 与 settings、不启动捕获（fake CaptureFn 计数 0）；granted 路径正常捕获——`cargo nextest run -p mybox-capture permission::tests` 绿。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-capture permission:: && cargo check --workspace</automated>
  </verify>
  <done>macOS 无权限时首次触发系统授权弹窗，被拒后自动打开系统设置屏幕录制面板并提示（CAP-08）。</done>
  <commit_message>feat(capture): request screen recording permission with settings guidance</commit_message>
</task>

<task type="auto">
  <name>Task 3: 端到端集成测试 + 手动清单（Phase 成功标准闭环）</name>
  <files>
    crates/modules/capture/src/bin/capture_checks.rs
    crates/modules/capture/tests/integration.rs
    crates/modules/capture/tests/manual_checklist.md
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/bin/display_checks.rs">子进程-per-check harness（ApplicationHandler + WindowHarness + deadline 看门狗）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/tests/integration.rs">CARGO_BIN_EXE_ + run_check(name) + #[ignore]</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-app/tests/manual_checklist.md">Phase 1 手动清单格式</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-VALIDATION.md">Overlay end-to-end 行、Manual-Only Verifications 表</file>
  </read_first>
  <action>
    <step>新建 `crates/modules/capture/src/bin/capture_checks.rs`（子进程-per-check，镜像 display_checks 的 WindowHarness 模式，winit EventLoop 在主线程、每进程一个）：`check_overlay_capture`（创建 Overlay 窗口，on_draw 内合成一个假 2x2 捕获图 + 遮罩并 present，退出 0）；`check_drag_selection`（合成 CursorMoved/MouseInput 事件驱动 session 状态机，断言 phase 到 Selected、selection 非 None）；`check_enter_clipboard`（真实会话上确认流程：构造含 2x2 图像的 session → confirm → crop → copy_to_clipboard，之后 `arboard::Clipboard::new().get_image()` 读回断言尺寸=选区尺寸——需要真实 Screen Recording/显示会话，#[ignore]）；`check_esc_destroy`（cancel 后 overlay_ids 清空断言）。main 按 argv[1] 分发，退出码 0/1/2。</step>
    <step>新建 `crates/modules/capture/tests/integration.rs`：`const CHECKS_BIN: &str = env!("CARGO_BIN_EXE_capture_checks");` + `fn run_check(name)` 子进程断言（镜像 mybox-core tests/integration.rs 行 22-38）；4 个 `#[ignore]` 测试调用 run_check。</step>
    <step>新建 `crates/modules/capture/tests/manual_checklist.md`（Phase 成功标准 1-6 的人工步骤）：1) `cargo run -p mybox-app` 启动，按热键 `Cmd+Shift+S`（模块启动时自动注册，改键在 config.toml `[capture].hotkey`；无需手改 [hotkeys]）或托盘「开始截图」→ 每屏覆盖窗口显示捕获画面 + 遮罩；2) 拖拽选区 → 边框 + WxH 实时；3) 8 手柄调整；4) 矩形/箭头/画笔/文字标注 + Ctrl+Z 撤销；5) Enter → 在其它应用（如 Preview/TextEdit）粘贴验证图像含标注；6) ESC 取消不复制；7) macOS 首次：系统设置→隐私与安全性→屏幕录制 移除 mybox 后触发截图 → 系统授权弹窗 + 设置深链引导；8) AlwaysOnTop 限制确认（RESEARCH Open Question 1 决议）：在全屏应用上方触发截图观察覆盖窗口是否显示——若不可见记录为已知限制（MVP 接受，Phase 4 待办），不作为本 plan 缺陷。</step>
    <step>运行全量套件并修复：`cargo nextest run && cargo test -- --ignored`（--ignored 需真实 macOS 显示会话与已授权权限）。</step>
  </action>
  <acceptance_criteria>
    <criterion>capture_checks.rs 含 ≥4 个 check_*（overlay_capture / drag_selection / enter_clipboard / esc_destroy），子进程分发正确（错误用法 exit 2）。</criterion>
    <criterion>integration.rs 含 ≥4 个 `#[ignore]` 测试调用 run_check。</criterion>
    <criterion>manual_checklist.md 覆盖 Phase 成功标准 1-6（含权限首次引导步骤）。</criterion>
    <criterion>`cargo nextest run` 退出 0；有显示会话的 macOS 上 `cargo test -- --ignored -p mybox-capture` 退出 0（check_enter_clipboard 读回剪贴板尺寸断言通过）。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run && cargo test -- --ignored -p mybox-capture</automated>
  </verify>
  <done>端到端自动化（#[ignore]）+ 人工清单齐备，Phase 成功标准 1-6 有验证路径（CAP-04/08 闭环）。</done>
  <commit_message>test(capture): end-to-end display integration checks and manual checklist</commit_message>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 确认输入 → 剪贴板 | 选区像素（含标注）从内存经 arboard 写系统剪贴板（主线程受限作用域） |
| macOS 权限 FFI → 流程 | CGRequestScreenCaptureAccess 触发系统 UI；深链 open 命令执行固定 URL（非注入面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-12 | Availability | clipboard.rs copy_to_clipboard | mitigate | arboard Clipboard 在受限作用域内 new→set_image→drop（Pitfall 6）；失败仅 log::error 不关窗，不 panic |
| T-2-13 | Information Disclosure | 截图进入系统剪贴板历史 | mitigate | set_image 后调用 exclude_from_history（macOS ConcealedType，已验证可用）——截图内容不进历史 |
| T-2-01 | Information Disclosure | SessionState.shots 驻留 | mitigate | session.finish() 在确认/取消时清空 shots/annotations/overlay_ids（drop-before-close） |
| T-2-02 | Spoofing / Integrity | 无权限黑图当成功 | mitigate | Task 2 完整 CAP-08 链路：preflight → CGRequest → 深链引导 → 仍无权限则 abort；不复制黑图 |
| T-2-14 | Tampering | open 深链命令 | mitigate | 命令为编译期固定字符串常量（非用户输入）；用 Command::new("open") 不经过 shell（无注入面） |
| T-2-15 | Availability | confirm 空选区/空 shots | mitigate | confirm 前 guard：selection/shots 缺失时返回 None 不进入剪贴板路径；crop 尺寸 clamp 到图像边界 |
</threat_model>

<verification>
- 快速套件：`cargo nextest run -p mybox-core -p mybox-capture` 退出 0
- 全量（真实 macOS 显示会话 + 已授权屏幕录制）：`cargo nextest run && cargo test -- --ignored`
- 人工：`cargo run -p mybox-app` → 完整流程（热键→选区→标注→Enter 粘贴验证→ESC 取消→权限首次引导）
</verification>

<success_criteria>
- [ ] Enter/工具栏确认 → 选区（含标注）复制到剪贴板 → 覆盖窗口关闭（CAP-04, D-01, D-04）
- [ ] 不标注直接确认 → 复制原始选区图像（D-01）
- [ ] macOS 权限：检测 → CGRequest → 深链引导 → 未授权 abort（CAP-08）
- [ ] 确认/取消后 shots 清空（内存释放）
- [ ] 端到端 #[ignore] 集成测试 + 手动清单齐备，全量套件绿
</success_criteria>

<output>
Create `.planning/phases/02-screenshot/02-04-SUMMARY.md` when done
</output>
