---
phase: 02-screenshot
plan: 03
type: execute
wave: 3
depends_on: [02-02]
autonomous: true
files_modified:
  - crates/modules/capture/src/annotate.rs
  - crates/modules/capture/src/toolbar.rs
  - crates/modules/capture/src/text.rs
  - crates/modules/capture/src/session.rs
  - crates/modules/capture/src/overlay.rs
requirements: [CAP-06, CAP-07]

must_haves:
  truths:
    - "用户可在选区上绘制矩形框、箭头、画笔（自由路径）、文字四种标注（CAP-06）"
    - "拖拽结束后选区旁出现统一工具栏（确认/取消/撤销 + 四种标注工具按钮），点选即切换当前工具，无显式模式切换（D-03）"
    - "按 Ctrl+Z 撤销最后一条标注，可连续撤销到无标注状态（CAP-07）"
    - "标注与选区手柄可同时使用（D-03：选区手柄在标注过程中仍可调整）"
  artifacts:
    - path: "crates/modules/capture/src/annotate.rs"
      provides: "Annotation 枚举绘制（rect/arrow/pen/text）+ 撤销栈"
      contains: "Annotation"
    - path: "crates/modules/capture/src/toolbar.rs"
      provides: "工具栏布局 + 命中测试（tiny-skia 绘制，无 egui）"
      contains: "hit_test"
    - path: "crates/modules/capture/src/session.rs"
      provides: "current_tool 状态 + annotations 列表 + undo()"
      contains: "undo"
    - path: "crates/modules/capture/src/overlay.rs"
      provides: "on_draw 渲染标注+工具栏；on_event 工具输入 + Ctrl+Z"
      contains: "current_tool"
  key_links:
    - from: "toolbar.rs hit_test"
      to: "session.current_tool"
      via: "工具栏按钮点击 → tool_action() 设置当前工具"
      pattern: "tool_action"
    - from: "overlay.rs on_event 标注输入"
      to: "session.annotations"
      via: "当前工具决定输入行为 → Annotation push → Redraw"
      pattern: "annotations"
    - from: "Ctrl+Z 输入"
      to: "session.undo()"
      via: "ModifiersChanged + logical_key 'z' → pop → Redraw"
      pattern: "undo"
---

<objective>
交付标注能力：**用户在选区上绘制矩形/箭头/画笔/文字，用统一工具栏切换工具，按 Ctrl+Z 撤销**。基于 02-02 的覆盖窗口、选区与渲染链路。

Purpose: 落实 CAP-06（四种标注工具）、CAP-07（Ctrl+Z 撤销到原始状态）与 D-03（统一 no-modes 工具栏：选区手柄与标注工具共存）。标注架构采用 retained `Vec<Annotation>` + 撤销 pop + 每帧从列表全量重画（RESEARCH 反模式规避——绝不把标注烘焙进像素）。
Output: annotate.rs（标注模型 + 绘制 + 撤销栈）、toolbar.rs（工具栏布局 + 命中）、text.rs（文字标注复用）、session.rs（工具状态 + undo）、overlay.rs（渲染 + 输入接线）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-screenshot/02-02-SUMMARY.md（前置交付的实际 API 形状）
@.planning/phases/02-screenshot/02-RESEARCH.md（Code Example: tiny-skia 路径绘制、ab_glyph 文字；Anti-Pattern「标注烘焙进像素」；Pitfall 3）
@.planning/phases/02-screenshot/02-PATTERNS.md（annotate.rs / toolbar.rs / text.rs 段；tray.rs generate_icon_rgba tiny-skia 路径先例）
@.planning/phases/02-screenshot/02-CONTEXT.md（D-01 Select→Annotate→Confirm、D-02/D-03 无模式切换）
@.planning/phases/02-screenshot/02-VALIDATION.md（CAP-06/07 测试映射）

# 关键接口（02-02 已交付，执行者直接使用）
<interfaces>
From crates/modules/capture/src/session.rs：
  pub struct SelectionRect { pub x0: f32, pub y0: f32, pub x1: f32, pub y1: f32 }
  pub enum Tool { Select, Rect, Arrow, Pen, Text }
  pub enum Annotation { Rect { a: tiny_skia::Point, b: tiny_skia::Point }, Arrow { a: tiny_skia::Point, b: tiny_skia::Point }, Pen { pts: Vec<tiny_skia::Point> }, Text { at: tiny_skia::Point, s: String, size: f32 } }
  pub struct CaptureSession { pub fn state(&self) -> Arc<parking_lot::Mutex<SessionState>>; ... }
  pub struct SessionState { pub selection: Option<(usize, SelectionRect)>, pub phase: Phase, pub shots: Vec<(MonitorGeom, RgbaImage)>, pub overlay_ids: Vec<WindowId>, ... }
From crates/modules/capture/src/selection.rs：Handle / hit_test_handle / apply_handle_drag / normalize
From crates/modules/capture/src/text.rs：load_font() -> FontArc、draw_text(pm, font, text, at, size)
From crates/mybox-core：WindowKind::Overlay、WindowManagerHandle::redraw(id)、mybox_core::tiny_skia（重导出）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotation 模型 + tiny-skia 绘制 + 撤销栈（CAP-06 绘制, CAP-07 模型）</name>
  <files>
    crates/modules/capture/src/annotate.rs
    crates/modules/capture/src/text.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/tray.rs">tiny-skia 路径绘制先例（generate_icon_rgba，行 55-77：PathBuilder/fill_path/Paint）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Code Example: 保留标注列表 tiny-skia 绘制（行 372-405：Annotation 枚举 + stroke_rect/stroke_path/箭头三角）、ab_glyph 文字（行 408-426）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Anti-Pattern「把标注烘焙进像素」→ retained 列表 + 每帧重画</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/capture/src/session.rs">Annotation 枚举现有声明（02-01 骨架）</file>
  </read_first>
  <action>
    <step>annotate.rs 使用 session.rs 已有的 `Annotation` 枚举（若 02-01 仅声明未实现，本 Task 实现绘制）。实现 `impl Annotation { pub fn draw(&self, pm: &mut tiny_skia::PixmapMut) }`：(1) `Rect { a, b }` → `tiny_skia::Rect::from_ltrb(a.x.min(b.x), a.y.min(b.y), a.x.max(b.x), a.y.max(b.y))` + `stroke_rect`；(2) `Arrow { a, b }` → `PathBuilder` move_to(a)/line_to(b) + `stroke_path`，再画实心箭头三角（move_to 尖端 b / line_to 两个尾点 / close / fill_path，三角顶点在 b、底边垂直 b-a 方向、长度约 10px）；(3) `Pen { pts }` → PathBuilder move_to(pts[0]) + line_to 遍历 + `stroke_path`；(4) `Text { at, s, size }` → `text::draw_text(pm, &load_font(), &s, (at.x, at.y), size)`。全部用标注橙 `Paint::set_color_rgba8(0xFF, 0x60, 0x00, 0xFF)` + `Stroke { width: 3.0, line_cap: LineCap::Round, ..Default::default() }`。空 pts 时（Pen 无点）直接返回不 panic（T-2-08 guard）。</step>
    <step>annotate.rs 定义 `#[derive(Default)] pub struct AnnotationList { pub items: Vec<Annotation> }`：`push(ann)`、`undo(&mut self) -> Option<Annotation>`（pop）、`is_empty() -> bool`、`iter()`。</step>
    <step>annotate.rs 纯逻辑测试（headless tiny-skia Pixmap 像素断言，复用 renderer/mod.rs 测试风格）：(1) Rect draw 后选区内部中心像素为标注橙 premultiplied 值；(2) Arrow draw 后起点-终点连线上存在非透明像素；(3) Pen 多段点 draw 后中间点非透明；(4) Text draw 后文本区域存在 alpha>0 像素；(5) undo 弹出最后一项，undo 到空后 is_empty 为 true。</step>
    <step>text.rs 扩展 `draw_text` 支持可选中文字色（若需）：保持签名兼容，加 `color: tiny_skia::Color` 参数（默认橙色），供 Annotation::Text 使用；02-02 的 WxH 标签调用点同步更新（白色）。</step>
  </action>
  <acceptance_criteria>
    <criterion>annotate.rs 实现 `Annotation::draw` 覆盖 Rect/Arrow/Pen/Text 四变体，使用标注橙 0xFF6000 + round 线帽；Pen 空列表安全返回不 panic。</criterion>
    <criterion>annotate.rs 含 `AnnotationList`（push/undo/is_empty/iter），undo 为 pop 语义。</criterion>
    <criterion>单测覆盖四工具像素输出 + undo 语义：`cargo nextest run -p mybox-capture annotate::tests` 全绿。</criterion>
    <criterion>text.rs draw_text 支持颜色参数且 02-02 调用点（WxH 标签）同步更新编译通过。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-capture annotate:: text:: && cargo check --workspace</automated>
  </verify>
  <done>四类标注可绘制到 Pixmap，撤销栈 pop 语义可用（CAP-06 绘制 + CAP-07 模型）。</done>
  <commit_message>feat(capture): annotation drawing (rect/arrow/pen/text) with undo stack</commit_message>
</task>

<task type="auto">
  <name>Task 2: 工具栏 + 工具输入 + Ctrl+Z 撤销接线（CAP-06, CAP-07, D-03）</name>
  <files>
    crates/modules/capture/src/toolbar.rs
    crates/modules/capture/src/session.rs
    crates/modules/capture/src/overlay.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/tray.rs">tiny-skia fill_rect/stroke_rect 绘制先例</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-PATTERNS.md">toolbar.rs 段（layout_buttons/hit_test 纯函数）、overlay.rs on_event 段</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-CONTEXT.md">D-03 统一 no-modes 工具栏（手柄与工具共存）、D-01 标注可选</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Overlay rendering（纯 tiny-skia 工具栏，NO egui）；ModifiersChanged + logical key 'z'（CAP-07 行）</file>
  </read_first>
  <action>
    <step>toolbar.rs 定义 `pub enum ToolAction { Confirm, Cancel, Undo, Tool(Tool) }` 与 `pub struct ToolbarButton { pub action: ToolAction, pub rect: tiny_skia::Rect }`。纯函数 `pub fn layout_buttons(selection_bottom_left: (f32, f32), screen_w: f32) -> Vec<ToolbarButton>`：工具栏横向排列（按钮 32x32、间距 4px），包含 确认✓ / 取消✗ / 撤销↶ / 矩形 / 箭头 / 画笔 / 文字 七个按钮，锚定在选区左下外侧（越界时向内偏移）。`pub fn hit_test(buttons: &[ToolbarButton], pos: tiny_skia::Point) -> Option<ToolAction>`（点落在按钮矩形内返回对应 action）。`pub fn draw_toolbar(pm: &mut tiny_skia::PixmapMut, buttons: &[ToolbarButton], current: Tool)`：按钮底为深灰 fill_rect + 白 stroke_rect，当前工具按钮高亮（橙色底）；按钮内符号用 tiny-skia 基本图形 + text::draw_text 画简笔图标（矩形=空心方框、箭头=折线、画笔=曲线、文字="A"、确认="✓"、取消="✗"、撤销="↶"，均可用 draw_text 单字符或简单路径）。</step>
    <step>session.rs：SessionState 增 `current_tool: Tool`（默认 Select）与 `annotations: AnnotationList`；CaptureSession 方法 `pub fn tool_action(&self, action: ToolAction)`（Confirm/Cancel 由 02-04 接线，本 Task 对 Confirm/Cancel 记录日志待 02-04；Tool(t) 设置 current_tool）、`pub fn undo(&self)`（`annotations.undo()` + 返回是否发生撤销）、`pub fn push_annotation(&self, ann: Annotation)`、`pub fn current_tool(&self) -> Tool`。</step>
    <step>overlay.rs on_event 扩展（在主线程）：(1) `MouseInput Pressed` → 先 `toolbar::hit_test(layout_buttons(..., w), pos)`：命中 Confirm/Cancel 时记录动作并触发重绘（Confirm 完整逻辑 02-04 接；本 Task 可先调用 session.tool_action 记录）；命中 Tool(t) → `session.tool_action(ToolAction::Tool(t))` + 重绘；命中 Undo → `session.undo()` + 重绘；未命中工具栏 → 若光标命中选区 8 手柄 → set_active_handle（02-02 行为保留）；否则按 `current_tool` 开始标注：Rect/Arrow/Pen → `on_annotation_start(pos)`，Text → `on_text_click(pos)`（放置一次，A6 不做文本编辑）。(2) `MouseInput Released` → Rect/Arrow 固定终点 → `session.push_annotation`；Pen 固定路径。(3) `CursorMoved` → Rect/Arrow 拖动更新临时点；Pen 累积点；同时保留 02-02 的选区手柄拖动与 WxH 更新（D-03 手柄与工具共存）。(4) 每状态变更后对 overlay_ids 批量 `windows.redraw(id)` 请求重绘。</step>
    <step>overlay.rs on_event 增加 Ctrl+Z：跟踪 `WindowEvent::ModifiersChanged(mods)` 存 `ctrl_down = mods.state().contains(ModifiersState::CONTROL)`（macOS 用 SUPER 也可——用 `CONTROL` 满足 CAP-07 文本 "Ctrl+Z"，macOS 建议同时接受 Command）；`KeyboardInput` 且 `logical_key == Key::Character("z")` 且 ctrl_down 且 Pressed → `session.undo()` + 重绘。</step>
    <step>session.rs 增临时标注字段 `pending_annotation: Option<Annotation>`（绘制中的实时状态，供 on_draw 显示未完成标注）与 `on_annotation_start/on_annotation_update/on_annotation_finish` 方法族（按 current_tool 组装 Annotation 到 pending 或 push）。</step>
    <step>overlay.rs on_draw 合成闭包追加：图像+遮罩+选区之后，渲染 `state.pending_annotation`（若有）与 `state.annotations.iter()` 全部标注（annotate::draw 全量重画，immediate-mode），再渲染工具栏（`toolbar::draw_toolbar(pm, &layout_buttons(...), state.current_tool)`）。</step>
    <step>单测：(1) toolbar::hit_test 命中/未命中；(2) session 工具切换 tool_action 生效；(3) undo 撤销到空后 annotations 为空（CAP-07：等价原始图像）；(4) 标注输入流程：Rect 拖拽 start→update→finish 后 annotations 含 1 条 Rect。</step>
  </action>
  <acceptance_criteria>
    <criterion>toolbar.rs 含 ToolAction/ToolbarButton/layout_buttons（7 按钮）/hit_test/draw_toolbar，纯函数有单测。</criterion>
    <criterion>session.rs 含 current_tool、annotations（AnnotationList）、undo()、push_annotation、pending_annotation 与 on_annotation_* 方法族。</criterion>
    <criterion>overlay.rs on_event 按 current_tool 分流输入（Rect/Arrow/Pen 拖拽、Text 点击一次放置）；选区手柄拖动在标注过程中仍可用（D-03）。</criterion>
    <criterion>overlay.rs 处理 Ctrl+Z（ModifiersChanged CONTROL + 'z'）→ undo；on_draw 渲染 pending + 全部 annotations + 工具栏。</criterion>
    <criterion>单测 `annotate::tests::undo`：连续绘制 3 条 → undo 3 次 → annotations 空（等价原始图像，CAP-07）；`cargo nextest run -p mybox-capture` 全绿；`cargo check --workspace` 退出 0。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-capture && cargo check --workspace</automated>
  </verify>
  <done>用户可在选区上绘制四类标注，工具栏切换工具（手柄并存），Ctrl+Z 撤销到原始状态（CAP-06/07, D-03）。</done>
  <commit_message>feat(capture): unified toolbar, tool-driven annotation input, Ctrl+Z undo</commit_message>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 → 标注数据 | 鼠标轨迹点/文字字符串进入 retained 列表，最终仅内部渲染（无注入面） |
| 工具栏命中 → 动作 | 命中测试把屏幕坐标映射为动作，需 clamp 到按钮矩形避免误触 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-08 | Availability | annotate.rs 绘制 | mitigate | 空 Pen 点列表、零尺寸 Rect 直接安全返回不 panic（guard）；draw 闭包已有 catch_unwind 兜底（02-01） |
| T-2-09 | Tampering / Injection | Annotation::Text 内容 | mitigate | 文字仅经 ab_glyph 光栅化进本地 Pixmap（无脚本/HTML/URL 执行面）；对字符串长度做上限（如 64 字符）防畸形排版 |
| T-2-10 | Integrity | 撤销与烘焙冲突 | mitigate | retained 列表 + undo-pop + 全量重画；绝不把标注写入像素（RESEARCH Anti-Pattern），保证撤销到原始图像 |
| T-2-11 | Availability | 标注点风暴（Pen 拖拽海量点） | mitigate | Pen 按移动事件累积但节流（每事件最多 1 点，事件本身即受 winit 采样率限制）；重绘走批量 Redraw 无轮询 |
</threat_model>

<verification>
- 快速套件：`cargo nextest run -p mybox-core -p mybox-capture` 退出 0
- 显示会话（手动）：拖拽选区后工具栏出现 → 依次用矩形/箭头/画笔/文字 → Ctrl+Z 逐步撤销 → 观察标注随选区重绘
</verification>

<success_criteria>
- [ ] 四类标注工具（矩形/箭头/画笔/文字）可绘制（CAP-06）
- [ ] 统一工具栏：确认/取消/撤销 + 四工具，点选切换，与选区手柄并存（D-03）
- [ ] Ctrl+Z 逐步撤销到最后一条标注（CAP-07）
- [ ] 撤销到空 == 原始截图（retained 列表 + 全量重画，无像素烘焙）
</success_criteria>

<output>
Create `.planning/phases/02-screenshot/02-03-SUMMARY.md` when done
</output>
