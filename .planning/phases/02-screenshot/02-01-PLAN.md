---
phase: 02-screenshot
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
files_modified:
  - Cargo.toml
  - crates/mybox-core/src/window.rs
  - crates/mybox-core/src/app.rs
  - crates/mybox-core/src/bin/display_checks.rs
  - crates/mybox-core/src/lib.rs
  - crates/modules/capture/Cargo.toml
  - crates/modules/capture/src/lib.rs
  - crates/modules/capture/src/capture.rs
  - crates/modules/capture/src/permission.rs
  - crates/modules/capture/src/session.rs
  - crates/mybox-app/src/main.rs
requirements: [CAP-01, CAP-08]

must_haves:
  truths:
    - "RedrawRequested 处理器先调用 renderer.draw(spec.on_draw) 再调用 renderer.present()"
    - "模块可通过 WindowManagerHandle::redraw(id) 入队 WindowRequest::Redraw，about_to_wait 内触发 winit request_redraw()"
    - "截图热键/托盘菜单触发后，所有显示器画面在独立 worker 线程经 xcap 捕获，结果经 UiThreadProxy 存入共享 SessionState"
    - "启动时模块自动注册 start_screenshot 全局热键（读 [capture].hotkey，默认 Cmd+Shift+S），无需用户手改 config.toml [hotkeys]"
    - "macOS 上捕获前执行 Screen Recording 权限预检，未授权则中止流程并明确日志提示（CAP-08 检测半）"
  artifacts:
    - path: "crates/mybox-core/src/window.rs"
      provides: "WindowSpec.on_draw 字段 + WindowRequest::Redraw 变体 + WindowManagerHandle::redraw 方法"
      contains: "on_draw"
    - path: "crates/mybox-core/src/app.rs"
      provides: "RedrawRequested draw-then-present + about_to_wait Redraw 分支 + redraw_draws_then_presents 测试"
      contains: "handle_redraw"
    - path: "crates/mybox-core/src/lib.rs"
      provides: "pub use tiny_skia 重导出（模块边界，FRMW-02）"
      contains: "pub use tiny_skia"
    - path: "crates/modules/capture/src/capture.rs"
      provides: "capture_all_monitors -> Vec<(MonitorGeom, RgbaImage)> + CaptureFn（Arc<dyn Fn> 可注入）"
      exports: ["capture_all_monitors"]
    - path: "crates/modules/capture/src/session.rs"
      provides: "CaptureSession + SessionState（shots，Arc<parking_lot::Mutex>）+ store_shots"
      contains: "SessionState"
    - path: "crates/modules/capture/src/permission.rs"
      provides: "macOS Screen Recording 权限预检（可注入 checker）"
      contains: "CGPreflightScreenCaptureAccess"
    - path: "crates/modules/capture/src/lib.rs"
      provides: "CaptureModule（hotkey/menu -> preflight -> capture -> session）"
      contains: "CaptureModule"
  key_links:
    - from: "crates/modules/capture/src/lib.rs"
      to: "crates/modules/capture/src/capture.rs"
      via: "spawn named capture thread + UiThreadProxy::run"
      pattern: "capture_all_monitors"
    - from: "crates/modules/capture/src/lib.rs"
      to: "crates/modules/capture/src/session.rs"
      via: "ui.run(Box::new(...)) 存入 SessionState.shots"
      pattern: "store_shots"
    - from: "crates/mybox-core/src/app.rs"
      to: "renderer.draw"
      via: "RedrawRequested 分支调用 handle_redraw"
      pattern: "on_draw"
---

<objective>
交付截图模块的两类前置能力：(1) 关闭 Phase 1 遗留的三个框架缺口——`Renderer::draw` 内容调用链、模块侧重绘请求路径、`batch_create` 占位符处置；(2) 新建 `crates/modules/capture` 模块 crate 的捕获后端：热键/托盘菜单 → macOS 权限预检 → xcap 工作线程全屏捕获 → 结果存入共享 SessionState。本 plan 结束时按热键会真实捕获屏幕到内存（无可见窗口——覆盖窗口显示在 02-02 交付）。

Purpose: 这是 Phase 2 的地基。没有 on_draw 调用链，任何覆盖窗口都无法显示内容（01-REVIEW WR-05）；没有 Redraw 请求路径，交互无法驱动重绘（RESEARCH Pattern 2）；不先建捕获后端，覆盖窗口无画面可显示（Pitfall 1：必须先捕获后建窗）。
Output: 修改 mybox-core（window.rs/app.rs/lib.rs）+ 新建 mybox-capture crate（lib/capture/permission/session.rs）+ workspace 与 mybox-app 注册。

⚠ 本 plan 含 `checkpoint:human-verify`（Task 1 包合法性门禁），`autonomous: false`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-screenshot/02-CONTEXT.md
@.planning/phases/02-screenshot/02-RESEARCH.md
@.planning/phases/02-screenshot/02-PATTERNS.md
@.planning/phases/02-screenshot/02-VALIDATION.md
@.planning/phases/01-framework/01-REVIEW.md (WR-05/WR-06/WR-09)

# 关键接口（执行者直接使用，无需探索）
<interfaces>
From crates/mybox-core/src/window.rs (现有)：
  pub struct WindowSpec { pub kind: WindowKind, pub title: String, pub transparent: bool, pub always_on_top: bool, pub decorations: bool, pub visible: bool, pub inner_size: Option<(u32,u32)>, pub position: Option<(i32,i32)>, pub on_event: Option<Box<dyn Fn(&winit::event::WindowEvent) + Send + Sync>> }
  pub enum WindowRequest { Create(WindowSpec), Destroy(WindowId) }
  pub struct WindowManagerHandle { create(spec), destroy(id), set_wakeup(...), try_recv() }
  pub type WindowId = u64
From crates/mybox-core/src/context.rs：
  pub struct UiThreadProxy { pub fn run(&self, f: Box<dyn FnOnce() + Send>) }   // proxy 未设置时 stash，set_proxy() 统一 flush
  pub fn hotkeys(&self) -> &Arc<HotkeyManager>    // ctx.hotkeys().register_str(action, hotkey_str) -> Result<u32>
  pub fn config(&self) -> &Arc<ConfigCenter>      // ctx.config().get("capture", "hotkey") -> Option<toml::Value>
From crates/mybox-core/src/hotkey.rs：
  pub fn register_str(&self, action: &str, hotkey_str: &str) -> Result<u32>    // 需 hotkeys.init() 之后调用（App::run 内）
From crates/mybox-core/src/renderer/mod.rs：
  pub trait Renderer { fn resize(&mut self, w, h); fn draw(&mut self, f: &mut dyn FnMut(&mut tiny_skia::PixmapMut, u32, u32)); fn present(&mut self) -> Result<()>; }
From crates/modules/test/src/lib.rs（EXACT 模板）：Module impl + sample_context() + wait_until 测试辅助
From crates/mybox-app/src/main.rs：App::builder().module(...)?.build()?.run()
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 验证 xcap/arboard/ab_glyph 包合法性（Package Legitimacy Gate, T-2-SC）</name>
  <files>（无文件修改）</files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">## Package Legitimacy Audit 表（四包均 [ASSUMED]，无 [SLOP]/[SUS]；slopcheck 不可用按协议降级）</file>
  </read_first>
  <action>
    在 Task 2 执行 `cargo add`/编辑 Cargo.toml 之前，人工确认三个新依赖在 crates.io 合法且版本正确。这是 blocking-human 门禁，auto_advance 不生效——必须等待人类回复。
  </action>
  <acceptance_criteria>
    <criterion>人类回复 "approved"（或指出有问题的包）。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>n/a — 人工验证门禁</automated>
  </verify>
  <done>三个 [ASSUMED] 包经 crates.io 人工核实后放行，方可执行 Task 2。</done>
  <what-built>无自动化产物——纯人工核验三个 Rust 依赖的 crates.io 存在性与来源仓库。</what-built>
  <how-to-verify>
    1. 打开 https://crates.io/crates/xcap —— 确认存在 0.9.8，仓库指向 github.com/nashaofu/xcap
    2. 打开 https://crates.io/crates/arboard —— 确认存在 3.6.1，仓库指向 github.com/1Password/arboard
    3. 打开 https://crates.io/crates/ab_glyph —— 确认存在 0.2.32，仓库指向 github.com/alexheretic/ab-glyph
    4. 核对 RESEARCH.md Package Legitimacy Audit 表：三包均标记 [ASSUMED]（非 [SLOP]/[SUS]），理由充分
    5. 确认 objc2-core-graphics 0.3.2 已在 Cargo.lock（xcap/arboard/tray-icon 传递依赖），无需新门禁
  </how-to-verify>
  <resume-signal>回复 "approved" 或指出有问题的包</resume-signal>
  <commit_message>chore(phase-02): gate new dependencies behind human verification</commit_message>
</task>

<task type="auto">
  <name>Task 2: 核心渲染链路（on_draw + Redraw 请求路径 + batch_create 处置）</name>
  <files>
    crates/mybox-core/src/window.rs
    crates/mybox-core/src/app.rs
    crates/mybox-core/src/lib.rs
    crates/mybox-core/src/bin/display_checks.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/window.rs">WindowSpec（行 29-58）、WindowRequest（行 149-152）、WindowManager::batch_create 占位（行 301-305）、renderer_factory 字段（行 231-237）、window_manager 测试模块（行 359-559）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/app.rs">RedrawRequested 分支（行 371-375）、about_to_wait drain（行 406-420）、App::build 的 WindowManager::new 调用（行 142-147）、sample_app（行 453-455）、MockRenderer（行 431-438）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/bin/display_checks.rs">WindowHarness::new 的 WindowManager::new(Box::new(...)) 调用（行 42）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/lib.rs">重导出清单（行 19-41）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-PATTERNS.md">window.rs / app.rs 修改段（on_draw 闭包字段、WindowRequest::Redraw、RedrawRequested draw-then-present、about_to_wait Redraw 分支、batch_create 处置）</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Pattern 1/2/3、Open Question 3、Pitfall 3</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/01-framework/01-REVIEW.md">WR-05（draw 死链）、WR-06（Ui 闭包 panic）、WR-09（batch_create id 冲突）</file>
  </read_first>
  <action>
    <step>window.rs 的 WindowSpec 增加字段 `pub on_draw: Option<Box<dyn Fn(&mut tiny_skia::PixmapMut, u32, u32) + Send + Sync>>`（镜像现有 on_event 闭包字段，行 40-41 模式）；impl Default 置 `on_draw: None`。</step>
    <step>window.rs 的 WindowRequest 枚举增加变体 `Redraw(WindowId)`（镜像现有 Create/Destroy）。</step>
    <step>window.rs 的 WindowManagerHandle 增加方法 `pub fn redraw(&self, id: WindowId)`：`tx.send(WindowRequest::Redraw(id))` 后触发 `self.trigger_wake()`（与 create/destroy 完全一致——W3 唤醒钩子）。</step>
    <step>window.rs 删除 `batch_create` 占位方法（行 301-305）及其唯一测试 `batch_create_returns_distinct_placeholder_ids`（行 527-532）——真实实现是模块侧每屏一窗循环（RESEARCH Pattern 3），占位符存在 WR-09 id 冲突风险。</step>
    <step>window.rs 删除 `renderer_factory` 字段（行 231-237）及 WindowManager::new 的该参数——它是 batch_create 专用死字段；`pub fn new()` 改为无参。同步更新 window.rs 测试模块内所有 `WindowManager::new(mock_factory())` 调用（行 464/472/482/494/516；行 528 位于 step 4 删除的 batch_create 测试内，随测试一并消失）为 `WindowManager::new()`，删除 mock_factory（行 374-377）。</step>
    <step>app.rs 的 App::build（行 142-147）与 sample_app（行 453-455）把 `WindowManager::new(Box::new(...))` 简化为 `WindowManager::new()`；同时更新 `crates/mybox-core/src/bin/display_checks.rs` 的 `WindowHarness::new`（行 42）——它也调用 `WindowManager::new(Box::new(...))`，但从不使用该 factory（renderer 在 `resumed()` 内直接构造并经 `register()` 传入），简化为 `WindowManager::new()`。display_checks.rs 是 `src/bin` 二进制，`cargo check --workspace` 会编译它——漏改则编译失败。</step>
    <step>app.rs 新增自由函数 `fn handle_redraw(state: &mut crate::window::WindowState)`：先调 `if let Some(draw) = &state.spec.on_draw { state.renderer.draw(&mut |pixmap, w, h| { let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| draw(pixmap, w, h))); }); }`，再 `if let Err(e) = state.renderer.present() { log::warn!("renderer present failed: {e}"); }`——catch_unwind 防模块绘制闭包 panic 杀死主循环（T-2-03；注意与 WR-06 不同：WR-06 专指 AppEvent::Ui 闭包路径，后者在下一步单独修复）。window_event 的 RedrawRequested 分支改为调用 `handle_redraw(state)`（行 371-375 替换）。</step>
    <step>app.rs 的 `user_event` 修复 WR-06（主线程 Ui 闭包 panic 保护）：把 `AppEvent::Ui(f) => f()`（行 396）改为 `AppEvent::Ui(f) => { let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(f)); }`——模块经 `ctx.ui().run(...)` 转发到主线程的闭包（本 plan Task 3 的捕获结果回写、后续 plan 的重绘/确认路径均走此通道）panic 时不再终止整个事件循环，与 bus 处理器的 catch_unwind（T-1-04）一致。这正是 01-REVIEW WR-06 描述的路径（draw 闭包的 catch_unwind 是 T-2-03，属同类但不同路径）。</step>
    <step>app.rs 的 about_to_wait drain 增加分支：`WindowRequest::Redraw(id) => { if let Some(s) = self.windows.get_mut(id) { if let Some(w) = &s.window { w.request_redraw(); } } }`。</step>
    <step>app.rs 测试模块新增 `redraw_draws_then_presents`：用记录型 MockRenderer（把 draw/present 调用记入 `Vec<&'static str>`），构造 WindowState（renderer=记录型、spec.on_draw=Some(闭包设置一个像素)），调用 `handle_redraw(&mut state)`，断言调用序为 ["draw", "present"]。</step>
    <step>lib.rs（mybox-core）增加 `pub use tiny_skia;` 重导出——模块 crate 的 on_draw 闭包需命名 `tiny_skia::PixmapMut`，经框架重导出保持 FRMW-02 模块边界（镜像 anyhow/log/toml/tray_icon 重导出）。</step>
  </action>
  <acceptance_criteria>
    <criterion>window.rs 的 WindowSpec 含 `on_draw` 字段且 Default 为 None；`window_spec_default_is_panel_decorated_visible` 测试新增 `assert!(spec.on_draw.is_none())`。</criterion>
    <criterion>window.rs 的 WindowRequest 含 `Redraw(WindowId)` 变体；WindowManagerHandle 含 `pub fn redraw(&self, id: WindowId)`（内部 send + trigger_wake）。</criterion>
    <criterion>window.rs 不再含 `batch_create` 方法或 `renderer_factory` 字段；`WindowManager::new()` 无参数（grep `batch_create` 在 mybox-core 全 crate 无命中）。</criterion>
    <criterion>app.rs 的 RedrawRequested 分支调用 `handle_redraw(state)`，其内部先 `renderer.draw`（含 catch_unwind 包裹 on_draw 调用，T-2-03）后 `renderer.present()`。</criterion>
    <criterion>app.rs 的 user_event `AppEvent::Ui(f)` 分支以 `std::panic::catch_unwind(std::panic::AssertUnwindSafe(f))` 包裹（WR-06 修复；源码断言 grep "catch_unwind" 在 user_event 分支命中）。</criterion>
    <criterion>app.rs 的 about_to_wait 含 `WindowRequest::Redraw(id)` 分支，调用 `state.window.request_redraw()`。</criterion>
    <criterion>lib.rs 含 `pub use tiny_skia;`。</criterion>
    <criterion>单测 `redraw_draws_then_presents` 绿：`cargo nextest run -p mybox-core app::redraw_draws_then_presents` 退出 0。</criterion>
    <criterion>`cargo nextest run -p mybox-core` 全绿（既有 77+ 测试不回归）；`cargo check --workspace` 退出 0——注意 display_checks.rs（src/bin）被 `cargo check --workspace` 编译，其 `WindowManager::new(Box::new(...))`（行 42）必须已改为 `WindowManager::new()`。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-core app:: && cargo check --workspace</automated>
  </verify>
  <done>三个框架缺口关闭：draw 调用链接通、Redraw 请求路径可用、batch_create 占位移除；核心测试全绿。</done>
  <commit_message>feat(core): wire on_draw render chain, WindowRequest::Redraw, remove batch_create placeholder</commit_message>
</task>

<task type="auto">
  <name>Task 3: mybox-capture 模块 crate + xcap 捕获后端 + 权限预检 + SessionState</name>
  <files>
    Cargo.toml
    crates/modules/capture/Cargo.toml
    crates/modules/capture/src/lib.rs
    crates/modules/capture/src/capture.rs
    crates/modules/capture/src/permission.rs
    crates/modules/capture/src/session.rs
    crates/mybox-app/src/main.rs
  </files>
  <read_first>
    <file path="/Users/fish/code/mypoj/mybox/crates/modules/test/src/lib.rs">模块 crate EXACT 模板（Module impl、init 订阅、sample_context、wait_until、Arc<Mutex> 断言）</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/context.rs">UiThreadProxy::run（行 126-138）；注意 drain_pending（行 140-144）是 `#[cfg(test)] pub(crate)`，仅 mybox-core 自身测试可见——mybox-capture 不可用</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-core/src/event.rs">EventFilter/EventPayload/FrameworkEvent 用法；handler 为 `Box<dyn Fn(&Event) + Send + Sync>`（行 87）——闭包捕获需 Send+Sync</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-PATTERNS.md">Cargo.toml / lib.rs / capture.rs / session.rs / permission.rs 段</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-RESEARCH.md">Pattern 4（捕获线程 + UiThreadProxy）、Code Example: xcap 捕获（Monitor::all/capture_image/scale_factor）、macOS 权限预检（CGPreflightScreenCaptureAccess）、Pitfall 1/2/4</file>
    <file path="/Users/fish/code/mypoj/mybox/.planning/phases/02-screenshot/02-CONTEXT.md">D-08 热键→bus 事件→窗口请求模式、CAP-08 检测</file>
    <file path="/Users/fish/code/mypoj/mybox/crates/mybox-app/src/main.rs">现有模块注册点（builder.module(...) 链，本 Task 追加 CaptureModule 注册）</file>
  </read_first>
  <action>
    <step>workspace Cargo.toml：`[workspace] members` 增加 "crates/modules/capture"；`[workspace.dependencies]` 增加 `xcap = "0.9.8"`、`arboard = "3.6.1"`、`ab_glyph = "0.2.32"`（三包已过 Task 1 门禁）。</step>
    <step>crates/modules/capture/Cargo.toml：`[package] name = "mybox-capture"`、edition 2021；`[dependencies]`：`mybox-core = { path = "../../mybox-core" }`、`xcap.workspace = true`、`arboard.workspace = true`、`ab_glyph.workspace = true`；`[target.'cfg(target_os = "macos")'.dependencies]`：`objc2-core-graphics = "0.3.2"`（已在 Cargo.lock，无新 objc2 major）。</step>
    <step>capture.rs：定义 `pub struct MonitorGeom { pub x: i32, pub y: i32, pub width: u32, pub height: u32 }`（物理像素，全局原点）；`pub type CaptureFn = Arc<dyn Fn() -> anyhow::Result<Vec<(MonitorGeom, xcap::image::RgbaImage)>> + Send + Sync>`（用 Arc 而非 Box：CaptureModule 需把同一 capture 克隆进两个 'static handler 闭包，而 EventBus 处理器要求 `Send + Sync`（event.rs 行 87），Arc 克隆使 handler 闭包满足；生产用 `Arc::new(capture_all_monitors)` 经 unsized coercion 构造，单测传 `Arc::new(move || ...)` fake；可注入，供单测 mock）；`pub fn capture_all_monitors() -> anyhow::Result<Vec<(MonitorGeom, xcap::image::RgbaImage)>>`：遍历 `xcap::Monitor::all()?`，对每屏取 `monitor.x()?`/`y()?`（i32，points）、`scale_factor()?`（f32）、`capture_image()?`（RgbaImage，RGBA8 straight，像素分辨率），物理几何 = `((x * scale).round() as i32, (y * scale).round() as i32, img.width(), img.height())`（RESEARCH Pattern 3 唯一逻辑→物理转换点）。</step>
    <step>permission.rs：`pub type AccessChecker = fn() -> bool;`；`pub fn real_access_checker() -> bool`——`#[cfg(target_os = "macos")]` 为 `unsafe { objc2_core_graphics::CGPreflightScreenCaptureAccess() }`，非 macOS stub 返回 true；`pub fn check_access(access: AccessChecker) -> bool { access() }`——模块流程以 AccessChecker 参数注入，单测可模拟 denied 路径。</step>
    <step>session.rs：`pub struct SessionState { pub shots: Vec<(MonitorGeom, xcap::image::RgbaImage)>, pub selection: Option<(usize, SelectionRect)>, pub current_tool: Tool, pub annotations: Vec<Annotation>, pub overlay_ids: Vec<WindowId>, pub pending_overlays: usize }`（本次 Task 先实现 shots 字段 + 骨架；selection/current_tool/annotations/overlay_ids 字段先声明为占位默认值，02-02/02-03/02-04 填充逻辑）；`pub struct SelectionRect { pub x0: f32, pub y0: f32, pub x1: f32, pub y1: f32 }`（选区所在 monitor 的局部像素坐标）；`pub enum Tool { Select, Rect, Arrow, Pen, Text }`；`pub enum Annotation { Rect { a: tiny_skia::Point, b: tiny_skia::Point }, Arrow { a: tiny_skia::Point, b: tiny_skia::Point }, Pen { pts: Vec<tiny_skia::Point> }, Text { at: tiny_skia::Point, s: String, size: f32 } }`（枚举先定义供后续模块编译，本 Task 不实现绘制）。`pub struct CaptureSession { state: Arc<parking_lot::Mutex<SessionState>> }`：`pub fn new() -> Self`、`pub fn state(&self) -> Arc<parking_lot::Mutex<SessionState>>`、`pub fn store_shots(&self, shots: Vec<(MonitorGeom, xcap::image::RgbaImage)>)`（锁内写入 shots + `log::info!("captured {} monitors", n)`）。</step>
    <step>lib.rs：`pub struct CaptureModule { capture: CaptureFn, access: AccessChecker }`（依赖注入字段，供单测替换；生产用 `pub fn new() -> Self` 填 `capture: Arc::new(capture_all_monitors)`、`access: permission::real_access_checker`）实现 Module——`id() == "capture"`、`name() == "截图"`；`default_config()` 返回 `toml::Table` 含 `hotkey = "Cmd+Shift+S"`；`menu_items()` 返回 `vec![tray_icon::menu::MenuItem::with_id("capture.start", "开始截图", true, None)]`；`init(ctx)`：clone `windows = ctx.windows().clone()`、`ui = ctx.ui().clone()`、`session = Arc::new(CaptureSession::new())`，再 clone `capture = self.capture.clone()`（Arc 克隆）、`access = self.access`（fn 指针 Copy）；订阅 `EventFilter::kind("core", "hotkey.triggered")` 匹配 `FrameworkEvent::HotkeyTriggered { action, .. }` 中 `action == "start_screenshot"` → `start_capture(&session, &ui, capture.clone(), access)`；订阅 `EventFilter::kind("core", "menu.triggered")` 匹配 payload `menu_id == "capture.start"` → 同一 `start_capture`（两个 'static 闭包各自 clone 所需 Arc/session/ui，均满足 EventBus 的 Send+Sync）。</step>
    <step>lib.rs 定义 `fn start_capture(session: &Arc<CaptureSession>, ui: &UiThreadProxy, capture: CaptureFn, access: AccessChecker)`：先 `if !permission::check_access(access) { log::warn!("capture aborted: macOS Screen Recording permission not granted (CAP-08 preflight)"); return; }`；再 spawn 命名线程 `"mybox-capture"`：`let result = capture();` 后 `ui.run(Box::new(move || match result { Ok(shots) => session.store_shots(shots), Err(e) => log::error!("capture failed: {e:#}") }))`（RESEARCH Pattern 4）。capture/access 由调用方注入：生产路径（init 的两个 handler）传 `Arc::new(capture_all_monitors)` 与 `permission::real_access_checker`，单测传 fake 与 `|| true`/`|| false`——这是 headless 单测的唯一注入点。headless 单测**不**依赖 `UiThreadProxy::drain_pending`（context.rs 行 140-144 是 `#[cfg(test)] pub(crate)`，只在 mybox-core 自身测试编译，mybox-capture 不可达），也**不**构造 MainThreadProxy（需主线程 winit EventLoop，headless 不可用），也**不**给 UiThreadProxy 加公开 drain hook（核心保持不动）。捕获先于任何覆盖窗口存在（Pitfall 1）。</step>
    <step>lib.rs `init(ctx)` 注册截图热键（关键接线——修复「default_config 的 hotkey 键是死配置，框架只注册 [hotkeys] 段」）：定义纯辅助 `fn hotkey_from_config(config: &ConfigCenter) -> String`（读 `config.get("capture", "hotkey").and_then(|v| v.as_str())`，无配置/非字符串时默认 "Cmd+Shift+S"）；`init` 内 clone `let hotkeys = ctx.hotkeys().clone();` 与 `let hotkey_str = hotkey_from_config(ctx.config());`，然后 `ctx.ui().run(Box::new(move || { if let Err(e) = hotkeys.register_str("start_screenshot", &hotkey_str) { log::warn!("failed to register start_screenshot hotkey: {e}"); } }))`。必须经 UiThreadProxy 延迟注册：模块 `init()` 在 `AppBuilder::build()` 内执行（app.rs 行 135），先于 `App::run()` 的 `hotkeys.init()`（app.rs 行 210），此时直接调 `register_str` 会返回 "hotkey manager not initialized" 错误；`ctx.ui().run` 在 proxy 未设置时 stash（context.rs 行 135-137），`set_proxy()`（run() 内 `hotkeys.init()` 之后，app.rs 行 231）统一 flush 到主线程——届时 OS 管理器已初始化，注册成功且在主线程。禁止改为 init 内直接调用 register_str（会静默失败）。用户改热键改 `config.toml [capture].hotkey`，无需再手动编辑 [hotkeys] 段（CAP-01 开箱即用）。</step>
    <step>mybox-app/src/main.rs：在 `builder.module(Box::new(mybox_test::TestModule))?;` 后增加 `builder.module(Box::new(mybox_capture::CaptureModule::new()))?;`。</step>
    <step>单测（mybox-capture 测试模块，镜像 TestModule 的 sample_context + wait_until；headless 无 winit 主线程）：test 1——worker 线程捕获路径（只验证 fake 在命名线程被调用，不验证回写）：构造 fake `CaptureFn`——`count` 为 `Arc<AtomicUsize>`，闭包内 `count.fetch_add(1, Ordering::SeqCst)` 后返回 `Ok(vec![(MonitorGeom { x: 0, y: 0, width: 2, height: 2 }, xcap::image::RgbaImage::new(2, 2).unwrap())])`；直接调 `start_capture(&session, &ui, fake, || true)`（access 注入恒真放行权限门；不依赖 UiThreadProxy 任何 drain 机制）；`wait_until(|| count.load(Ordering::SeqCst) > 0)` 断言 fake 在 spawn 的 worker 线程被调用。spawn 线程内 `ui.run(...)` 的闭包在无 proxy 时 stash、本次不执行也不断言（回写路径由 test 5 覆盖）。test 2——permission denied：`start_capture(&session, &ui, fake, || false)` 时 AccessChecker 返回 false，函数在 spawn 前 return，`Arc<AtomicUsize>` 计数保持 0（fake 未被调用）、仅日志。test 3——handler 路由：构造注入 fake 的 `CaptureModule { capture: fake.clone(), access: || true }`（或 `#[cfg(test)]` with_deps 构造器）并 `init(&sample_context)`；分别 `bus.emit` 两个事件——hotkey.triggered（from "core"，payload `EventPayload::Framework(FrameworkEvent::HotkeyTriggered { id, action: "start_screenshot" })`）与 menu.triggered（payload 含 `menu_id == "capture.start"`），两次 `wait_until` 断言 fake 计数递增到 1 再递增到 2——两个入口均经 handler 触发 start_capture。test 4——`hotkey_from_config` 纯函数：空 ConfigCenter 默认返回 "Cmd+Shift+S"；`config.set("capture","hotkey","Ctrl+Alt+S")` 后返回 "Ctrl+Alt+S"；`init` 在 sample_context（HotkeyManager 未 init）下不 panic 且正常返回（注册闭包经 ui proxy stash，OS 级注册由 02-04 manual checklist 验证）。test 5（session.rs 单测）——`store_shots` 独立单测（无需 UiThreadProxy）：`CaptureSession::new()` 后 `store_shots(vec![(MonitorGeom { x: 0, y: 0, width: 2, height: 2 }, xcap::image::RgbaImage::new(2, 2).unwrap())])`，断言 `session.state().lock().shots.len() == 1` 且 `shots[0].0` 几何为 (0,0,2,2)。</step>
  </action>
  <acceptance_criteria>
    <criterion>workspace Cargo.toml members 含 "crates/modules/capture"；workspace.dependencies 含 xcap/arboard/ab_glyph 三个版本锁定。</criterion>
    <criterion>crates/modules/capture/Cargo.toml 的 `[package] name = "mybox-capture"`；dependencies 含 mybox-core + xcap + arboard + ab_glyph（workspace 引用）+ macOS 目标 objc2-core-graphics 0.3.2。</criterion>
    <criterion>capture.rs 含 `capture_all_monitors()` 使用 `xcap::Monitor::all()` + `capture_image()`，返回 `Vec<(MonitorGeom, RgbaImage)>`，几何为物理像素（x*scale_factor 转换）；`CaptureFn` 为 `Arc<dyn Fn(...) + Send + Sync>`。</criterion>
    <criterion>permission.rs 含 `check_access(AccessChecker)`，macOS 实现调用 `CGPreflightScreenCaptureAccess()`；非 macOS 返回 true。</criterion>
    <criterion>session.rs 含 `CaptureSession` + `SessionState`（`Arc<parking_lot::Mutex>`）+ `store_shots()`；SelectionRect/Tool/Annotation 枚举已声明。</criterion>
    <criterion>lib.rs 的 CaptureModule 含注入字段 `capture: CaptureFn` + `access: AccessChecker`（`new()` 填生产默认 `Arc::new(capture_all_monitors)`/`real_access_checker`），`id() == "capture"`、default_config 含 `hotkey = "Cmd+Shift+S"`、menu item id `"capture.start"`；init 订阅 hotkey.triggered("start_screenshot") 与 menu.triggered("capture.start")，均调 `start_capture(..., capture, access)`（依赖自模块字段注入）。</criterion>
    <criterion>lib.rs `init` 读 `[capture].hotkey`（默认 "Cmd+Shift+S"，经 hotkey_from_config 纯辅助）并经 `ctx.ui().run` 入队 `ctx.hotkeys().register_str("start_screenshot", hotkey_str)`（延迟到 `hotkeys.init()` 之后 flush，不在 init 内直接调用）；default_config 的 `hotkey` 键由此成为有效配置而非死配置。</criterion>
    <criterion>start_capture 在 `check_access(access)` 返回 false 时不启动捕获线程（test 2 断言 fake 计数为 0）；返回 true 时经命名线程调用 `capture()`（test 1 断言计数 > 0），其结果经 UiThreadProxy 在真实运行时（`set_proxy` 后）写入 SessionState.shots——该 handoff headless 单测不可达（proxy 未 set 时闭包 stash），由 test 5（store_shots 单测）+ 02-04 manual checklist 覆盖。</criterion>
    <criterion>mybox-app/src/main.rs 注册 `mybox_capture::CaptureModule::new()`。</criterion>
    <criterion>`cargo nextest run -p mybox-capture` 全绿（含 capture::tests / permission denied / store_shots 测试）；`cargo nextest run -p mybox-core -p mybox-capture` 全绿；`cargo check --workspace` 退出 0。</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-capture && cargo nextest run -p mybox-core -p mybox-capture && cargo check --workspace</automated>
  </verify>
  <done>mybox-capture crate 编译通过；热键/托盘触发后真实捕获所有显示器到 SessionState（日志可见）；权限预检拦截未授权捕获（CAP-01 + CAP-08 检测半）。</done>
  <commit_message>feat(capture): capture module crate with xcap worker-thread capture and permission preflight</commit_message>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模块代码 → 主线程渲染 | on_draw 闭包在 RedrawRequested 主线程执行，panic 会杀死事件循环（WR-06 类） |
| 本地事件 payload → 动作 | 热键 action / menu_id 字符串决定模块动作，禁止执行任意代码（T-1-02 延续） |
| 依赖供应链 → 构建 | xcap/arboard/ab_glyph 为 [ASSUMED] 新依赖（slopcheck 不可用） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Information Disclosure | SessionState.shots（捕获像素驻留内存） | mitigate | 按会话持有而非按应用持有；02-04 确认/取消时清空 shots（drop-before-close） |
| T-2-02 | Spoofing / Integrity | capture.rs + permission.rs | mitigate | macOS 捕获前 CGPreflightScreenCaptureAccess 预检，未授权中止并明确日志；绝不把全黑捕获当成功（Pitfall 4） |
| T-2-03 | Availability | app.rs handle_redraw | mitigate | on_draw 调用包裹 std::panic::catch_unwind（AssertUnwindSafe），与 bus catch_unwind 一致 |
| T-2-16 | Availability | app.rs user_event AppEvent::Ui | mitigate | `AppEvent::Ui(f)` 以 catch_unwind(AssertUnwindSafe(f)) 包裹（WR-06 正式修复）；模块经 ctx.ui().run 的闭包（捕获结果回写/重绘/确认）panic 不再终止事件循环 |
| T-2-04 | Tampering | 热键/menu payload → 动作 | mitigate | 仅按 action 白名单字符串（"start_screenshot"/"capture.start"）分支，不执行任意代码（T-1-02 延续） |
| T-2-SC | Tampering | xcap 0.9.8 / arboard 3.6.1 / ab_glyph 0.2.32 | mitigate | Task 1 blocking-human checkpoint 在 cargo add 前经 crates.io 人工核实（[ASSUMED] 协议；永不可 auto-approve） |
</threat_model>

<verification>
- 快速套件：`cargo nextest run -p mybox-core -p mybox-capture` 退出 0
- 全 workspace：`cargo check --workspace` 退出 0
- 捕获后端行为：`cargo run -p mybox-app` 后按热键 `Cmd+Shift+S`（模块启动时自动注册，读 config.toml `[capture].hotkey`；无需手改 [hotkeys]）或托盘"开始截图"，日志出现 `captured N monitors`；无权限时日志出现权限拒绝提示
</verification>

<success_criteria>
- [ ] 框架 draw 调用链接通：RedrawRequested → renderer.draw(spec.on_draw) → present（单测 redraw_draws_then_presents 断言 draw 先于 present）
- [ ] 模块可经 WindowManagerHandle::redraw(id) 请求重绘（about_to_wait 触发 winit request_redraw）
- [ ] batch_create 占位符与 renderer_factory 死字段移除，无残留调用者（含 display_checks.rs:42 与 window.rs/app.rs 全部调用点）
- [ ] mybox-capture crate 注册并编译；热键/托盘触发后经 worker 线程捕获所有显示器画面存入 SessionState
- [ ] 启动时模块自动注册 start_screenshot 热键（[capture].hotkey，默认 Cmd+Shift+S），开箱即用（无需手改 [hotkeys]）
- [ ] macOS 未授权 Screen Recording 时捕获被预检拦截并明确提示（CAP-08 检测）
- [ ] mybox-core + mybox-capture 全量单测绿
</success_criteria>

<output>
Create `.planning/phases/02-screenshot/02-01-SUMMARY.md` when done
</output>
