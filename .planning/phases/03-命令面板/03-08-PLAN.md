---
phase: 03-命令面板
plan: 08
type: execute
wave: 8
depends_on: ["03-07"]
autonomous: true
gap_closure: true
files_modified:
  - crates/mybox-core/src/command.rs
  - crates/modules/palette/src/session.rs
  - crates/modules/palette/src/bin/palette_checks.rs
  - crates/modules/palette/tests/integration.rs
requirements: [PAL-03]

must_haves:
  truths:
    - "面板窗口创建后即显式请求 IME 允许（session 记录 ime_allowed 标志，不再依赖 egui 焦点后帧的时序——GAP-7 输入子问题消除）"
    - "注入的 Ime::Commit 中文文本经完整链路（egui-winit → TextEdit → set_input）进入输入并触发过滤（session.input 含中文、状态转 Filtering）"
    - "四个内置命令可用拼音关键词命中（tuichu→退出应用 / peizhi→打开配置目录 / chongqi→重启应用 / rizhi→打开日志）——无 IME 场景下的前缀发现路径"
  artifacts:
    - path: "crates/mybox-core/src/command.rs"
      provides: "四个内置命令 keywords 增加拼音别名（纯数据，与 capture 既有 'jietu' 同机制）"
      contains: "\"tuichu\""
    - path: "crates/modules/palette/src/session.rs"
      provides: "ensure_winit_state 创建 egui-winit State 后显式 window.set_ime_allowed(true) + ime_allowed 标志"
      contains: "set_ime_allowed"
    - path: "crates/modules/palette/src/bin/palette_checks.rs"
      provides: "E2E 探针 ime_commit_updates_input（Preedit/Commit 注入 → session.input 断言 + 拼音关键词过滤断言 + ime_allowed 标志断言）"
      contains: "ime_commit_updates_input"
  key_links:
    - from: "窗口首事件（ensure_winit_state）"
      to: "OS IME 输入通道"
      via: "window.set_ime_allowed(true)（egui-winit 自身的 set_ime_allowed 依赖 TextEdit 聚焦后帧的 PlatformOutput.ime 时序——真实桌面首帧竞态下候选窗不出现；显式开启消除时序依赖）"
      pattern: "set_ime_allowed"
    - from: "winit Ime::Preedit/Commit 事件"
      to: "session.set_input"
      via: "egui-winit State::on_window_event（Ime 事件 → egui Event::Ime）→ TextEdit 插入文本 → input_resp.changed() → session.set_input（glyph_shape 探针已证明该路径帧间 diff>0，本探针补 session 级断言）"
      pattern: "Ime::Commit"
    - from: "拼音查询（如 tuichu）"
      to: "内置命令"
      via: "fuzzy-matcher 关键词梯队（keywords 纯数据扩展——与 SPEC 验收 'jt'→jietu 同机制，无代码路径变化）"
      pattern: "keywords"
---

<objective>
修复 GAP-7：中文输入不可用 + 中文命令前缀不可发现。**Planner 决策（根因分析与边界选择如下，明示理由）：**

1. **输入子问题（主修复——IME 显式开启，代码层）**：egui-winit 0.30 完整处理 Ime 事件（lib.rs:335-362：`Ime::Preedit/Commit` → `egui::Event::Ime` → TextEdit 插入文本；glyph_shape 探针已证明该链路帧间 diff>0），egui 0.30 TextEdit 聚焦时输出 `IMEOutput`（builder.rs:775）并由 egui-winit 据此调用 `window.set_ime_allowed`/`set_ime_cursor_area`（lib.rs:851-863）。**缺陷**：该 enable 依赖"TextEdit 获得焦点 → 下一帧 egui 输出 ime → handle_platform_output → set_ime_allowed"的多帧时序，真实桌面（Accessory 策略 + 首帧聚焦竞态）下 OS 候选窗不出现。**修复**：面板窗口的唯一用途就是文本输入——在 `ensure_winit_state` 创建 egui-winit State 的同一首次调用中显式 `window.set_ime_allowed(true)`（session 记录 `ime_allowed` 标志，一次生效）。egui-winit 后续按焦点变化调用 `set_ime_allowed(false/true)` 的行为保留（Executing 态输入禁用时关闭 IME 是正确的）。
2. **前缀发现子问题（辅修复——拼音 keywords，纯数据）**：四个内置命令 keywords 增加拼音别名（`tuichu`/`peizhi`/`chongqi`/`rizhi`）。capture 已有 `jietu`（SPEC "jt" 验收），此修复把同一机制覆盖到全部内置命令——无 IME 场景（或 IME 被 OS 禁用）下用户可用拼音命中中文命令。

**SPEC 边界声明**：无需修改 SPEC/CONTEXT 的 out-of-scope 记录——SPEC 排除的是「中文 IME 组合输入**特殊处理**」（自研 IME 状态机/拼音转换引擎），本计划只做两件事：(a) 显式开启系统 IME（egui-winit 标准路径，RESEARCH Anti-Patterns 明示"Chinese IME works through the standard path"）；(b) 使用既有 keywords 数据字段（UI-SPEC 命令清单标注 "Suggested keywords" 非锁定枚举）。均未扩大 SPEC 边界，故不改动 03-SPEC.md/03-CONTEXT.md。

Output: core 内置命令拼音 keywords + palette IME 显式开启 + E2E `ime_commit_updates_input` 探针（Ime 注入 → session.input 中文断言 + 拼音过滤断言 + ime_allowed 标志断言）。REQUIREMENTS.md 不改动（PAL-03 已 `[x]` Complete；本计划强化其输入可达性，无待更新状态）。
</objective>

<execution_context>
@/Users/fish/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fish/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-命令面板/03-HUMAN-UAT.md
@.planning/phases/03-命令面板/03-CONTEXT.md
@.planning/phases/03-命令面板/03-RESEARCH.md

Gap 事实（03-HUMAN-UAT.md 测试 10）："命令项全中文但输入框不能输入中文，用户无从得知命令前缀"。
关键代码位置：crates/mybox-core/src/command.rs:104-187（四个内置命令 keywords）、crates/modules/palette/src/session.rs:358-373（ensure_winit_state——IME 显式开启的落点）。
依赖源码事实（勿再读源码）：winit 0.30.13 `Window::set_ime_allowed(&self, bool)`（window.rs:1283）；egui-winit 0.30 的 Ime 事件处理（lib.rs:335-362）与按 PlatformOutput.ime 的 set_ime_allowed debounce（lib.rs:851-863）；egui 0.30 TextEdit 聚焦时输出 IMEOutput（builder.rs:775）。glyph_shape 探针（03-04）已证明 Ime::Commit 注入经 egui-winit 翻译后产生渲染变化。
注意：03-05/03-06/03-07 已改 session.rs/palette_checks.rs/integration.rs（修订计数、modifiers、多个探针）——本计划在其成果之上叠加，勿回退。
</context>

<tasks>

<task type="auto">
  <name>Task 1: core 内置命令拼音 keywords（纯数据 + 断言测试）</name>
  <read_first>
    - crates/mybox-core/src/command.rs（BuiltinCommands::build_with 103-189 四个命令的 keywords 行、tests 模块 245-468）
    - .planning/phases/03-命令面板/03-UI-SPEC.md（200-208 命令清单——"Suggested keywords"为建议性非锁定）
  </read_first>
  <files>crates/mybox-core/src/command.rs</files>
  <action>
    **拼音 keywords（纯数据扩展，GAP-7 前缀发现路径）**：`BuiltinCommands::build_with` 中四个命令的 keywords vec 各追加一个拼音全拼（与 capture 的 `"jietu"` 同机制——fuzzy-matcher 关键词梯队天然支持，无代码路径变化；注释写明 GAP-7：无 IME 场景下用户可用拼音命中中文命令）：
    - `builtin.quit`（108 行 keywords `["退出", "quit", "exit"]`）→ 追加 `"tuichu"`
    - `builtin.open_config`（131 行 `["配置", "config"]`）→ 追加 `"peizhi"`
    - `builtin.restart`（147 行 `["重启", "restart"]`）→ 追加 `"chongqi"`
    - `builtin.open_log`（176 行 `["日志", "log"]`）→ 追加 `"rizhi"`

    **单测**：tests 模块新增 `builtin_keywords_include_pinyin_aliases`：用既有 `builtins()` helper 取 4 命令，断言 `quit.keywords.contains(&"tuichu")`、`open_config.keywords.contains(&"peizhi")`、`restart.keywords.contains(&"chongqi")`、`open_log.keywords.contains(&"rizhi")`（锁定数据不再丢失）。
  </action>
  <acceptance_criteria>
    - 源码断言：command.rs 含四个字符串字面量 `"tuichu"`、`"peizhi"`、`"chongqi"`、`"rizhi"`（各在对应命令的 keywords vec 内）
    - 源码断言：command.rs tests 含 `fn builtin_keywords_include_pinyin_aliases`
    - 测试命令：`cargo nextest run -p mybox-core command` 退出码 0（含新测试与既有 command 测试全绿）
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-core command</automated>
  </verify>
  <done>四个内置命令携带拼音关键词，拼音查询可命中中文命令。</done>
</task>

<task type="auto">
  <name>Task 2: session 显式开启 IME（ime_allowed 标志 + set_ime_allowed 调用）</name>
  <read_first>
    - crates/modules/palette/src/session.rs（SessionInner 44-66、ensure_winit_state 358-373、构造 79-96）
    - .planning/phases/03-命令面板/03-RESEARCH.md（371 行 Anti-Patterns："Hand-rolling winit→egui input translation"——IME 走标准路径的明示）
  </read_first>
  <files>crates/modules/palette/src/session.rs</files>
  <action>
    **a. 标志字段**：`SessionInner` 新增 `ime_allowed: bool`（构造处初始化 `false`）。新增访问器 `pub fn ime_allowed(&self) -> bool`（state 锁内读——E2E 探针断言用）。

    **b. ensure_winit_state 显式开启**（358-373 重写）：把 `set_ime_allowed` 调用移出 state 锁段（winit 调用无需持锁）：
    - 锁段内：`if inner.winit_state.is_none() { ...创建 State（现有代码）... }` 之后、`if !inner.ime_allowed { inner.ime_allowed = true; true } else { false }`（用局部变量带出）。
    - 锁外：`if enable_ime { window.set_ime_allowed(true); mybox_core::log::debug!("palette: IME allowed on the palette window (GAP-7)"); }`
    - doc 注释写明 GAP-7 根因：egui-winit 的 `set_ime_allowed` 依赖"TextEdit 聚焦 → 下一帧 PlatformOutput.ime → handle_platform_output"多帧时序（egui-winit lib.rs:851 源码），真实桌面首帧竞态下 OS 候选窗不出现；面板窗口唯一用途是文本输入，首次事件即显式开启消除时序依赖。egui-winit 后续按焦点变化调用 `set_ime_allowed(false/true)` 的行为保留（Executing 输入禁用时关闭 IME 正确）。
  </action>
  <acceptance_criteria>
    - 源码断言：session.rs 的 `SessionInner` 含 `ime_allowed: bool`；含 `pub fn ime_allowed(&self) -> bool`
    - 源码断言：`fn ensure_winit_state` 函数体内含 `window.set_ime_allowed(true)` 且其调用位于 state 锁段之外；含 `if !inner.ime_allowed { inner.ime_allowed = true; ... }` 一次生效守卫
    - 测试命令：`cargo nextest run -p mybox-palette session` 退出码 0（既有测试无回归；ime_allowed 标志的行为断言由 Task 3 探针经真实闭包覆盖——headless 无法构造 winit Window）
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-palette session</automated>
    <automated>cargo check --workspace</automated>
  </verify>
  <done>面板窗口首次事件即请求 IME 允许（一次生效、主线程调用、不依赖 egui 焦点时序）。</done>
</task>

<task type="auto">
  <name>Task 3: E2E 探针 ime_commit_updates_input + 集成测试接线</name>
  <read_first>
    - crates/modules/palette/src/bin/palette_checks.rs（check_glyph_shape 935-1075——Ime 注入先例、PaletteHarness 47-168、main 1079-1102——注意 03-05/03-06/03-07 已新增多个探针与 press_key_mods，勿回退）
    - crates/modules/palette/tests/integration.rs（run_check 模板 28-37）
    - crates/modules/palette/src/session.rs（Task 2 产物：ime_allowed；set_input 语义——中文进入过滤）
  </read_first>
  <files>crates/modules/palette/src/bin/palette_checks.rs, crates/modules/palette/tests/integration.rs</files>
  <action>
    **a. 新探针 `check_ime_commit_updates_input`**（真实窗口 + 真实 egui-winit 事件链，仿 check_glyph_shape 的 Ime 注入脚手架）：registry 2 个 fake 命令——`capture.start`（name "开始截图"、keywords ["截图","capture","screen","jietu"]）与 `builtin.quit`（name "退出应用"、keywords ["退出","quit","exit","tuichu"]——镜像 Task 1 的 core 新数据）；summon → expect_create → harness，driver 状态机：
    - stage 0：`h.inject(RedrawRequested)?` 首帧（首个事件触发 ensure_winit_state）→ 断言 `non_background_pixels() > 0` → **断言 `s.ime_allowed() == true`**（GAP-7 显式开启标志经真实闭包设置——本探针核心覆盖点）→ `h.inject(WindowEvent::Ime(winit::event::Ime::Preedit("测".to_string(), None)))?` → stage 1。
    - stage 1：`h.inject(RedrawRequested)?` → `h.inject(WindowEvent::Ime(winit::event::Ime::Commit("截图".to_string())))?` → stage 2。
    - stage 2：`h.inject(RedrawRequested)?`（Commit 帧：egui-winit → egui Event::Ime → TextEdit 插入 → input_resp.changed → session.set_input）→ 断言 `s.input() == "截图"`、`s.state() == PaletteState::Filtering`、`s.filtered() == vec![0]`（截图 name 梯队命中 开始截图）→ `s.set_input("tuichu")` → 断言 `s.filtered() == vec![1]`（拼音关键词命中 退出应用——GAP-7 前缀发现路径的 session 级断言）→ `press_key(&s, ..., Key::Named(NamedKey::Escape))` → stage 3。
    - stage 3：轮询 `handle.try_recv()` 出现 `WindowRequest::Destroy(id)` 且 id == created_id、`s.state()==Hidden` → `h.pass()`。
    - **覆盖声明（保持诚实）**：探针注入合成 Ime 事件，覆盖 winit 事件 → egui-winit → TextEdit → session 全链路 + ime_allowed 标志；OS 输入法组合窗（候选词窗口）的出现与交互只能由 UAT 测试 10 人工复验。main() 分发 match 与 usage 字符串各加 `"ime_commit_updates_input"`。

    **b. integration.rs**：新增 `#[test] #[ignore] fn palette_ime_commit_updates_input() { run_check("ime_commit_updates_input"); }`（文档注释注明 PAL-03/GAP-7 回归）。
  </action>
  <acceptance_criteria>
    - 源码断言：palette_checks.rs 含 `fn check_ime_commit_updates_input`，其中含 `ime_allowed() == true` 断言、`Ime::Preedit`/`Ime::Commit` 注入、`s.input() == "截图"`、`s.filtered() == vec![0]`、`s.set_input("tuichu")` 与 `vec![1]` 断言、ESC-Destroy 收尾；main() match 与 usage 含 `ime_commit_updates_input`
    - 源码断言：integration.rs 含 `fn palette_ime_commit_updates_input`（`#[ignore]`）
    - 测试命令（headless）：`cargo nextest run -p mybox-palette -p mybox-core` 退出码 0
    - 测试命令（桌面会话，verify-phase 重跑）：`cargo test -p mybox-palette --test integration -- --ignored` 10/10 通过（9 既有含 03-05..03-07 探针 + ime_commit_updates_input）
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-palette -p mybox-core</automated>
    <automated>cargo build -p mybox-palette --bin palette_checks && cargo test -p mybox-palette --test integration -- --ignored</automated>
  </verify>
  <done>中文 Ime Commit 经完整链路进入输入并触发过滤；IME 显式开启标志经真实闭包设置；拼音关键词命中内置命令。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS IME 组合事件（Preedit/Commit）→ egui-winit → TextEdit | 外部文本输入进入会话（本地 OS 输入法） |
| TextEdit 回写 → session.set_input | 输入内容进入过滤（V5 输入验证面） |
| set_ime_allowed → winit 窗口 | 面板显式开启系统 IME 通道 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering (输入注入/长度不受控) | Ime::Commit 文本 → set_input | mitigate | `set_input` 既有 `MAX_QUERY_LEN = 64` 字符截断（Security V5）+ fuzzy-matcher element_limit 绑定匹配成本——Ime 提交的长文本走同一既有防御，本计划不削弱也不新增面 |
| T-03-17 | Availability (无 IME 平台行为) | `window.set_ime_allowed(true)` | accept | winit 跨平台 API（Windows 默认支持、调用无害；macOS 生效）；非 macOS 平台该调用为空操作路径——winit 内部处理，无 panic 面 |
| T-03-18 | Information Disclosure (候选窗/输入内容) | IME 候选窗位置 | accept | 候选窗由 OS 在 `set_ime_cursor_area` 位置显示（egui-winit 标准路径），面板本地渲染；日志不输出输入内容（GAP-7 修复的 debug 日志仅记录 IME 开启事件，不含文本） |
| T-03-SC | Tampering | npm/pip/cargo 安装 | accept | 本计划零新增依赖（无 package-manager install 任务） |

</threat_model>

<verification>
- `cargo nextest run -p mybox-palette -p mybox-core`：全部单测通过（含拼音 keywords 断言）
- `cargo check --workspace`：exit 0 无 warning
- 桌面会话（verify-phase）：`cargo test -p mybox-palette --test integration -- --ignored` 10/10 通过，其中 `ime_commit_updates_input` 断言 ime_allowed 标志、中文 Commit 进入 session.input 并过滤、拼音关键词命中
- 真实桌面人工复验（UAT 重跑测试 10）：输入框可输入中文（IME 候选窗出现），或拼音（如 tuichu）可命中中文命令
</verification>

<success_criteria>
- GAP-7 两个子问题均有闭环路径：IME 显式开启（代码层 + 探针标志断言 + UAT 人工复验）；拼音 keywords（数据层 + 单测 + 探针过滤断言）
- SPEC 边界未扩大：无自研 IME 特殊处理、无拼音转换引擎；显式开启系统 IME 与 keywords 数据均为既有机制
- `ime_commit_updates_input` E2E 探针可在桌面会话重复运行
</success_criteria>

<output>
创建 `.planning/phases/03-命令面板/03-08-SUMMARY.md`（含 gap 修复记录、IME 决策理由、SPEC 边界声明、验证证据）
</output>
