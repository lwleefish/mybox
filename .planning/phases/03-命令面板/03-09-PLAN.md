---
phase: 03-命令面板
plan: 09
type: execute
wave: 9
depends_on: ["03-08"]
autonomous: true
gap_closure: true
files_modified:
  - crates/modules/palette/src/session.rs
  - crates/modules/palette/src/lib.rs
  - crates/modules/palette/src/bin/palette_checks.rs
  - crates/modules/palette/tests/integration.rs
requirements: [PAL-01, PAL-03]

must_haves:
  truths:
    - "面板【每一次】窗口创建后即显式请求 IME 允许（GAP-7 输入子问题在重唤出场景同样消除）"
    - "Hidden 态窗口下一步 sync_window_geometry 早退（WR-04 1px resize 排除）"
    - "summon_palette 初始高度与帧循环一致 max(1)（WR-02 零命令潜伏缺陷关闭）"
  artifacts:
    - path: "crates/modules/palette/src/session.rs"
      provides: "summon() 复位 ime_allowed=false 与 winit_state=None——使每次窗口创建重新走 ensure_winit_state 的显式 set_ime_allowed(true) + 重建 egui-winit State 路径"
      contains: "inner.ime_allowed = false"
    - path: "crates/modules/palette/src/lib.rs"
      provides: "sync_window_geometry 对 Hidden 态早退（WR-04 1px resize 排除）+ summon_palette 初始高度 all.len().max(1)（WR-02 与帧循环 max(1) 一致）"
      contains: "all.len().max(1)"
    - path: "crates/modules/palette/src/bin/palette_checks.rs"
      provides: "E2E 探针 ime_commit_updates_input 扩展重唤出阶段（summon → ESC Destroy → 再 summon → 断言第二窗口 ime_allowed 由 false 复位→true 的 re-set 路径被行使）"
      contains: "ime_commit_updates_input"
  key_links:
    - from: "summon()（每次窗口创建前的复位点）"
      to: "ensure_winit_state 的显式 set_ime_allowed(true) 路径"
      via: "summon 复位 ime_allowed=false 与 winit_state=None，使下一次 ensure_winit_state 重新命中 `if !inner.ime_allowed` 守卫并新建 egui-winit State，对新 winit Window 调用 window.set_ime_allowed(true)（egui-winit 0.30 去抖仅在 allow_ime 翻转时调用——复位即翻转）"
      pattern: "inner.ime_allowed = false"
    - from: "ESC/hotkey 关闭路径（close + Destroy）"
      to: "下一次 summon 触发的新窗口 IME 显式开启"
      via: "summon() 在 close 之后被打——复位 ime_allowed=false 让 ensure_winit_state 的 `if !inner.ime_allowed { inner.ime_allowed = true; true }` 重新生效"
      pattern: "winit_state = None"
    - from: "sync_window_geometry 在 Hidden 态被调用（capture.start 点击执行路径）"
      to: "无 1px request_inner_size + 1px framebuffer 重分配"
      via: "函数顶 `if session.state() == PaletteState::Hidden { return; }` 守卫——Hidden 态不参与几何同步"
      pattern: "PaletteState::Hidden"
---

<objective>
修复 Phase 3 留存的唯一 BLOCKER gap（GAP-8 / REVIEW WR-01）+ 同函数两个 warning（WR-04、WR-02），全部并入一个 gap-closure 计划。**Planner 决策（根因与边界选择如下，明示理由）：**

1. **GAP-8（BLOCKER 主修复——IME 重唤出复位，代码层 2 行）**：03-08 的 IME 显式开启是 per-session 一次性——`summon()`（session.rs 130-149）只复位 modifiers/input/selection 等，不复位 `ime_allowed=false` 与 `winit_state=None`；`ensure_winit_state`（session.rs 478-503）的 `if !inner.ime_allowed` 守卫一次置位后永不复位、`winit_state` 只创建一次永不重建。egui-winit 0.30 vendored 源码 lib.rs:848-852 已验证：只在 `allow_ime` 翻转时调用 `window.set_ime_allowed`——复用同一 State（`allow_ime` 保持 `true`）→ 无翻转 → 永不重开；winit macOS 后端每窗口默认禁用 IME。结论：ESC/热键关闭后第二次及以后的唤出窗口不再收到 `set_ime_allowed(true)`，中文输入法在核心循环（UAT 测试 1 的同一流程）死亡。**修复**：`summon()` 复位 `inner.ime_allowed = false` 与 `inner.winit_state = None`，使每次窗口创建都重新执行显式 `window.set_ime_allowed(true)` 并针对新窗口重建 egui-winit State（REVIEW WR-01 的修复建议）。
2. **WR-04（WARNING 顺带修复——同函数 sync_window_geometry）**：lib.rs 486-504 的 `sync_window_geometry` 对 Hidden 态不早退——Hidden 时 `ui::window_height(Hidden, ..)` 返回 0，经 `max(1)` 放大为 1px → `request_inner_size(1px)` + `resize_framebuffer(1px)` 一对 1px 重分配。`capture.start` 点击执行路径在同帧触发 `geometry_revision` 递增 + Destroy（窗口随后销毁，下次 summon 重装帧缓冲）——瞬态、无持久损坏但每张截图都付一次无谓分配。**修复**：`sync_window_geometry` 顶加 `if session.state() == PaletteState::Hidden { return; }` 早退守卫。
3. **WR-02（WARNING 顺带修复——同函数族 summon_palette 初始高度）**：lib.rs 176 行 `summon_palette` 初始高度用 `all.len()`，而 lib.rs 491 行帧循环用 `all.len().max(1)`——零命令 case 两处分别算出 80 vs 128（不一致）。生产恒有 ≥4 内置命令（不可达），但修复 = 两处统一 `max(1)`。**修复**：lib.rs 176 行 `all.len()` 改 `all.len().max(1)`。
4. **探针覆盖洞修复**：03-08 的 `ime_commit_updates_input` 探针只覆盖首次唤出（stage 0 断言 `ime_allowed == true`），未覆盖重唤出路径——这正是缺陷让 10/10 全绿仍漏的关键洞。**修复**：扩展探针增加重唤出阶段（`summon → ESC 配对 Destroy → 再 summon → 断言 ime_allowed 复位 false → 第二窗口首帧断言 ime_allowed 重新置 true`），把 REVIEW WR-01 缺陷路径锁进可重复执行的探针。

**SPEC 边界声明**：本计划只做三件代码级修复 + 一个探针扩展——无新增依赖、无自研 IME 状态机、无拼音转换引擎、无新模块；03-SPEC/CONTEXT/RESEARCH/PATTERNS/UI-SPEC/VALIDATION/VERIFICATION 不改动。（HUMAN-UAT.md 的 UAT 1/3/4/10 复验在 gap-closure 后由人工执行——本计划不替代其物理按键/OS 输入法人工验收。）

Output: session IME 重唤出复位（GAP-8）+ lib.rs Hidden 早退（WR-04）+ lib.rs 初始高度统一 max(1)（WR-02）+ E2E 探针 `ime_commit_updates_input` 重唤出阶段扩展（覆盖洞关闭）+ integration.rs 测试 10 文档注释更新。REQUIREMENTS.md 不改动（PAL-01/PAL-03 已 `[x]` Complete；本计划强化 PAL-01 重唤出循环的 IME 可达性 + PAL-03 中文输入路径在重唤出场景同样可用）。
</objective>

<execution_context>
@/Users/fish/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fish/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-命令面板/03-08-PLAN.md
@.planning/phases/03-命令面板/03-08-SUMMARY.md
@.planning/phases/03-命令面板/03-VERIFICATION.md

Gap 事实（03-VERIFICATION.md frontmatter `gaps[0]`）："面板【每一次】窗口创建后即显式请求 IME 允许（GAP-7 输入子问题在重唤出场景同样消除）"——status: failed，1 个 BLOCKER。
WR-04 事实（03-VERIFICATION.md Anti-Patterns 表）：lib.rs 486-504 Hidden 态 1px resize + 1px framebuffer 重分配，capture.start 点击执行路径可达。
WR-02 事实（03-VERIFICATION.md Anti-Patterns 表）：lib.rs 176 vs 491 初始高度 `all.len()` 与 `len().max(1)` 不一致（零命令 80 vs 128）。
关键代码位置（勿重复读源码）：session.rs 130-149（summon）、478-503（ensure_winit_state）、247-249（ime_allowed 访问器）；lib.rs 168-182（summon_palette）、486-505（sync_window_geometry）；palette_checks.rs 758-868（consecutive_summon_close——re-summon 模式参照）、1753-1877（check_ime_commit_updates_input——本计划扩展目标）；integration.rs 140-153（测试 10 接线）。
依赖源码事实（勿读）：winit 0.30 macOS 后端每窗口默认禁用 IME；egui-winit 0.30 vendored lib.rs:848-852 仅在 `allow_ime` 翻转时调用 `window.set_ime_allowed`——复用 State 时 `allow_ime` 保持 true 无翻转永不重开。consecutive_summon_close 探针已验证 re-summon 模式：`summon_palette(&s, &h, &registry, &ui_lock)` → `expect_create(&h)` → `harness.pending_spec = Some(spec)` → `harness.realize_window(el)?`。
注意：03-05..03-08 已改 session.rs/palette_checks.rs/integration.rs（修订计数、modifiers、IME 显式开启、多个探针、`ime_allowed` 字段与访问器已存在）——本计划在其成果之上叠加复位逻辑，勿回退既有字段/访问器/守卫。
</context>

<tasks>

<task type="auto">
  <name>Task 1: session.summon 复位 ime_allowed=false 与 winit_state=None（GAP-8 主修复）</name>
  <read_first>
    - crates/modules/palette/src/session.rs（summon 130-149、ensure_winit_state 458-503、ime_allowed 访问器 247-249、SessionInner 44-66 字段表、构造 79-96）
    - crates/modules/palette/src/bin/palette_checks.rs（check_ime_commit_updates_input 1753-1877——Task 3 探针的当前结构参照）
    - .planning/phases/03-命令面板/03-08-PLAN.md（Task 2 的 IME 显式开启决策与锁外调用纪律——本任务遵循同一锁序）
    - .planning/phases/03-命令面板/03-VERIFICATION.md（frontmatter gaps[0].missing[0]、Anti-Patterns GAP-8 行——修复规格来源）
  </read_first>
  <files>crates/modules/palette/src/session.rs</files>
  <action>
    **a. summon() 复位 IME 状态（GAP-8 主修复，2 行）**：在 `summon()`（session.rs 130-149）现有复位序列内（`inner.modifiers = ...` 之后、`inner.state = PaletteState::Idle` 之前或之后均可——保持代码可读）追加两条复位：
    - `inner.ime_allowed = false;`
    - `inner.winit_state = None;`

    **b. doc 注释**：在 `summon()` 既有 doc（126-129 行）下方追加一行说明——明示 GAP-8 修复理由：每次窗口创建都重新走 `ensure_winit_state` 的 `if !inner.ime_allowed` 守卫并对新 winit Window 调用 `window.set_ime_allowed(true)`，复位 `winit_state = None` 让 egui-winit State 针对新窗口重建（egui-winit 0.30 vendored lib.rs:848-852 仅在 `allow_ime` 翻转时调用 `set_ime_allowed`——不复位即无翻转即不重开，winit macOS 后端每窗口默认禁用 IME）。

    **c. close() 路径不动**：03-VERIFICATION.md frontmatter `gaps[0].missing[0]` 明示"summon()（或 close() 等价路径）"——选择 summon（每次唤出都复位，无论前次是 ESC 还是 hotkey close），覆盖更窄但更确定；close 路径覆盖 hotkey-toggle 关闭场景但漏 ESC 关闭后 re-summon。**选择 summon**：所有 re-summon 都经过 summon，single source of truth。

    **不动**：`ensure_winit_state` 478-503、`ime_allowed()` 访问器 247-249、构造处初始化 `ime_allowed: false`（120 行）——03-08 已正确建立，本任务只在 summon 加复位，不重写 ensure_winit_state。锁序遵守 03-08 纪律：所有 winit 调用都在 state 锁段之外（本任务不涉及 winit 调用，仅做锁内状态复位）。
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-palette session</automated>
    <automated>cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：session.rs 的 `pub fn summon` 函数体内含 `inner.ime_allowed = false;` 字面量
    - 源码断言：session.rs 的 `pub fn summon` 函数体内含 `inner.winit_state = None;` 字面量
    - 源码断言：session.rs 的 `fn ensure_winit_state` 函数体未被 Task 1 改动（仍含 03-08 写入的 `window.set_ime_allowed(true)` + `if !inner.ime_allowed { inner.ime_allowed = true; ... }` 守卫——回归保护）
    - 文档断言：summon doc 注释含 "GAP-8" 字符串（说明复位理由的明示锚点）
    - 测试命令：`cargo nextest run -p mybox-palette session` 退出码 0（既有 23/23 session 测试无回归——task 1 的复位行为破坏性影响由 Task 3 探针经真实闭包锁定）
    - 测试命令：`cargo check --workspace` 退出码 0 无 warning
  </acceptance_criteria>
  <done>summon() 复位 ime_allowed=false 与 winit_state=None，每次窗口创建都重新走 ensure_winit_state 显式开启路径。</done>
</task>

<task type="auto">
  <name>Task 2: lib.rs sync_window_geometry Hidden 早退（WR-04）+ summon_palette 初始高度 max(1) 统一（WR-02）</name>
  <read_first>
    - crates/modules/palette/src/lib.rs（summon_palette 168-182、sync_window_geometry 480-505、PaletteState 引入路径——查看文件顶 use 列表）
    - crates/modules/palette/src/ui.rs（window_height 函数 + Hidden 态返回值——确认 WR-04 早退时 last_height 不被 1px 污染）
    - .planning/phases/03-命令面板/03-VERIFICATION.md（Anti-Patterns 表 WR-04 / WR-02 行——修复规格来源 + 229 行建议）
  </read_first>
  <files>crates/modules/palette/src/lib.rs</files>
  <action>
    **a. sync_window_geometry Hidden 早退（WR-04）**：在 `sync_window_geometry`（lib.rs 486-505）函数体的第一行（函数签名 `{` 之后、`let logical_h = ...` 之前）加：
    - `if session.state() == PaletteState::Hidden { return; }`
    - doc 注释追加 WR-04 修复理由：Hidden 态窗口即将销毁（Destroy 随后被排出），无谓的 1px `request_inner_size` + 1px `resize_framebuffer` 是 capture.start 点击执行路径（同帧触发 geometry_revision 递增）每张截图付一次的浪费；早退同时确保 `last_height` 不被 1px 值污染，下一次 summon 重装帧缓冲后第一次 sync 走真实尺寸。**关键**：早退在 `last_height` 锁之前——`last_height` 不被更新，下次 summon 走 Idle 态时 sync 不会被 `*last == physical_h` 短路跳过真实首次同步。

    **b. summon_palette 初始高度 max(1) 统一（WR-02）**：在 `summon_palette`（lib.rs 168-182）176 行将 `ui::window_height(PaletteState::Idle, all.len())` 改为 `ui::window_height(PaletteState::Idle, all.len().max(1))`——与帧循环 491 行 `ui::window_height(session.state(), session.filtered().len().max(1))` 的 `max(1)` 规则对齐。零命令 case 两处都算出 80 而非 128（一致）；生产恒有 ≥4 内置命令，零命令不可达，但潜伏缺陷关闭。

    **不动**：summon_palette 其余行、build_window_spec、on_event_win 闭包——本任务只动 sync_window_geometry 第一行与 summon_palette 176 行两处。
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-palette</automated>
    <automated>cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：lib.rs 的 `fn sync_window_geometry` 函数体第一行（`let logical_h = ...` 之前）含 `if session.state() == PaletteState::Hidden { return; }` 字面量
    - 源码断言：lib.rs 的 `fn summon_palette` 内含 `ui::window_height(PaletteState::Idle, all.len().max(1))` 字面量（WR-02 修复点）
    - 源码断言：lib.rs 的 `fn sync_window_geometry` 内仍含（未删除）03-05 的 `request_inner_size` + `session.resize_framebuffer(new_size.width, new_size.height)` 调用（回归保护，WR-02 帧缓冲伸缩纪律不被本任务破坏）
    - 源码断言：lib.rs `fn sync_window_geometry` doc 注释含 "WR-04" 字符串（说明早退理由的锚点）
    - 测试命令：`cargo nextest run -p mybox-palette` 退出码 0（既有 palette 单测无回归——Hidden 早退不改变 Idle/Filtering/Executing 路径行为；max(1) 统一在零命令态外不可达，生产无回归）
    - 测试命令：`cargo check --workspace` 退出码 0 无 warning
  </acceptance_criteria>
  <done>sync_window_geometry 对 Hidden 态早退（WR-04 1px resize 排除）；summon_palette 初始高度 all.len().max(1) 与帧循环一致（WR-02 零命令潜伏缺陷关闭）。</done>
</task>

<task type="auto">
  <name>Task 3: E2E 探针 ime_commit_updates_input 扩展重唤出阶段 + integration.rs 测试 10 文档更新</name>
  <read_first>
    - crates/modules/palette/src/bin/palette_checks.rs（check_ime_commit_updates_input 1753-1877——本任务扩展目标；check_consecutive_summon_close 758-868——re-summon 模式参照：summon_palette → expect_create → harness.pending_spec = Some(spec) → harness.realize_window(el)?；PaletteHarness 47-168——pending_spec / realize_window / created_id / handle 字段；main 1881-1908——分发与 usage）
    - crates/modules/palette/tests/integration.rs（测试 10 palette_ime_commit_updates_input 140-153——本任务更新文档注释）
    - crates/modules/palette/src/session.rs（Task 1 产物：summon 复位 ime_allowed=false / winit_state=None；ime_allowed() 访问器 247-249）
    - .planning/phases/03-命令面板/03-VERIFICATION.md（frontmatter gaps[0].missing[1]——探针扩展规格）
  </read_first>
  <files>crates/modules/palette/src/bin/palette_checks.rs, crates/modules/palette/tests/integration.rs</files>
  <action>
    **a. 探针 stage 状态机扩展**（check_ime_commit_updates_input 1753-1877）：保留现有 stage 0/1/2（首次唤出 + Preedit + Commit + tuichu 拼音断言），把原 stage 3（ESC → Destroy → Hidden → pass）改为 stage 3+4+5+6 重唤出序列。把闭包参数 `_el` 改 `el`（re-summon 需要 realize_window(el)——与 consecutive_summon_close stage 1 的 re-summon 臂同模式）；同时捕获 `Arc::clone(&session)` / `Arc::clone(&handle)` / `Arc::clone(&ui_proxy)` / `Arc::clone(&registry)` 进闭包（参照 consecutive_summon_close 774-777 行的 Arc::clone 模式）。

    **stage 3（新）——ESC 配对 Destroy + 再 summon**：在原 stage 2 的 `press_key(&s, &h, &ui_lock, Key::Named(NamedKey::Escape))` 之后转 stage 3。stage 3 arm：`match h.handle.try_recv() { Some(WindowRequest::Destroy(id)) => { assert id == h.created_id; assert s.state() == Hidden; assert s.has_live_window()==false; assert s.consume_pending_close()==false（无 pending_close 残留）→ 调用 summon_palette(&s, &h, &registry, &ui_lock) 重新唤出（需在闭包外把 registry 改 Arc::clone 进闭包——参照 consecutive_summon_close 776-777 行）→ expect_create(&h)? → harness.pending_spec = Some(spec) → harness.realize_window(el)? → stage = 4 } _ => return Ok(()) }`

    **stage 4（新，核心覆盖点）——第二窗口复位 + re-set 断言**：`h.inject(WindowEvent::RedrawRequested)?` 后：
    - 断言 `s.ime_allowed() == false`（reset assertion：summon 复位的 ime_allowed=false 在首帧之前可观察——这是 GAP-8 修复路径被行使的关键证据，对齐 03-VERIFICATION.md gaps[0].missing[1] 的"断言第二窗口的 ime_allowed 复位→重新置位路径被行使"）
    - 再 `h.inject(WindowEvent::RedrawRequested)?`（首帧经真实生产闭包触发 ensure_winit_state——复位的 ime_allowed=false 让 `if !inner.ime_allowed` 守卫重新生效、新建 egui-winit State、对新 winit Window 调用 set_ime_allowed(true)）
    - 断言 `s.ime_allowed() == true`（re-set path 在第二窗口上被行使——这正是 WR-01 缺陷路径锁定的证据）

    **stage 5（新，零回归断言——可选但推荐）**：在第二窗口上注入中文 Ime 流验证零回归：`h.inject(WindowEvent::Ime(Ime::Preedit("重".to_string(), None)))?` + `h.inject(WindowEvent::RedrawRequested)?` + `h.inject(WindowEvent::Ime(Ime::Commit("重新截图".to_string())))?` + `h.inject(WindowEvent::RedrawRequested)?` → 断言 `s.input() == "重新截图"` + `s.state() == Filtering`（验证新 egui-winit State 正确接收 Ime · 03-VERIFICATION.md gaps[0].missing[1] 末尾"可加 Ime::Commit 中文断言于第二窗口"的建议）

    **stage 6（新，收敛）**：`press_key(&s, &h, &ui_lock, Key::Named(NamedKey::Escape))` → `match h.handle.try_recv() { Some(WindowRequest::Destroy(id)) => { assert id == h.created_id（这里 created_id 已是第二窗口的 id，由 realize_window 在 stage 3 末更新）; assert s.state()==Hidden; assert s.has_live_window()==false; assert s.consume_pending_close()==false; harness.pass(); } _ => return Ok(()) }`

    **覆盖声明修正（保持诚实）**：在 check_ime_commit_updates_input 的模块 doc 注释中追加："本探针已扩展重唤出阶段：summon → ESC 配对 Destroy → 再 summon → 断言第二窗口的 ime_allowed 由 false 复位→true 的 re-set 路径被行使（GAP-8 修复覆盖洞）。OS 输入法组合窗（候选词）的出现/交互仍只能由 UAT 测试 10 重唤出场景人工复验。"

    main() 分发（1881-1908）：无需新增 match 臂——本探针仍是 `ime_commit_updates_input`（同名称，内容扩展）；usage 字符串无需改动。

    **b. integration.rs 测试 10 文档注释更新**（140-153）：把现有 Test 10 doc 注释末尾"The OS candidate-window appearance is re-verified by human UAT test 10."之前追加："**Re-summon extension (03-09, GAP-8/Wr-01）：** ESC 关闭后通过 `summon_palette` 重新唤出第二窗口，断言 `ime_allowed` 在第二窗口首帧由 false 复位→true（REVIEW WR-01 缺陷路径——03-08 探针只覆盖首次唤出，让缺陷在 10/10 全绿下漏过）。"第一行 doc 注释"Test 10 — GAP-7 regression (03-08, PAL-03)"扩展为"Test 10 — GAP-7 + GAP-8 regression (03-08/03-09, PAL-01/PAL-03)"。无新测试接线（仍是单一 `palette_ime_commit_updates_input`，桌面会话测试数：10/10）。

    **不动**：其他 9 个探针函数（summon_render / fuzzy_navigation_execute / capture_hides_first / five_summon_esc_no_residue / consecutive_summon_close / glyph_shape / position_stable_on_filter / hover_click_alignment / ctrl_pn_navigation）与对应 integration 测试 1-9——本任务只扩展 ime_commit_updates_input 及其 integration 测试 10。
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-palette -p mybox-core</automated>
    <automated>cargo build -p mybox-palette --bin palette_checks && cargo test -p mybox-palette --test integration -- --ignored</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：palette_checks.rs 的 `fn check_ime_commit_updates_input` 内含 `s.ime_allowed() == false` 字面量（reset assertion）与紧随其后的 `s.ime_allowed() == true` 字面量（re-set assertion）——两断言都在重唤出 stage 4 出现
    - 源码断言：palette_checks.rs 的 `fn check_ime_commit_updates_input` 内含对 `summon_palette(&s, &h, &registry, &ui_lock)`（或 Arc::clone 等价闭包调用）的二次调用（re-summon 入口）与紧随 `expect_create(&h)?` + `harness.realize_window(el)?` 模式（与 consecutive_summon_close 852-857 同模式）
    - 源码断言：palette_checks.rs 的 `fn check_ime_commit_updates_input` 内含 `Ime::Commit("重新截图".to_string())` 或等价中文 Ime 注入（零回归 stage 5 可选断言）
    - 源码断言：palette_checks.rs 的 `fn check_ime_commit_updates_input` doc 注释含 "GAP-8"（覆盖声明修正锚点）
    - 源码断言：palette_checks.rs 的 `fn check_ime_commit_updates_input` 闭包参数已从 `_el` 改为 `el`（被 realize_window 使用）
    - 源码断言：integration.rs 测试 10 `fn palette_ime_commit_updates_input` 的 doc 注释含 "GAP-8" 与 "03-09"（双重标记）
    - 测试命令（headless 编译与单测）：`cargo nextest run -p mybox-palette -p mybox-core` 退出码 0
    - 测试命令（桌面会话，verify-phase 重跑）：`cargo test -p mybox-palette --test integration -- --ignored` 10/10 通过——其中 `ime_commit_updates_input` 实跑断言：首次唤出 ime_allowed==true → 中文 Commit 进入 session.input → tuichu 拼音命中 → ESC → Destroy → 再 summon → 第二窗口首帧前 ime_allowed==false（复位证据）→ 首帧后 ime_allowed==true（re-set 证据）→ 第二窗口中文 Ime 流零回归 → ESC → Destroy → Hidden 收尾
  </acceptance_criteria>
  <done>E2E 探针 ime_commit_updates_input 锁定 GAP-8 修复路径——重唤出后第二窗口 ime_allowed 复位→重新置位经真实闭包断言；中文 IME 流在第二窗口零回归；10/10 集成测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 窗口生命周期状态机（summon/close/Destroy）→ egui-winit State 重建 | 每次窗口创建重置内部 IME/State 缓存——复位时序与并发事件可达性 |
| ensure_winit_state 的 winit 调用 → winit Window | 在新窗口上重新请求 IME 允许——OS 输入法通道开启的副作用面 |
| sync_window_geometry Hidden 早退 → 帧缓冲生命周期 | 早退避免 Hidden 态无谓分配——早退必须在 last_height 锁前避免状态污染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Tampering (race TOCTOU 在复位) | summon 复位 `ime_allowed=false`/`winit_state=None` vs ensure_winit_state 读取 | mitigate | 复位与读取都通过同一 `state: Mutex<SessionInner>` 串行访问——summon 与 ensure_winit_state 均单线程主线程路径（hotkey/toggle/事件回调），mutex 保证复位对后续 ensure_winit_state 可见；不持有锁调用 winit（03-08 锁序纪律） |
| T-03-20 | Information Disclosure (复位时序错误使 IME 启用于已关闭窗口) | summon 复位 ime_allowed=false 与 winit 销毁时机 | accept | summon 在新窗口 Create 之前同步复位；winit 在 Destroy 时自行拆卸 IME 与该窗口的关联；复位错位最坏情况是 IME 在新窗口未开启（功能性 fail-closed 而非 fail-open），无信息泄露面 |
| T-03-21 | Denial of Service (winit_state=None 触发每次 summon 重建 egui-winit State 的内存压力) | summon 复位 winit_state=None | accept | egui-winit State 是几百字节级别结构，每次唤出重建一次性开销；03-08 的"per-session 永久复用"以正确性为代价换取的省略是不可持续的（GAP-8 即后果）；本任务恢复每次 summon 重建，性能影响可忽略 |
| T-03-22 | Tampering (update synchrony) Hidden 早退污染 last_height 导致下次 summon 首次 sync 被跳过) | sync_window_geometry Hidden 早退与 last_height 锁的相对位置 | mitigate | 早退置于 `let mut last = last_height.lock().unwrap();` 之前——`*last` 不被写为 1；下次 summon 后第一次 sync 进入 Idle 态不被 `*last == physical_h` 短路跳过真实同步（Action 已明示锁序约束） |
| T-03-23 | Reliability (max(1) 统一行为差异) | summon_palette 初始高度与帧循环 max(1) 统一 | accept | 生产恒有 ≥4 内置命令——零命令态不可达；统一后最差行为是零命令时初始与帧循环一致算出 80 而非冲突 80 vs 128（统一为 80），无新失败模式 |
| T-03-24 | Repudiation (探针重唤出阶段断言与生产路径一致性) | check_ime_commit_updates_input stage 3-6 用真实 on_event_win 闭包与生产 summon_palette | mitigate | stage 3 re-summon 直接调用生产 `summon_palette(&s, &h, &registry, &ui_lock)`（与 consecutive_summon_close 同模式），不绕过 session/lib.rs 任何代码路径——覆盖声明已明示合成 Ime 限制与 OS 候选窗留待 UAT，诚实性已校准 |
| T-03-SC | Tampering | cargo 安装 | accept | 本计划零新增依赖（无 package-manager install 任务）——无需 slopcheck 与 human checkpoint |
</threat_model>

<verification>
- `cargo nextest run -p mybox-palette session`：session 单测全绿（含 Task 1 复位无回归）
- `cargo nextest run -p mybox-palette`：palette 单测全绿（含 Task 2 Hidden 早退与 max(1) 统一无回归）
- `cargo nextest run -p mybox-palette -p mybox-core`：headless 编译与单测全绿
- `cargo nextest run --workspace`：228+ 单测全绿（与 03-08 同基线：本计划不增减单测数，仅在 summon 内加两条复位语句不改单测预期）
- `cargo check --workspace`：exit 0 无 warning
- 桌面会话（verify-phase 重跑）：`cargo test -p mybox-palette --test integration -- --ignored` 10/10 通过，其中 `ime_commit_updates_input` 实跑断言重唤出 stage——第二窗口 ime_allowed 由 false 复位→true 的 re-set 路径被行使（GAP-8 BLOCKER 闭环证据）
- QUALITY 账本目标：03-09 关闭后，VERIFICATION.md Anti-Patterns 表 GAP-8 BLOCKER 行、WR-04 行、WR-02 行全部从 Anti-Patterns 列表移除；剩余 deferred 4 项（WR-03 锁存/IN-01 panic/IN-02 锁序文档/IN-04 配置目录降级）仍归 Phase 4——本计划不动 deferred 列表
- Human 最终复验：UAT 测试 1 重跑（物理热键循环 ≥3 轮）+ UAT 测试 10 重跑（首次唤出与 ESC 关闭后重唤出均输入中文，候选窗两次都出现）——OS 物理输入链路只能人工验证
</verification>

<success_criteria>
- GAP-8 BLOCKER 代码层闭环：`summon()` 复位 `ime_allowed=false` 与 `winit_state=None`，`ensure_winit_state` 在每次窗口创建重新执行显式 `window.set_ime_allowed(true)` 并重建 egui-winit State——源码可证 + 探针重唤出阶段经真实闭包断言
- 探针覆盖洞关闭：`ime_commit_updates_input` 增加重唤出 stage，覆盖声明明示重唤出路径——缺陷不再在 10/10 全绿下漏过
- WR-04 顺带关闭：`sync_window_geometry` 对 Hidden 态早退，capture.start 点击执行路径不再付 1px resize + 1px framebuffer 重分配
- WR-02 顺带关闭：`summon_palette` 初始高度 `all.len().max(1)` 与帧循环 max(1) 规则一致，零命令潜伏缺陷（80 vs 128）关闭
- SPEC 边界未扩大：无新增依赖、无自研 IME 状态机、无拼音转换引擎——03-SPEC/CONTEXT/RESEARCH/PATTERNS/UI-SPEC/VALIDATION/VERIFICATION 不改动
- 三任务原子提交：Task 1（session.rs 复位）、Task 2（lib.rs WR-04 + WR-02）、Task 3（探针扩展 + integration doc）各自 cargo nextest 一次通过、零 warning；同 wave 共享文件统一提交
- Phase 03 完整收尾：GAP-1..GAP-8 全部关闭；REQUIREMENTS PAL-01..PAL-05 全部 Complete；剩余 4 项 deferred（WR-03/IN-01/IN-02/IN-04）归 Phase 4 错误处理打磨；UAT 1/3/4/10 人工最终复验在 03-09 后执行
</success_criteria>

<output>
创建 `.planning/phases/03-命令面板/03-09-SUMMARY.md`（含 gap 修复记录、GAP-8 BLOCKER 闭环证据、WR-04/WR-02 顺带修复说明、探针重唤出阶段覆盖洞关闭声明、spec 边界声明、验证证据）
</output>