---
phase: 03-命令面板
plan: 10
type: execute
wave: 10
depends_on: ["03-09"]
autonomous: true
gap_closure: true
files_modified:
  - crates/modules/palette/src/filter.rs
  - crates/modules/palette/src/ui.rs
  - crates/modules/palette/src/lib.rs
  - crates/modules/palette/src/bin/palette_checks.rs
  - crates/modules/palette/tests/integration.rs
  - .planning/phases/03-命令面板/03-UI-SPEC.md
requirements: [PAL-03, PAL-04]

must_haves:
  truths:
    - "输入「jt」/「jietu」时「开始截图」命中排前，命中的拼音 keyword 字符以 #FF6000 高亮"
    - "全部拼音 keyword（jietu/tuichu/peizhi/chongqi/rizhi）命中路径同机制显示 keyword 文本 + #FF6000 高亮（不止 capture.start）"
    - "鼠标点击执行「开始截图」时窗口在点击帧内同步隐藏（window.is_visible() == Some(false)），Destroy 排出前已从屏幕消失——截图绝不含面板"
    - "Enter 与鼠标点击两条路径时序一致：面板先关闭、不再出现"
  artifacts:
    - path: "crates/modules/palette/src/filter.rs"
      provides: "Match.keyword_hit（命中的 keyword 字符串 + fuzzy_indices 字符位置）——keyword 梯队命中索引的数据载体"
      contains: "keyword_hit"
    - path: "crates/modules/palette/src/ui.rs"
      provides: "draw_command_row 渲染 keyword tag（description 行尾「 · {keyword}」，命中字符 #FF6000）"
      contains: "keyword_hit"
    - path: "crates/modules/palette/src/lib.rs"
      provides: "RedrawRequested 帧循环 Hidden 守卫 + window.set_visible(false) 同步隐藏早退"
      contains: "set_visible"
    - path: "crates/modules/palette/src/bin/palette_checks.rs"
      provides: "E2E 探针 keyword_highlight + click_hide_before_capture"
      contains: "click_hide_before_capture"
  key_links:
    - from: "filter.rs keyword 梯队分支（fuzzy_indices）"
      to: "Match.keyword_hit"
      via: "fuzzy_indices(kw, &query) 返回 (score, indices)——取最高分 keyword 及其字符索引存入 Match"
      pattern: "fuzzy_indices"
    - from: "ui.rs draw_command_row"
      to: "keyword tag LayoutJob（ACCENT 命中字符）"
      via: "highlight_job 对 keyword 的 indices 着色 #FF6000；「 · 」分隔符 TEXT_DIM"
      pattern: "keyword_hit"
    - from: "ui.rs resp.clicked() → execute → session.close() → Hidden"
      to: "lib.rs 帧循环 Hidden 守卫 → window.set_visible(false)"
      via: "egui_ctx.run 返回后检查 state==Hidden → 同步隐藏 + return（跳过 paint/present/request_redraw）"
      pattern: "PaletteState::Hidden"
---

<objective>
关闭 Phase 3 UAT 全部 2 个留存 gap（03-UAT.md gaps[0] minor + gaps[1] major），一个 gap-closure 计划（03-10）：

1. **Gap 1（minor，UAT 测试 5——keyword 梯队命中无高亮）**：输入「jt」/「jietu」时「开始截图」命中排前（过滤排名正常）但命中字符无 #FF6000 高亮。根因是双层缺失：(a) 数据层 filter.rs 的 keyword 梯队分支（L94-102）用 `fuzzy_match` 仅计分，构造的 Match 恒为空 indices，且 Match 无 keyword 索引字段；(b) 渲染层 ui.rs 行只渲染 name+description，keywords 字符串从不显示，查询字符在 name/description 中不存在（实测 fuzzy_indices 均 None），无高亮目标。**Planner 决策（Route A，不重新协商 UAT truth）**：UAT truth「命中字符以 #FF6000 高亮（含拼音关键词命中路径）」是用户锁定期望——实现行内渲染被命中的 keyword 文本（description 行尾「 · jietu」标签）并用现有 highlight_job 机制着色命中字符。该 Route 需要 UI-SPEC.md 修订（CommandRow 规格 + Accent 保留区文案）——文档修订随代码实现一并落地。

2. **Gap 2（major，UAT 测试 11——鼠标点击路径面板被拍进截图）**：鼠标点击「开始截图」面板先关闭又出现、面板被截入截图；Enter 路径正常。根因：点击路径的 execute()（含 hide_before_execute 的 Destroy 入队）发生在 egui 帧内（ui.rs L415-419 `resp.clicked()` 在 egui_ctx.run 闭包中），点击帧在 execute() 后仍完成整帧 paint + present 并 L341 额外 request_redraw；Destroy 只在 about_to_wait 排出（主线程被帧工作阻塞 2-10ms+），与 capture 链屏幕读取（点击后 3-20ms）竞态重叠——面板仍可见时被 xcap 读入截图。Enter 路径 on_palette_key 返回 true 后 on_event_win 早退（L267）不跑帧循环，Destroy 微秒级排出。**修复**：lib.rs RedrawRequested 帧循环在 `egui_ctx.run` 返回后、`apply_textures` 之前插入 Hidden 守卫——`session.state() == PaletteState::Hidden` 时 `window.set_visible(false)` + `return`，同步隐藏窗口（macOS `orderOut` 即刻生效，winit 0.30.13 vendored window_delegate.rs:905/912 已验证）并跳过本帧剩余工作（apply_textures/tessellate/paint/handle_platform_output/geometry 同步/L341 request_redraw）。execute.rs 不动（Destroy 先于 runner 的队列序保持，Pitfall 4）。

**SPEC/UI 边界声明**：无新增依赖、无自研拼音转换引擎；filter/execute/session 状态机结构不变；唯一文档修订是 UI-SPEC.md 的 CommandRow 行 + Accent 保留区文案（Route A 渲染元素的规格化）。03-SPEC/CONTEXT/RESEARCH/PATTERNS/VALIDATION/VERIFICATION 不改动。

**范围边界声明（Planner 决策，scope_sanity 检查的 borderline 接受）**：本计划 4 任务、6 文件，处于粒度警告阈值（2-3 任务建议的边界）。**不拆分**——理由：(a) Task 4 的两个探针是 Task 1-3 修复的端到端验证，探针依赖 Task 1-3 产物（keyword_hit 数据 + keyword tag 渲染 + Hidden 守卫）才能编写与运行，拆分会制造探针先行/空跑或跨 wave 文件冲突（palette_checks.rs/integration.rs 是共享文件）；(b) 拆分 03-11 仅把探针移到下一 wave，不减少任何工作，反而增加一次共享文件串行化成本；(c) 4 任务各自上下文成本 ~10-15%（Task 4 探针 ~25%），总量 ~50-60%，在质量退化曲线可接受区间。两 gap 的验证耦合（探针同时锁定两修复路径）是保持单计划的根本原因。

Output: filter.rs keyword_hit 数据通道 + ui.rs keyword tag 渲染 + lib.rs Hidden 帧循环守卫 + 2 个新 E2E 探针（keyword_highlight / click_hide_before_capture）+ integration.rs 测试 11/12 + UI-SPEC.md CommandRow 修订。REQUIREMENTS.md 不改动（PAL-03/PAL-04 已 Complete；本计划强化 PAL-03 keyword 梯队高亮完整性 + PAL-04 点击路径时序与 Enter 一致）。
</objective>

<execution_context>
@/Users/fish/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fish/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-命令面板/03-UAT.md
@.planning/debug/palette-keyword-tier-highlight.md
@.planning/debug/palette-capture-click-path.md
@.planning/phases/03-命令面板/03-09-PLAN.md

Gap 事实（03-UAT.md gaps）：gaps[0] = keyword 梯队命中无 #FF6000 高亮（minor，测试 5）；gaps[1] = 鼠标点击执行「开始截图」面板被拍进截图（major，测试 11）。两个 debug 会话均已 diagnosed（root_cause 已确认，未修复——本计划实现其 fix 方向）。

关键代码位置（勿重复读源码）：
- filter.rs：Match 结构体 L28-37（cmd_index/score/name_indices/description_indices）、tier_of L41-49（name 非空→0 / description 非空→1 / 否则 2）、keyword 梯队分支 L94-102（`matcher.fuzzy_match(kw, &query)` 仅计分）、空查询 Match 构造 L61-71、MAX_QUERY_LEN=64、DESCRIPTION_TIER_OFFSET=100_000、KEYWORD_TIER_OFFSET=200_000、单测 L164-239（query_jietu_hits_capture_via_pinyin_keyword 等 7 个）
- ui.rs：draw_command_list L338-355（传 name_hl/description_hl）、draw_command_row L378-431（row 交互 + name/desc 两个 highlight_job + galley）、highlight_job L436-455（ACCENT 只变色）、char_indices_to_byte_ranges L460-482（越界跳过 + 合并连续段）、ACCENT 常量 L26（#FF6000）、TEXT_DIM L29（#A8A8A8）、row_interact_hovers_and_clicks_execute 单测 L681-691（draw_command_row 直接调用点——新增参数后必须补 None）
- lib.rs：on_event_win 闭包 L226-344、键盘早退 L267、RedrawRequested 帧循环 L286-343（egui_ctx.run L297 → apply_textures L298 → tessellate L299 → paint L305-322 → handle_platform_output L323-328 → geometry 同步 L337-342 含 L341 request_redraw）、repaint() 基于 window_id（close 后 None → no-op）
- execute.rs：execute L31-77（set_executing → hide_before_execute 时 session.close()+windows.destroy 先入队 → run_command；finalize 由 generation 守卫 no-op）——**本计划不动**
- palette_checks.rs：PaletteHarness L47-255（inject/realize_window/render_present/pass/window_event 驱动/50ms 轮询/10s watchdog）、fake_command L290-304、gated_runner L324-336、registry_with L337-345、check_fuzzy_navigation_execute L465-583（keyword_highlight 探针脚手架参照——set_input + filtered 断言 + gated runner + Destroy 收尾）、check_hover_click_alignment L1372-1569（click_hide_before_capture 探针脚手架参照——CursorMoved/MouseInput 注入 + 行带像素扫描 + gated runner 释放）、main() 分发 L2043-2058（match 臂 + usage 字符串）
- integration.rs：run_check L28-37（spawn CARGO_BIN_EXE_palette_checks）、测试 1-10 L42-164（追加测试 11/12）

依赖源码事实（勿读）：winit 0.30.13 macOS 后端 `set_visible(false)` → `self.window().orderOut(None)`（window_delegate.rs:905），`is_visible()` → `Some(self.window().isVisible())`（window_delegate.rs:912）——orderOut 同步即时、is_visible 确定性 Some。egui 0.30 LayoutJob 的 `.text` 字段为 pub String、`.sections` 为 pub Vec（byte_range 索引进 .text）——keyword tag 合并进 desc_job 时按 byte_range 逐段 append。

注意：03-05..03-09 已改 session.rs/lib.rs/palette_checks.rs/integration.rs（WR-01 修订计数、WR-04 Hidden 早退、max(1) 统一、IME 复位、consecutive_summon_close 的 re-summon 模式）——本计划在其成果之上叠加，勿回退既有守卫/字段/探针。
</context>

<tasks>

<task type="auto">
  <name>Task 1: filter.rs Match.keyword_hit——keyword 梯队命中索引数据通道（Gap 1 数据层）</name>
  <read_first>
    - crates/modules/palette/src/filter.rs（Match L28-37、tier_of L41-49、keyword 分支 L94-102、空查询构造 L61-71、单测 L164-239）
    - .planning/phases/03-命令面板/03-UAT.md（gaps[0].missing[0]——"Match 增加 keyword 命中信息（命中的 keyword 字符串 + fuzzy_indices）"）
    - .planning/debug/palette-keyword-tier-highlight.md（root_cause + Evidence 各条——修复规格来源）
  </read_first>
  <files>crates/modules/palette/src/filter.rs</files>
  <action>
    **a. 新增 `KeywordHit` 结构体与 `Match.keyword_hit` 字段**：在 filter.rs 顶部（tier_of 之前）定义 `#[derive(Clone, Debug, PartialEq, Eq)] pub struct KeywordHit { pub keyword: &'static str, pub indices: Vec<usize> }`——**必须自带 derive（镜像 Match L28 的四个 derive）**：Rust 的 derive 不会传播到嵌套类型，`Match` 的 `#[derive(PartialEq, Eq)]` 只作用于 Match 自身字段，`Option<KeywordHit>` 字段的相等比较要求 KeywordHit 自身实现 PartialEq/Eq（缺失则 `cargo check` 首跑即编译失败；单测 `keyword_hit == Some(KeywordHit { .. })` 与 `matches == filter_commands(...)` 的比较都依赖它）。keyword 为命中的 keyword 字符串字面量——Command.keywords 是 `Vec<&'static str>`，可零拷贝引用；indices 为 fuzzy_indices 返回的**字符**位置，与 name_indices/description_indices 同语义，UI 层统一经 char_indices_to_byte_ranges 转字节区间。`Match` 结构体追加 `pub keyword_hit: Option<KeywordHit>`（默认 None；仅 keyword 梯队命中时 Some）。

    **b. keyword 梯队分支改用 fuzzy_indices 并保留最佳命中**：把 L94-102 的 `matcher.fuzzy_match(kw, &query)`（仅分数）替换为遍历 `cmd.keywords` 逐一调用 `matcher.fuzzy_indices(kw, &query)`，取 (score, keyword, indices) 三元组中 score 最高者（同分取先者——与现 `.max()` 语义一致，注册顺序确定性不破坏）。构造 `Match { cmd_index, score: s - KEYWORD_TIER_OFFSET, name_indices: Vec::new(), description_indices: Vec::new(), keyword_hit: Some(KeywordHit { keyword, indices }) }`。无任何 keyword 命中仍 `continue` 排除该命令。

    **c. 其余 Match 构造点补 `keyword_hit: None`**：空查询分支（L61-71）与 name/description 梯队分支（L83-93）构造的 Match 均显式 `keyword_hit: None`。**tier_of 不动**——keyword 梯队仍靠 name/description indices 双空推导为 tier 2，新增字段不影响梯队判定。

    **d. doc 注释**：filter.rs 模块 doc（L1-8）与 `Match` 结构体 doc 追加一句：keyword 梯队命中携带 `keyword_hit`（keyword 字符串 + 字符索引），供 UI 层渲染 keyword 标签并高亮命中字符（Gap 1 / UAT test 5）。

    **e. 单测**：在 filter.rs tests mod 内新增/扩展。**前置（必做）：扩展测试 fixture `inventory()`（L139-162）**——为 builtin.quit / builtin.open_config / builtin.restart / builtin.open_log 的 keywords 分别追加 'tuichu' / 'peizhi' / 'chongqi' / 'rizhi'（与生产 command.rs L111/134/150/179 的拼音别名对齐；已核实无既有测试依赖这些 keyword 缺席——`query_screenshot_ranks_capture_first_and_indices_correct`、`name_tier_beats_keyword_tier` 等均不受影响，`tie_break_keeps_registration_order` 用自建 cmd 不经过 inventory()）。否则 `pinyin_keywords_all_carry_keyword_hit` 首跑即失败（fixture 无拼音 keyword 可命中）。
    - 扩展 `query_jietu_hits_capture_via_pinyin_keyword`：断言 `matches[0].keyword_hit == Some(KeywordHit { keyword: "jietu", indices: vec![0, 3] })`（"jt" 是 "jietu" 的子序列，j 在 char 0、t 在 char 3）
    - 新测试 `pinyin_keywords_all_carry_keyword_hit`：对 "tuichu"/"peizhi"/"chongqi"/"rizhi" 四个查询逐一断言命中命令的 `keyword_hit` 为 Some 且 `indices` 非空、`keyword` 字段与查询字符串一致（覆盖整个 keyword 梯队——UAT gaps[0].missing[2] 的 filter 层断言要求；依赖 e 前置的 inventory() fixture 扩展）
    - 新测试 `name_tier_match_has_no_keyword_hit`：查询「截图」命中 name 梯队，断言 `keyword_hit.is_none()`
    - 新测试 `empty_query_has_no_keyword_hit`：空查询全部 Match 的 `keyword_hit.is_none()`
    - 既有 7 个单测不删不改预期（Match 新增字段不改排序/梯队行为；`Match` 的 `PartialEq, Eq` derive 覆盖新字段的比较——`KeywordHit` 自身已在 a 步带同名 derive，Rust derive 不传播，故必须由 KeywordHit 自己实现）
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-palette filter::</automated>
    <automated>cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：filter.rs 含 `pub struct KeywordHit`（含 `keyword: &'static str` 与 `indices: Vec<usize>` 字段）
    - 源码断言：filter.rs 的 KeywordHit 定义带 `#[derive(Clone, Debug, PartialEq, Eq)]` 属性行（镜像 Match L28——Rust derive 不传播到嵌套类型，缺失则 `keyword_hit == Some(..)` 单测比较编译失败）
    - 源码断言：filter.rs 的 `pub struct Match` 含 `pub keyword_hit: Option<KeywordHit>` 字段
    - 源码断言：filter.rs keyword 梯队分支不再含 `fuzzy_match` 调用（改用 fuzzy_indices——grep `fuzzy_match` 在 filter.rs 内 0 命中）
    - 源码断言：filter.rs 空查询分支与 name/description 分支构造的 Match 含 `keyword_hit: None` 字面量
    - 源码断言：filter.rs 单测含 `KeywordHit { keyword: "jietu", indices: vec![0, 3] }` 断言与 `keyword_hit.is_none()` 断言
    - 源码断言：filter.rs 单测 fixture `inventory()` 内含 `"tuichu"` / `"peizhi"` / `"chongqi"` / `"rizhi"` 四个拼音 keyword 字面量（builtin 命令 keywords——`pinyin_keywords_all_carry_keyword_hit` 的命中前提）
    - 测试命令：`cargo nextest run -p mybox-palette filter::` 退出码 0（含新增 4 项断言单测 + 既有 7 项无回归）
    - 测试命令：`cargo check --workspace` 退出码 0 无 warning
  </acceptance_criteria>
  <done>keyword 梯队命中在 filter 层携带 keyword 字符串 + 字符索引；全部拼音 keyword 命中路径有数据可高亮。</done>
</task>

<task type="auto">
  <name>Task 2: ui.rs keyword tag 渲染 + UI-SPEC CommandRow 修订（Gap 1 渲染层）</name>
  <read_first>
    - crates/modules/palette/src/ui.rs（draw_command_list L338-355、draw_command_row L378-431、highlight_job L436-455、char_indices_to_byte_ranges L460-482、row_interact 单测 L681-691）
    - .planning/phases/03-命令面板/03-UAT.md（gaps[0].missing[1]——"行内渲染被命中的 keyword 文本并用 highlight_job 着色 #FF6000"）
    - .planning/phases/03-命令面板/03-UI-SPEC.md（L63 高亮只变色不变字号字重、L75 Accent 色表行、L88 Accent 保留区、L121 CommandRow 行）
    - crates/modules/palette/src/filter.rs（Task 1 产物：KeywordHit + Match.keyword_hit）
  </read_first>
  <files>crates/modules/palette/src/ui.rs, .planning/phases/03-命令面板/03-UI-SPEC.md</files>
  <action>
    **a. 新增 `keyword_tag_job` 纯函数（可测，镜像 highlight_job 模式）**：在 ui.rs highlight_job 附近新增 `fn keyword_tag_job(keyword: &str, indices: &[usize], size: f32) -> (String, egui::text::LayoutJob)`——返回 `(标签字符串, LayoutJob)`：标签字符串为 `" · " + keyword`；LayoutJob 内「 · 」段用 TEXT_DIM + size 字号，「 · 」之后按 `char_indices_to_byte_ranges(keyword, indices)` 分段——命中段 ACCENT、未命中段 TEXT_DIM（复用 highlight_job 的 fmt 闭包模式，只改颜色不改字号字重——UI-SPEC L63 不变式）。返回字符串是因为 LayoutJob 的 `.text` 为 pub 字段、`.sections` 的 byte_range 索引进 `.text`——调用方需按 byte_range 从该字符串切片逐段合并。

    **b. draw_command_row 追加 keyword_hit 参数并渲染标签**：`draw_command_row` 签名追加 `keyword_hit: Option<&filter::KeywordHit>`（放在 description_hl 之后、session 之前——同步更新全部调用点）。在 L427-429 的 desc_job/galley 渲染处：desc_job 构建后，若 `keyword_hit` 为 Some，调用 `keyword_tag_job(kh.keyword, &kh.indices, FONT_SIZE_DESC)` 得到 (tag, tag_job)，遍历 `tag_job.sections` 按 `byte_range` 从 tag 切片、以各 section 的 format 逐段 `desc_job.append(...)`（LayoutJob 支持多次 append——最终一个 desc_galley 含 desc 文本 + 「 · keyword」标签，命中字符 ACCENT）。**行高不变**：标签在同一条 description 行内追加，不新增行、不改变 name/desc 垂直布局（row_geometry_fits_48px 不变式守护）。

    **c. draw_command_list 传参**：L343-354 处 `hl` 是 `Option<&Match>`——追加 `hl.and_then(|m| m.keyword_hit.as_ref())` 作为新实参传给 draw_command_row（name/description 梯队的 Match keyword_hit 为 None → 不渲染标签，行为与现状一致）。

    **d. 既有单测调用点补参**：`row_interact_hovers_and_clicks_execute`（L681-691）直接调用 draw_command_row——追加 `None` 实参（该测试的 cmd keywords 为空）。

    **e. 新增 ui.rs 单测**：`keyword_tag_job_marks_matched_chars_accent`——对 `keyword_tag_job("jietu", &[0, 3], 12.0)` 断言返回标签为 `" · jietu"`、sections 含 ACCENT 段（byte_range 覆盖 j 与 t 的位置）且「 · 」段为 TEXT_DIM；对空 indices 断言全 TEXT_DIM 无 ACCENT。

    **f. UI-SPEC.md 修订（Route A 渲染元素规格化）**：
    - L121 CommandRow 行末尾追加规格：「keyword-tier 命中（拼音等）时 description 行尾追加 ` · {keyword}` 标签——同 12/400，命中字符 #FF6000（UAT test 5 高亮目标）；标签在同行内、不新增行」
    - L75 Accent 色表行文案「Matched-keyword character highlight ONLY」扩展为「Matched-keyword character highlight ONLY（name / description / 渲染的 keyword 标签）」
    - L88 Accent 保留区文案「matched-keyword character highlight in command name and description text」扩展为「...in command name, description text, and the rendered keyword tag」——「Nothing else」不变式保持
    - L63 高亮只变色不变字号字重不变式保持（新增标签同样遵守）
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-palette ui::</automated>
    <automated>cargo nextest run -p mybox-palette</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：ui.rs 含 `fn keyword_tag_job`（签名含 keyword/indices/size 参数）
    - 源码断言：ui.rs 的 `fn draw_command_row` 签名含 `keyword_hit` 参数，且函数体内对该参数做 Option 匹配并在 Some 时调用 `keyword_tag_job`
    - 源码断言：ui.rs 的 `fn draw_command_list` 调用 draw_command_row 时传 `keyword_hit` 实参（`hl.and_then(...)` 或等价）
    - 源码断言：ui.rs 单测含 `keyword_tag_job("jietu", &[0, 3], 12.0)`（或等价）断言 ACCENT 段与 TEXT_DIM 分隔段
    - 文档断言：03-UI-SPEC.md 的 CommandRow 行（L121 附近）含 `keyword` 标签规格文案与「#FF6000」字样；Accent 保留区（L88 附近）含「rendered keyword tag」字样
    - 测试命令：`cargo nextest run -p mybox-palette ui::` 退出码 0（含新增 keyword_tag_job 单测 + 既有 ui 单测含 row_interact 补参后无回归）
    - 测试命令：`cargo nextest run -p mybox-palette` 退出码 0（filter + ui + session + execute 全绿）
  </acceptance_criteria>
  <done>keyword 梯队命中的 keyword 文本在 description 行尾渲染（「 · jietu」），命中字符 #FF6000 高亮；UI-SPEC CommandRow/Accent 规格同步修订。</done>
</task>

<task type="auto">
  <name>Task 3: lib.rs 帧循环 Hidden 守卫 + window.set_visible(false)（Gap 2 主修复）</name>
  <read_first>
    - crates/modules/palette/src/lib.rs（RedrawRequested 帧循环 L286-343——插入点：L297 egui_ctx.run 之后、L298 apply_textures 之前）
    - .planning/phases/03-命令面板/03-UAT.md（gaps[1].missing[0]——"hide_before_execute 时帧内同步 window.set_visible(false)（macOS 即刻 orderOut）并跳过本帧剩余 paint/present"；missing[1]——"Hidden 态跳过 window.request_redraw()"）
    - .planning/debug/palette-capture-click-path.md（root_cause + Evidence——修复规格来源）
    - crates/modules/palette/src/execute.rs（L31-77——不动，确认 Destroy 先入队 + finalize generation 守卫）
    - crates/modules/palette/src/session.rs（close L210-213——state 置 Hidden + 返回 window_id；repaint 依赖 window_id）
  </read_first>
  <files>crates/modules/palette/src/lib.rs</files>
  <action>
    **a. 插入 Hidden 守卫（Gap 2 主修复）**：在 RedrawRequested 帧循环的 `let full_output = egui_ctx.run(...)`（L297）之后、`session.apply_textures(...)`（L298）之前插入：

    ```
    // Gap 2 (03-10, UAT test 11): 鼠标点击路径的 execute()（含 hide_before_execute
    // 的 Destroy 入队）发生在本帧 egui_ctx.run 闭包内（ui.rs draw_command_row
    // resp.clicked() → execute → session.close() → Hidden）。Destroy 只在
    // about_to_wait 排出（本帧剩余 paint+present 阻塞主线程 2-10ms+），与
    // capture 链屏幕读取（3-20ms）竞态重叠——面板仍可见时被拍进截图。此处
    // 同步隐藏窗口（macOS orderOut 即刻生效，winit 0.30.13 window_delegate
    // is_visible 确定性 Some）并跳过本帧剩余全部工作——不 paint、不 present、
    // 不 request_redraw（L341 在下方几何同步块内，同样被跳过）。Enter 路径
    // on_palette_key 返回 true 时 L267 已早退、不跑帧循环；此处 Hidden 态只
    // 可能来自帧内执行（点击路径）或关闭后残留帧——两者都该立即隐藏。
    if session.state() == PaletteState::Hidden {
        window.set_visible(false);
        return;
    }
    ```

    **b. 插入位置语义确认**：守卫必须位于 `egui_ctx.run` 返回之后（点击已在帧内执行完毕、state 已置 Hidden）且位于 apply_textures 之前（跳过纹理上传/tessellate/paint/handle_platform_output/几何同步/L341 request_redraw——UAT missing[0]+missing[1] 同时满足）。`window` 是闭包参数 `&Arc<winit::window::Window>`，`set_visible(false)` 为 `Window::set_visible(&self, bool)`——直接调用，无锁序问题（不在 session state 锁段内）。

    **c. 不动的部分**：execute.rs 保持现状（Destroy 先于 run_command 的 FIFO 队列序——Pitfall 4）；session.close() 保持现状（state→Hidden + 返回 window_id）；repaint()（L280-282）保持 window_id 语义——close 后 window_id 为 None → no-op，关闭后无残留重绘请求；WR-04 的 sync_window_geometry Hidden 早退（L510）保持——本守卫在更上游跳过该调用，WR-04 守卫成为冗余保险而非替代。

    **d. doc 注释**：帧循环处追加 Gap 2 修复锚点注释（含「Gap 2」与「UAT test 11」字样）。
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-palette</automated>
    <automated>cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：lib.rs 的 RedrawRequested 帧循环内（`egui_ctx.run` 之后、`apply_textures` 之前）含 `session.state() == PaletteState::Hidden` 守卫与 `window.set_visible(false)` 调用、`return` 语句（三者同分支）
    - 源码断言：lib.rs 守卫处 doc 注释含 "Gap 2" 与 "UAT test 11" 字样（修复锚点）
    - 源码断言：execute.rs 未被本任务改动（git diff 确认 execute.rs 无变化——队列序保证不动）
    - 测试命令：`cargo nextest run -p mybox-palette` 退出码 0（既有 209+ 单测无回归——Hidden 守卫只影响点击帧，单测层不覆盖帧循环；行为验证由 Task 4 探针经真实闭包锁定）
    - 测试命令：`cargo check --workspace` 退出码 0 无 warning
  </acceptance_criteria>
  <done>鼠标点击执行路径在点击帧内同步隐藏窗口（set_visible(false)）并跳过本帧剩余渲染工作与 request_redraw——Destroy 排出前窗口已从屏幕消失。</done>
</task>

<task type="auto">
  <name>Task 4: E2E 探针 keyword_highlight + click_hide_before_capture + integration.rs 测试 11/12（两 gap 的端到端锁定）</name>
  <read_first>
    - crates/modules/palette/src/bin/palette_checks.rs（check_fuzzy_navigation_execute L465-583——keyword_highlight 脚手架；check_hover_click_alignment L1372-1569——click_hide_before_capture 脚手架：CursorMoved/MouseInput 注入、行带像素扫描、gated runner 释放、Destroy 收尾；PaletteHarness L47-255——inject/render_present/realize_window；main 分发 L2043-2058；usage L16）
    - crates/modules/palette/tests/integration.rs（run_check L28-37、测试 1-10 L42-164）
    - crates/modules/palette/src/lib.rs（Task 3 产物：Hidden 守卫 + set_visible(false)）
    - crates/modules/palette/src/filter.rs + ui.rs（Task 1/2 产物：keyword_hit + keyword_tag_job）
    - .planning/phases/03-命令面板/03-UAT.md（gaps[0].missing[2]——"覆盖整个 keyword 梯队 + filter 层断言 keyword 命中索引的测试"；gaps[1].missing[2]——"真实窗口点击路径 E2E 探针（断言窗口在 runner 读屏前已隐藏）"）
  </read_first>
  <files>crates/modules/palette/src/bin/palette_checks.rs, crates/modules/palette/tests/integration.rs</files>
  <action>
    **a. 新探针 `check_keyword_highlight`（Gap 1 E2E，仿 fuzzy_navigation_execute 脚手架）**：真实窗口 + 真实 on_event_win + 真实帧循环。registry 镜像生产 inventory（注册顺序含 capture.start 首位）：capture.start（"开始截图"，keywords ["截图","capture","screen","jietu"]）、builtin.quit（"退出应用"，keywords ["退出","quit","exit","tuichu"]）、builtin.open_config（"打开配置目录"，keywords ["配置","config","peizhi"]）、builtin.restart（"重启应用"，keywords ["重启","restart","chongqi"]）、builtin.open_log（"打开日志文件"，keywords ["日志","log","rizhi"]）——全部 ok_runner。阶段状态机（driver 闭包内 stage 计数）：

    - stage 0：`h.inject(RedrawRequested)?` 基线 Idle 帧，`h.non_background_pixels() > 0` 断言
    - stage 1：`s.set_input("jt")` → 断言 `s.state() == Filtering` + `s.filtered() == vec![0]`（capture.start 首位）→ `h.inject(RedrawRequested)?` 渲染过滤帧 → 扫描帧缓冲行 1 带（y ∈ round(68·scale)..round(116·scale)，scale 从 `h.window.scale_factor()` 取——hover_click_alignment L1437-1468 模式）内**精确 (255,96,0) 像素**（ACCENT #FF6000——keyword tag "jietu" 的 j/t 命中字符）计数，断言 > 0（打印实测值，阈值校准纪律同 03-04/03-06）
    - stage 2：`s.set_input("tuichu")` → 断言 `s.filtered() == vec![1]`（builtin.quit 经拼音 keyword 命中）→ `h.inject(RedrawRequested)?` → 行 1 带 ACCENT 像素 > 0（**整个 keyword 梯队渲染覆盖**——不只 capture.start）
    - stage 3：`press_key(&s, &h.handle, &ui_lock, Key::Named(NamedKey::Escape))` → 轮询 `h.handle.try_recv()` 配对 `Destroy(id == h.created_id)` → 断言 `s.state() == Hidden` + `!s.has_live_window()` → `h.pass()`
    - **覆盖声明（保持诚实）**：探针经真实窗口 + 真实帧循环断言帧缓冲中精确 ACCENT 像素存在（渲染路径锁定）；filter 层 keyword 索引断言由 Task 1 单测全量覆盖（全部 5 个拼音 keyword）；OS 级"用户肉眼看到橙色高亮"由 UAT 测试 5 人工复验

    **b. 新探针 `check_click_hide_before_capture`（Gap 2 E2E，仿 hover_click_alignment 脚手架）**：真实窗口 + 真实 on_event_win + 合成指针事件。registry：capture.start **首位**（"开始截图"，keywords ["jietu"]，`hide_before_execute: true`，`gated_runner(counter, release_rx)`——读屏侧模拟）+ 4 个 builtin（ok_runner）。阶段状态机：

    - stage 0：`h.inject(RedrawRequested)?` 基线 Idle 帧（注册行 widget）；记录 `scale = h.window.scale_factor()`
    - stage 1：注入 `CursorMoved` 到行 1 中心 logical (300, 92)·scale → `h.inject(RedrawRequested)?` hover 帧
    - stage 2：注入 `MouseInput Pressed` → `h.inject(RedrawRequested)?` → 注入 `MouseInput Released` → stage 3
    - stage 3（**核心覆盖点**）：`h.inject(RedrawRequested)?` 释放帧——点击在 egui_ctx.run 闭包内触发 execute → hide_before_execute → session.close()（Hidden + Destroy 入队）→ **Task 3 的 Hidden 守卫同步执行 `window.set_visible(false)` + 早退**。断言：
      - `s.state() == PaletteState::Hidden`（点击路径关闭面板）
      - `h.window.as_ref().ok_or("window not realized")?.is_visible() == Some(false)`——**窗口服务器视角的隐藏断言**（macOS orderOut 即时，winit is_visible 确定性 Some；打印实际值，若平台返回 None 视为失败并附实测——诚实校准）
      - `counter.load(SeqCst) == 0`——gated runner（模拟截图读屏）尚未开始——**窗口隐藏先于读屏**（UAT gaps[1].missing[2] 的直接证据）
      - `Destroy(id == h.created_id)` 已入队（try_recv 轮询，drain Redraw 杂散）
    - stage 4：`release_tx.send(())` 释放 gate → 轮询 `counter == 1`（runner 恰好一次）→ 短暂 sleep 后断言 handle 队列无二次 Destroy（finalize 被 generation/state 守卫 no-op——capture_hides_first 同断言）→ `h.pass()`
    - **覆盖声明（保持诚实）**：探针经真实窗口 + 真实 on_event_win 闭包驱动点击→execute→Hidden→set_visible(false) 全链路；`is_visible() == Some(false)` 断言窗口服务器在 runner 读屏（gated 模拟）前已隐藏；OS 合成器级"截图画面绝不含面板"最终 truth 由 UAT 测试 11 人工复验

    **c. main() 分发 + usage**：L2043-2058 的 match 加两个臂 `"keyword_highlight" => check_keyword_highlight(),` 与 `"click_hide_before_capture" => check_click_hide_before_capture(),`；usage 字符串（L16 + L2058）各追加两个新探针名。模块 doc（L16）同步。

    **d. integration.rs 测试 11/12**：追加 `#[test] #[ignore] fn palette_keyword_highlight() { run_check("keyword_highlight"); }`（doc：Gap 1 / UAT test 5 回归——真实窗口断言 keyword 标签 ACCENT 像素）与 `#[test] #[ignore] fn palette_click_hide_before_capture() { run_check("click_hide_before_capture"); }`（doc：Gap 2 / UAT test 11 回归——真实窗口点击路径断言 is_visible()==Some(false) 先于 gated 读屏）。既有测试 1-10 不动。

    **不动**：既有 10 个探针函数与对应 integration 测试 1-10——本任务只新增两个探针。
  </action>
  <verify>
    <automated>cargo nextest run -p mybox-palette -p mybox-core</automated>
    <automated>cargo build -p mybox-palette --bin palette_checks</automated>
    <automated>cargo test -p mybox-palette --test integration -- --ignored</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：palette_checks.rs 含 `fn check_keyword_highlight`（内含 `set_input("jt")`、`filtered() == vec![0]`、`set_input("tuichu")`、`filtered() == vec![1]`、行 1 带 ACCENT 像素计数断言）
    - 源码断言：palette_checks.rs 含 `fn check_click_hide_before_capture`（内含 `is_visible() == Some(false)` 断言——或等价 `is_visible().unwrap_or(true) == false` 诚实形态、`PaletteState::Hidden` 断言、`gated_runner` 释放、二次 Destroy 缺席断言）
    - 源码断言：palette_checks.rs main() 分发 match 含 `"keyword_highlight"` 与 `"click_hide_before_capture"` 两臂；usage 字符串（L16 与 L2058）均含两个新探针名
    - 源码断言：integration.rs 含 `fn palette_keyword_highlight` 与 `fn palette_click_hide_before_capture`（均 `#[ignore]` + `run_check` 接线）
    - 测试命令（headless 编译与单测）：`cargo nextest run -p mybox-palette -p mybox-core` 退出码 0
    - 测试命令（编译）：`cargo build -p mybox-palette --bin palette_checks` 退出码 0
    - 测试命令（桌面会话，verify-phase 重跑）：`cargo test -p mybox-palette --test integration -- --ignored` 12/12 通过——其中 `keyword_highlight` 实跑断言行 1 带 ACCENT 像素 > 0（jt 与 tuichu 两阶段）；`click_hide_before_capture` 实跑断言点击后 `is_visible() == Some(false)` 且 gated 读屏计数器为 0、Destroy 已入队、释放后无二次 Destroy
  </acceptance_criteria>
  <done>两个新 E2E 探针经真实窗口 + 真实闭包锁定两 gap 修复路径；12/12 集成测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 输入查询 → filter 梯队数据 | keyword 梯队新增 keyword_hit 数据通道（keyword 字符串 + 字符索引）——索引正确性与梯队归属 |
| filter 数据 → UI 渲染 | keyword tag 在 description 行内追加渲染——行宽/行高不变式与 ACCENT 保留区纪律 |
| 点击帧（egui 帧内 execute）→ 窗口可见性 | Hidden 守卫 set_visible(false) 的时序——窗口服务器视角隐藏先于 Destroy 排出与截图读屏 |
| 帧循环早退 → 帧工作跳过 | Hidden 守卫 return 跳过 paint/present/request_redraw——跳过面的正确性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-25 | Tampering (keyword 索引越界/错位) | filter.rs KeywordHit.indices | mitigate | indices 仅由 `fuzzy_indices` 产出（与 name/description 同机制）；UI 层经既有 `char_indices_to_byte_ranges` 转换（含越界跳过 + 连续段合并，4 项既有单测覆盖）——越界/错位位置被转换层吞掉，无 panic 面 |
| T-03-26 | Denial of Service (keyword tag 撑爆行宽/行高) | ui.rs keyword_tag_job 渲染 | accept | 标签在 description 同一行内追加（不新增行——row_geometry_fits_48px 不变式守护）；拼音 keyword 均为短 ASCII（≤6 字符），PANEL_WIDTH 600 内余量充足；最坏情况文本溢出被 painter clip，无布局破坏 |
| T-03-27 | Tampering (race：Hidden 守卫 vs repaint/request_redraw) | lib.rs 帧循环守卫 | mitigate | 守卫在 egui_ctx.run 返回后立即判断（点击帧内 execute 已置 Hidden——单线程主循环无跨线程竞态）；repaint() 基于 window_id（close 后 None → no-op，无残留重绘请求）；L341 request_redraw 在守卫后被跳过（守卫 return 使该行不可达） |
| T-03-28 | Reliability (set_visible(false) 对已销毁/已隐藏窗口调用) | lib.rs window.set_visible(false) | accept | 守卫先于 about_to_wait 的 Destroy 排出执行（窗口 Arc 仍存活）；winit set_visible 幂等——对已隐藏窗口重复调用无害；Destroy 排出后窗口关闭，无二次可见性操作 |
| T-03-29 | Repudiation (探针断言与生产路径一致性) | check_click_hide_before_capture is_visible 断言 | mitigate | 探针经真实窗口 + 真实 on_event_win 闭包驱动点击→execute→Hidden→守卫全链路（不绕过 lib.rs 任何代码路径）；覆盖声明已明示合成指针事件限制 + OS 合成器级 truth 由 UAT 测试 11 人工复验——诚实性已校准（is_visible 平台返回 None 时按失败处理并打印实测） |
| T-03-SC | Tampering | cargo 安装 | accept | 本计划零新增依赖（无 package-manager install 任务）——无需 slopcheck 与 human checkpoint |
</threat_model>

<verification>
- `cargo nextest run -p mybox-palette filter::`：filter 单测全绿（含 keyword_hit 新增 4 项断言 + 既有 7 项无回归——Gap 1 数据层）
- `cargo nextest run -p mybox-palette ui::`：ui 单测全绿（含 keyword_tag_job 新增断言 + row_interact 补参后无回归——Gap 1 渲染层）
- `cargo nextest run -p mybox-palette`：palette 全量单测全绿（含 Task 3 Hidden 守卫无回归）
- `cargo nextest run -p mybox-palette -p mybox-core`：headless 编译与单测全绿
- `cargo nextest run --workspace`：全工作区单测全绿（与 03-09 同基线 + 新增断言）
- `cargo check --workspace`：exit 0 无 warning
- 桌面会话（verify-phase 重跑）：`cargo test -p mybox-palette --test integration -- --ignored` 12/12 通过——`keyword_highlight` 实跑断言 jt/tuichu 两阶段行 1 带 ACCENT 像素 > 0；`click_hide_before_capture` 实跑断言点击后 is_visible()==Some(false) + gated 读屏计数器 0 + Destroy 已入队 + 释放后无二次 Destroy
- QUALITY 账本目标：03-10 关闭后，03-UAT.md 的 2 个 gap（gaps[0] keyword 高亮 minor、gaps[1] 点击截图时序 major）全部从 failed 转 closed；debug 会话两个文件标记 fix 已落地
- Human 最终复验：UAT 测试 5 重跑（输入 jt/jietu/tuichu 观察橙色高亮）+ UAT 测试 11 重跑（鼠标点击「开始截图」观察面板先关闭且截图画面绝不含面板）——OS 物理链路与肉眼视觉只能人工验证
</verification>

<success_criteria>
- Gap 1 关闭（UAT test 5）：keyword 梯队命中携带 keyword 字符串 + 字符索引（filter 层单测断言全部 5 个拼音 keyword）；行内渲染「 · {keyword}」标签且命中字符 #FF6000（E2E 探针帧缓冲 ACCENT 像素断言 + UAT 人工复验）
- Gap 2 关闭（UAT test 11）：鼠标点击执行「开始截图」时窗口在点击帧内同步 set_visible(false)（探针断言 is_visible()==Some(false)），Destroy 排出与截图读屏前已从屏幕消失；Enter 与点击两路径时序一致；无二次 Destroy
- UI-SPEC 修订落地：CommandRow 规格 + Accent 保留区文案扩展（Route A 渲染元素规格化），「只变色不变字号字重」「Nothing else」不变式保持
- 探针覆盖洞关闭：点击路径屏幕时序不再只靠 headless 入队序断言（GAP-2 教训——headless 探针让缺陷在 10/10 全绿下漏过）；keyword 高亮覆盖整个梯队而非单命令
- SPEC 边界未扩大：无新增依赖、无自研拼音转换引擎；filter/execute/session 状态机结构不变；仅 UI-SPEC.md 一处文档修订
- 四任务原子提交：Task 1（filter.rs keyword_hit）、Task 2（ui.rs + UI-SPEC）、Task 3（lib.rs 守卫）、Task 4（探针 + integration）各自 cargo 验证一次通过、零 warning；共享文件（palette_checks.rs/integration.rs）统一提交
- Phase 03 完整收尾：UAT 15/15 全 pass（2 个 issue 全部关闭）；REQUIREMENTS PAL-01..PAL-05 全部 Complete；剩余 deferred（WR-03/IN-01/IN-02/IN-04）归 Phase 4
</success_criteria>

<output>
创建 `.planning/phases/03-命令面板/03-10-SUMMARY.md`（含两 gap 修复记录、keyword 梯队高亮数据+渲染双层修复说明、点击路径 Hidden 守卫修复说明、两个新探针覆盖声明、UI-SPEC 修订说明、spec 边界声明、验证证据）
</output>