---
phase: 03-命令面板
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
autonomous: true
gap_closure: true
files_modified:
  - crates/modules/palette/src/raster.rs
  - crates/modules/palette/src/session.rs
  - crates/modules/palette/src/bin/palette_checks.rs
  - crates/modules/palette/tests/integration.rs
  - .planning/REQUIREMENTS.md
requirements: [PAL-02]

must_haves:
  truths:
    - "面板内所有文字（中文命令名、描述、占位符「输入命令…」）渲染为可识别的字形，而非纯色块（GAP-2 症状消除）"
    - "多帧渲染（增量字形进入字体图集后）文字仍保持正确——partial 图集补丁不再破坏已光栅化字形"
    - "命令列表可辨识地列出全部已注册命令（PAL-02 的视觉呈现成立）"
  artifacts:
    - path: "crates/modules/palette/src/raster.rs"
      provides: "solid/textured 路径按 UV 判别（WHITE_UV 契约），字形三角形进入纹理采样路径"
      contains: "WHITE_UV"
    - path: "crates/modules/palette/src/session.rs"
      provides: "apply_textures 处理 ImageDelta.pos——partial 图集补丁原位写入，full 整图替换"
      contains: "change.pos"
    - path: "crates/modules/palette/src/bin/palette_checks.rs"
      provides: "E2E 探针 glyph_shape（真实窗口 3 帧渲染 + 字形结构断言：非纯色块）"
      contains: "glyph_shape"
  key_links:
    - from: "egui tessellate 输出（ClippedPrimitive）"
      to: "raster::paint 三角形路径分派"
      via: "uses_texture = 任意顶点 uv != WHITE_UV (0,0)（epaint lib.rs:84-88 契约）——颜色相等不可作为判别（字形三角形颜色统一）"
      pattern: "WHITE_UV"
    - from: "full_output.textures_delta（epaint TextureAtlas::take_delta 的 partial 补丁）"
      to: "session 纹理表"
      via: "apply_textures 按 ImageDelta.pos 分支：None 整图替换 / Some([x,y]) 原位行拷贝补丁"
      pattern: "change.pos"
    - from: "session 纹理表快照"
      to: "paint_textured_triangle 双线性 UV 采样"
      via: "textures.get(&mesh.texture_id) + FontImage coverage→RGBA 展开"
      pattern: "texture_id"
---

<objective>
修复 GAP-2（BLOCKER）：真实桌面上窗口内所有文字（含中文命令名）渲染为灰色方块、字形无法识别。

Purpose: 根因有二（均为代码层缺陷，headless 测试只断言"存在非背景像素"故漏过）：
1. **路径判别错误（主因，源码已证实）**：`raster::paint`（raster.rs:97）以 `v0.color == v1.color && v1.color == v2.color` 判别 solid/textured 路径。egui 字形三角形的三个顶点颜色相同（文本颜色）→ 永远走 solid 路径 → 字体图集从不被采样 → 文字渲染为文本颜色的纯色三角块（名字白色块/描述 TEXT_DIM #A8A8A8 灰块）。epaint 的契约是 UV：solid mesh 的 uv 为 `WHITE_UV`(0,0)（epaint-0.30.0/src/lib.rs:84-88），textured 判别 = 任意顶点 uv != WHITE_UV。
2. **图集补丁破坏（次要，源码已证实）**：epaint `TextureAtlas::take_delta`（texture_atlas.rs:188-201）在字形增量光栅化（无 resize）时发出 `ImageDelta::partial(pos, region, ...)`；`session.apply_textures`（session.rs:290-298）忽略 `change.pos` 用补丁**整图替换**纹理表 → 首帧之后所有字形 UV 采样错位（采到补丁外的垃圾像素）。

Output: raster 判别修复 + apply_textures 原位补丁 + 单元测试（含 RED 复现）+ E2E `glyph_shape` 探针（verify-phase 桌面会话可重跑）+ REQUIREMENTS.md PAL-02 同步。
</objective>

<execution_context>
@/Users/fish/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/fish/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-命令面板/03-VERIFICATION.md
@.planning/phases/03-命令面板/03-HUMAN-UAT.md
@.planning/phases/03-命令面板/03-02-SUMMARY.md
@.planning/phases/03-命令面板/03-PATTERNS.md

Gap 事实（03-HUMAN-UAT.md 测试 5）："窗口内文字全是灰色块形状，无法识别（字形渲染失败，疑似字体/纹理问题）"。
关键代码：crates/modules/palette/src/raster.rs:93-118（分派）、session.rs:290-298（apply_textures）、ui.rs:17-27（颜色 token：TEXT 白 / TEXT_DIM #A8A8A8 灰 / PLACEHOLDER #6E6E6E）。
依赖源码事实（勿再读源码）：epaint-0.30.0 lib.rs:84 `pub const WHITE_UV: Pos2 = pos2(0.0, 0.0)` + 88 行注释（"use TextureId::Managed(0) and the WHITE_UV for uv-coord"）；texture_atlas.rs take_delta 的 full/partial 分支；`FontImage { pub size: [usize;2], pub pixels: Vec<f32> }`（coverage 0..1）；`ImageDelta { pub image: ImageData, pub options: TextureOptions, pub pos: Option<[usize;2]> }`；`egui::TextureOptions`/`egui::TexturesDelta` 由 egui 根 re-export（egui-0.30.0 lib.rs:453-466）。
注意：03-03 已改 palette_checks.rs 的 realize_window（on_created 配对）与 session.rs（新增 on_window_created）——本计划在其成果之上叠加，勿回退。
</context>

<tasks>

<task type="auto">
  <name>Task 1: raster 三角形路径判别改 UV——字形进入纹理采样路径</name>
  <read_first>
    - crates/modules/palette/src/raster.rs（paint 54-130、paint_textured_triangle 213-314、tests 350-535）
    - crates/modules/palette/src/ui.rs（17-27 颜色 token、261-329 命令行渲染——理解文本网格形状）
    - crates/modules/palette/src/fonts.rs（CJK 安装与 has_glyphs 测试形态）
  </read_first>
  <files>crates/modules/palette/src/raster.rs</files>
  <action>
    **判别修复**：`paint`（raster.rs:93-118）中把

    `if v0.color == v1.color && v1.color == v2.color { paint_solid_triangle(...) } else { paint_textured_triangle(...) }`

    替换为 UV 判别（epaint 契约，同 eframe 语义）：

    `let uses_texture = mesh.texture_id != egui::TextureId::default() || v0.uv != egui::epaint::WHITE_UV || v1.uv != egui::epaint::WHITE_UV || v2.uv != egui::epaint::WHITE_UV;`
    `if !uses_texture { paint_solid_triangle(...) } else { paint_textured_triangle(...) }`

    注释写明 GAP-2 根因：字形三角形顶点颜色相同（文本色），颜色相等≠无纹理；epaint 保证 solid mesh 的 uv 恒为 WHITE_UV(0,0)（lib.rs:84-88）。paint_solid_triangle / paint_textured_triangle 函数体不变。

    **新增 RED 测试 `textured_dispatch_uses_uv_not_color`**（走公共入口 `paint`，直接复现根因）：构造 2 色棋盘纹理（`egui::epaint::ImageData::Color(Arc::new(egui::epaint::ColorImage { size: [2, 2], pixels: vec![Color32::WHITE, Color32::BLACK, Color32::BLACK, Color32::WHITE] }))` 存入 textures 表，key `TextureId::Managed(0)`）；三角形三顶点颜色全部 `Color32::WHITE`（相等！），uv 分别为 (0,0)、(1,0)、(0,1)，texture_id Managed(0)；100x100 帧缓冲调用 `paint(&mut framebuffer, &prims, &textures, 1.0)`。断言：输出中非透明像素的**不同 (r,g,b,a) 值种类 ≥ 2**（修复前走 solid 路径输出单一白色——测试必红；修复后棋盘纹理产生黑白两种输出——必绿）。

    **强化既有测试 `paint_renders_chinese_label`**（现在只断言 non-transparent > 0，纯色块也能通过）：paint 之后，遍历帧缓冲取非透明像素 (alpha>0) 的最小/最大 x/y 得到字形 bbox；断言 bbox 存在（宽高均 ≥ 4 物理像素）；bbox 内统计：(a) alpha==255 的像素占比 ∈ (0.02, 0.7)（真实字形笔画覆盖率，纯色块 ≈ 1.0 必红）；(b) 不同 (r,g,b,a) 值种类 ≥ 16（字形抗锯齿边缘产生大量灰阶，纯色块 ≤ 3 必红）。
  </action>
  <acceptance_criteria>
    - 源码断言：raster.rs 的 `paint` 中含 `uses_texture` 判别且条件含 `egui::epaint::WHITE_UV`（三处顶点 uv 比较 + texture_id 比较）
    - 源码断言：raster.rs tests 含 `fn textured_dispatch_uses_uv_not_color`
    - 测试命令：`cargo nextest run -p mybox-palette raster` 退出码 0（含 2 个新/强化测试与既有 4 个测试全部通过；`solid_fast_path_matches_barycentric` 等直调子函数的既有测试不受分派改动影响）
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-palette raster</automated>
  </verify>
  <done>字形三角形经纹理路径采样字体图集；纯色（无纹理）形状仍走 solid 快速路径。文本不再是纯色块。</done>
</task>

<task type="auto">
  <name>Task 2: apply_textures 支持 partial 图集补丁原位写入</name>
  <read_first>
    - crates/modules/palette/src/session.rs（apply_textures 290-298、textures 表 300-303、tests 模块）
    - crates/modules/palette/src/raster.rs（31-48 texture_pixels——FontImage/C 变体形态参照）
    - crates/modules/palette/src/lib.rs（帧循环 275-293 apply_textures 调用点——确认签名兼容）
  </read_first>
  <files>crates/modules/palette/src/session.rs</files>
  <action>
    **apply_textures 改写**（session.rs:290-298，签名保持 `pub fn apply_textures(&self, delta: egui::TexturesDelta)`）：`delta.set` 循环内按 `change.pos` 分支——

    - `None` → `inner.textures.insert(id, change.image);`（整图替换，现状行为不变）
    - `Some([x, y])` → 若表中已有该 id 的 `ImageData`，**原位补丁**：匹配双变体拷贝——`ImageData::Font(existing)` 与 `ImageData::Font(patch)`：把 patch 的 `pixels: Vec<f32>` 按行写入 existing 的 `pixels`（行宽 = existing.size[0]，patch 行宽 = patch.size[0]，起点 offset = y*existing.size[0] + x）；`ImageData::Color` 同理但每像素 4 字节 RGBA 步长（行宽/offset ×4）。existing.size 保持不变。补丁越界（x+patch.size[0] > existing.size[0] 或 y 越界）时 `mybox_core::log::warn!` 并跳过越界行/整个补丁（绝不 panic）。表中尚无该 id 时退化 insert patch image（防御分支）。

    doc 注释写明根因：epaint `TextureAtlas::take_delta`（texture_atlas.rs:188-201）在字形增量光栅化时发 `ImageDelta::partial`，整图替换会破坏已光栅化字形（GAP-2 次因）。

    **新增单测 `apply_textures_patches_partial_font_delta_in_place`**：先经 `egui::TexturesDelta { set: vec![(TextureId::Managed(0), egui::epaint::ImageDelta::full(egui::epaint::ImageData::Font(Arc::new(egui::epaint::FontImage { size: [4, 4], pixels: vec![0.25; 16] })), egui::TextureOptions::LINEAR))], free: vec![] }` 播种 4x4 图集；再 apply partial：`ImageDelta::partial([1, 1], ImageData::Font(Arc::new(FontImage { size: [2, 2], pixels: vec![0.9; 4] })), TextureOptions::LINEAR)`；断言存储的 FontImage `size == [4, 4]`、(1,1)==0.9、(2,2)==0.9、(0,0)==0.25、(3,3)==0.25（其余像素未被补丁触碰）。**新增 `apply_textures_full_delta_replaces_whole_image`**：full delta 后断言 size 与内容完全等于新图。修复前（整图替换）partial 测试断言 size==[4,4] 必红。
  </action>
  <acceptance_criteria>
    - 源码断言：session.rs `apply_textures` 函数体内含对 `change.pos` 的 match/分支，`Some([x, y])` 分支含原位行拷贝逻辑且含越界 warn 处理
    - 源码断言：session.rs tests 含 `fn apply_textures_patches_partial_font_delta_in_place` 与 `fn apply_textures_full_delta_replaces_whole_image`
    - 测试命令：`cargo nextest run -p mybox-palette session` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-palette session</automated>
  </verify>
  <done>增量字形进入图集时（partial delta）纹理表保持完整尺寸、已光栅化字形采样不受影响。</done>
</task>

<task type="auto">
  <name>Task 3: E2E 探针 glyph_shape + 集成测试接线 + REQUIREMENTS.md PAL-02 同步</name>
  <read_first>
    - crates/modules/palette/src/bin/palette_checks.rs（check_summon_render 348-420 脚手架、non_background_pixels 131-140、main 705-726——注意 03-03 已将 realize_window 改为 on_created 配对，勿回退）
    - crates/modules/palette/tests/integration.rs（run_check 模板 28-37）
    - .planning/REQUIREMENTS.md（PAL-02 行 40、traceability 行 113）
    - crates/modules/palette/src/ui.rs（56-64 几何表、258-329 命令行渲染——探针断言依据）
  </read_first>
  <files>crates/modules/palette/src/bin/palette_checks.rs, crates/modules/palette/tests/integration.rs, .planning/REQUIREMENTS.md</files>
  <action>
    **a. 新探针 `check_glyph_shape`**（真实窗口 + 真实渲染链，仿 check_summon_render 脚手架）：registry 注册 2 个 fake 命令，第一个 name 为中文 `"开始截图"`、description 为 `"截取屏幕区域"`（CJK 字形必须走纹理路径）。`summon_palette` → `expect_create` → `realize_window` → 连续驱动 **3 个** RedrawRequested 帧（每帧 `h.inject(WindowEvent::RedrawRequested)`——多帧使 egui 发出增量图集补丁，覆盖 GAP-2 次因）；第 3 帧后从 session 帧缓冲（`with_framebuffer`）读像素并断言**字形结构**（纯色块回归的直接判据）：

    - 非背景像素数 > 0（背景 = #202020 即 (32,32,32)，沿用 non_background_pixels 的判定：alpha>0 且 ≠(32,32,32)）
    - 对全部非背景像素求 bbox（min/max x/y），断言 bbox 宽高均 ≥ 8 物理像素
    - bbox 内 alpha==255 的完全覆盖像素占比 < 0.7（纯色块 ≈ 1.0 必红；真实字形 0.02~0.6）
    - bbox 内不同 (r,g,b,a) 值种类 ≥ 16（纯色块仅 1~3 种必红）
    - bbox 内至少存在 1 个 alpha==255 像素（字形笔画确实落地）

    断言通过后 `press_key(Escape)` 关窗、pass。main() 分发 match 与 usage 字符串各加 `"glyph_shape"`。

    **b. integration.rs**：新增 `#[test] #[ignore] fn palette_glyph_shape() { run_check("glyph_shape"); }`（注释注明 PAL-02/GAP-2 回归）。

    **c. REQUIREMENTS.md 同步（PAL-02）**：第 40 行 `- [ ] **PAL-02**: 命令面板列出所有模块注册的命令` 改为 `- [x] **PAL-02**: 命令面板列出所有模块注册的命令`；第 113 行 `| PAL-02 | Phase 3 | Pending |` 改为 `| PAL-02 | Phase 3 | Complete |`；末尾 `*Last updated: 2026-08-15 — 03-03 gap closure (PAL-01)*` 改为 `*Last updated: 2026-08-15 — 03-03/03-04 gap closure (PAL-01, PAL-02)*`。
  </action>
  <acceptance_criteria>
    - 源码断言：palette_checks.rs 含 `fn check_glyph_shape`、≥3 帧注入、bbox/覆盖率/值种类断言；main() match 与 usage 含 `glyph_shape`
    - 源码断言：integration.rs 含 `fn palette_glyph_shape`（`#[ignore]`）
    - 源码断言：REQUIREMENTS.md 第 40 行为 `- [x] **PAL-02**:`，第 113 行为 `| PAL-02 | Phase 3 | Complete |`
    - 测试命令（headless）：`cargo nextest run -p mybox-palette` 退出码 0
    - 测试命令（桌面会话，verify-phase 重跑）：`cargo test -p mybox-palette --test integration -- --ignored` 6/6 通过（5 既有含 03-03 探针 + glyph_shape）
  </acceptance_criteria>
  <verify>
    <automated>cargo nextest run -p mybox-palette</automated>
    <automated>cargo build -p mybox-palette --bin palette_checks && cargo test -p mybox-palette --test integration -- --ignored</automated>
  </verify>
  <done>真实窗口中文字帧缓冲呈字形结构（非纯色块）；3 帧后（含增量图集补丁）仍正确；PAL-02 文档状态为 Complete。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| egui tessellator 输出 → raster::paint | ClippedPrimitive 网格/UV/颜色数据进入光栅化（本地渲染数据） |
| epaint 纹理 delta → session 纹理表 | ImageDelta（full/partial）写入纹理存储，采样时读取 |
| session 纹理表 → 像素输出 | paint_textured_triangle 双线性采样写入帧缓冲 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering (数据完整性) | `session.apply_textures` partial 补丁越界写 | mitigate | 补丁按 `ImageDelta.pos` 原位行拷贝 + 越界时 warn-and-skip（绝不 panic/越界写）；单测锁定 4x4 图集上 [1,1] 2x2 补丁的正确边界 |
| T-03-06 | Availability (渲染错误) | `raster::paint` 路径判别错误导致字形永不采样 | mitigate | UV（WHITE_UV）判别替代颜色相等判别；`textured_dispatch_uses_uv_not_color` + `paint_renders_chinese_label` 结构断言（覆盖率 ∈ (0.02,0.7)、值种类 ≥16）锁定 |
| T-03-07 | Availability | 零尺寸纹理 / 缺失 texture_id 采样 | accept | 既有守卫不变（paint_textured_triangle 的 `tex_w==0||tex_h==0` 早退 + `textures.get` else-skip）——本计划不引入新暴露面 |
| T-03-SC | Tampering | npm/pip/cargo 安装 | accept | 本计划零新增依赖（无 package-manager install 任务） |

</threat_model>

<verification>
- `cargo nextest run -p mybox-palette`：全部单测通过（含 UV 判别 RED 测试、图集补丁测试、强化字形结构断言）
- `cargo check --workspace`：exit 0 无 warning
- 桌面会话（verify-phase）：`cargo test -p mybox-palette --test integration -- --ignored` 6/6 通过，其中 `glyph_shape` 在真实窗口上断言字形结构（非纯色块）且 3 帧后仍正确
- 真实桌面人工复验（UAT 重跑测试 5）：面板内中文命令名、描述、占位符均显示可识别的字形，无灰色方块
</verification>

<success_criteria>
- GAP-2 的两个根因均被消除：字形经 UV 判别进入纹理采样路径；partial 图集补丁原位写入不再破坏已光栅化字形
- `glyph_shape` E2E 探针可在桌面会话重复运行并断言字形结构（覆盖率 < 0.7、值种类 ≥ 16、存在完全覆盖像素）
- PAL-02 在 REQUIREMENTS.md 中标记 Complete（实现与文档一致）
</success_criteria>

<output>
创建 `.planning/phases/03-命令面板/03-04-SUMMARY.md`（含 requirements-completed: [PAL-02]、gap 修复记录、验证证据）
</output>
